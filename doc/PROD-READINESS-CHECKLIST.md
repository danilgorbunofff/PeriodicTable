# Prod Readiness Checklist — periodictable.lol

> Goal: prove the project is ready to meet new users. Work top to bottom.
> Mark each box only with evidence (command output, screenshot, inbox mail).
> Never commit `.env`. Fresh machine: `git clone`, copy `.env` values from a safe place, `npm ci`.
> Live: https://www.periodictable.lol · Vercel project `periodic-table` · Neon Postgres (122 elements).
>
> **Product review set:** [`doc/review/`](review/00-REVIEW-PLAN.md) — the phase-by-phase review of the whole app, from a crawler's first request to end of life. The plan is `doc/review/00-REVIEW-PLAN.md`; findings land in `doc/review/FINDINGS.md`. This checklist remains the launch gate and the source of settled facts; where a review finding changes a gate answer, the line below cites its `R<phase>-<n>` id rather than restating it.

## 0. Pre-flight (any machine)

- [x] `node -v` = 20+ (22 preferred), `npm -v` present, `psql`/`curl`/`python3` present — ✅ 2026-09-11 on **this** machine. The heading used to say "(new PC)"; these are machine checks, and this is the machine that would launch, so they were run here rather than deferred: `node v25.9.0`, `npm 11.12.1`, `psql` `/opt/homebrew/bin/psql`, `curl` `/usr/bin/curl`, `python3` `/opt/homebrew/bin/python3`. ⚠️ **Nothing in the repo pins Node, and local is a different major than prod.** `package.json` has **no `engines` field**; `GET /v9/projects/prj_5pkunCWQkezWWWbQ9N9xJ5q9p69Y` → **`nodeVersion: "24.x"`** (with `buildCommand: null`, `framework: nextjs`). So dev/tests run **25.9.0** while prod builds run **24.x**. Both currently succeed (this machine: tsc clean, next lint clean, 327 passed; prod: 12/12 builds 1.1–1.6 min), so this is an **undocumented divergence, not a live defect** — but it is worth knowing before running anything locally that can reach prod, per item 13.
- [x] `git clone <repo>` + `npm ci` clean (no errors) — ✅ 2026-09-11, **actually done**, not assumed: `git clone https://github.com/danilgorbunofff/PeriodicTable.git` into `$(mktemp -d)` → HEAD **`7696c66`** → **`npm ci` exit 0**, 381 packages in `node_modules`. Then proved the clone is **usable**, not merely installed: `npx prisma generate` OK → **`npx tsc --noEmit` clean**. So the whole dependency graph resolves from `package-lock.json` alone, with no local artifacts propping it up. ⚠️ `npm ci` also prints **6 vulnerabilities (2 moderate, 3 high, 1 critical)** — every one is **Next.js**, and the only fix is `next@16.3.5`, a **major** bump from 14 (`npm audit fix --force` says "breaking change"). That is a real pre-launch question but a **separate** one: it is **not** an `npm ci` error, and `npm ci` exited 0. The item says "no errors", not "no output".
- [x] `.env` present locally, NOT committed (`git status --porcelain` shows no `.env`) — ✅ 2026-09-11: `.env` exists; `git check-ignore -v .env` → **`.gitignore:29:.env`**; the only tracked env file is **`.env.example`** (correct — that is the template, and it is meant to be tracked). ⚠️ `git ls-files | grep -c '\.env'` returns **1** and *looks* like a leaked `.env`; that 1 is `.env.example`. List the matches, do not count them — a bare count cannot tell a template from a secret.
- [x] `npx prisma migrate deploy` (local DB) + `npx prisma db seed` succeed — ✅ 2026-09-11 on a throwaway local DB: all 6 migrations applied, then `seed ok: 122 elements, 8 startups, 26 stakes, 26 activity rows`. ⚠️ **Both commands read `.env` → Neon prod if `DATABASE_URL` is not overridden** (shell env beats `.env`), and `prisma db seed` is the **demo** seeder (`prisma/seed.ts`, 8 startups + 26 mock stakes) with **no environment guard** — it is exactly what `npm run db:clear-demo` exists to undo. The launch seeder is a separate, manual file (`prisma/launch-seed.ts`). Always pass `DATABASE_URL=` explicitly.
- [x] Vercel → project `periodic-table` → Build Command includes `prisma migrate deploy` — **ANSWERED, and the answer is NO — deliberately.** ✅ 2026-09-11 via the API: `GET /v9/projects/prj_5pkunCWQkezWWWbQ9N9xJ5q9p69Y` returns **`buildCommand: null`**, i.e. there is **no dashboard override** (this was previously "a dashboard setting and invisible from this repo" — it is now known). So Vercel runs the `package.json` `build` script, which is **`prisma generate && next build`**: `generate` yes, **`migrate deploy` no**. Prod's schema is nevertheless current (`npx prisma migrate status` → *"Database schema is up to date!"*, 6 migrations) because it was migrated **by hand**. ⚠️ **Do NOT "fix" this by adding `migrate deploy` to the Build Command while `DATABASE_URL` is shared with Preview** — `vercel env ls` shows `DATABASE_URL` scoped **`Production, Preview`** (the same Neon database), so a build-time migration would run **on every preview build**, including one from an unmerged branch ⇒ a branch could migrate prod. Either (a) keep migrations a **manual/CI step** (`npm run db:deploy` against prod *before* promoting a deploy that ships a migration), or (b) first split the environments (separate Preview `DATABASE_URL`, e.g. a Neon branch) **and then** add it. The risk this line exists to catch is real and asymmetric: a deploy that ships a migration without applying it serves code expecting a column that does not exist. **RESOLVED 2026-09-14, by the guard this item asked for — `VERCEL_ENV`, in the build script rather than the dashboard.** `npm run build` is now **`prisma generate && node scripts/migrate-if-production.mjs && next build`**, and that script runs `prisma migrate deploy` when — and only when — **`VERCEL_ENV === "production"`** (logged skip otherwise: local builds, CI, previews). The manual prod step in option (a) is therefore gone while option (b)'s risk stays excluded: sharing `DATABASE_URL` with Preview no longer matters once the build refuses to migrate outside production, so a preview of an unmerged branch still cannot migrate prod. `npm run db:deploy` remains how local and CI databases are migrated, and a failed migration fails the build — which is the point, since the alternative is deploying code whose schema was never applied.

## 1. Codebase gates (automated)

| Check | Command | Pass |
| --- | --- | --- |
| Lint | `npm run lint` | 0 errors |
| Types | `npm run typecheck` | 0 errors |
| Tests | `TEST_DATABASE_URL=<local-pg> npm run test:ci` | all pass, **0 skipped** |
| Vulns | `npm run audit:prod` | `no unaccepted high/critical` |
| Prod-env fail-closed | `VERCEL_ENV=production NODE_ENV=production node scripts/check-prod-env.mjs` (no secrets) | exits 1 listing missing keys |
| Prod-env pass | same command **with** all prod secrets exported | `production config OK` |
| Cron file present | `cat vercel.json` | 2 daily crons (outbox 04:00, screenshot 04:30) — Vercel cannot create crons from the dashboard, and Hobby allows only 2 jobs at once-a-day frequency |

> ✅ **2026-09-11 — the whole gate is green with zero skips, for the first time on a real Postgres.** `npm run test:ci` → **314 passed (21 files), 0 skipped**; `npm run typecheck` exit 0; `npm run lint` 0 warnings/errors. This retires the DB suite that had been skipped indefinitely — including the new *"a Standard Webhooks delivery settles too"* case, which passes end-to-end (signature → `ProviderEvent` → `settlePayment` → stake applied). Both envelopes are exercised in one run: `verified via legacy signature` ×19, `verified via standard signature` ×1.
>
> Recipe — the container is `pt-test-pg` on port **55432** (`testu`/`testpw`; `-U postgres` fails, that role does not exist):
> ```bash
> PGPASSWORD=testpw psql -h 127.0.0.1 -p 55432 -U testu -d pttest -c "CREATE DATABASE pt_verify;"
> DATABASE_URL="postgresql://testu:testpw@127.0.0.1:55432/pt_verify" npx prisma migrate deploy
> TEST_DATABASE_URL="postgresql://testu:testpw@127.0.0.1:55432/pt_verify" npm run test:ci
> ```
> ⚠️ **Mind the asymmetry: `migrate deploy` reads `DATABASE_URL`, the suite reads `TEST_DATABASE_URL`** (which `lib/testDb.ts` copies onto `DATABASE_URL` for the app's Prisma singleton). Setting *only* `TEST_DATABASE_URL` makes `prisma` fall back to `.env` → **Neon production**. Today that is a harmless no-op (`No pending migrations to apply`), but the day a migration is pending it would apply it **to prod**. Always set both.

Known gaps (do NOT flip real money until fixed — see §7):
`requireProdEnv()` has zero runtime call sites (only `lib/env.test.ts`); `ADMIN_TOKEN` missing from `REQUIRED_PROD_ENV`;
`lib/manage.ts` is backend-only by design in v1 (listing edits not shipped — a listing is set at checkout and is final); `REFUNDED` enum never written;
`STRIPE_WEBHOOK_SECRET` has **no validator** in `lib/env.ts`, so a mistyped or placeholder value passes `check-prod-env` while the webhook silently stays broken — the only real check is a live delivery returning `200` (§3c). (`PROD_ENV_VALIDATORS` only shapes `NEXT_PUBLIC_APP_URL` and `CLICK_SALT`; every other required key is a non-empty test. This held for `WHOP_WEBHOOK_SECRET` before the migration and still holds after the rename.)

> ✅ Resolved 2026-09-11: the Whop signature contract is **no longer guesswork**. A genuine v1 delivery verified as Standard Webhooks, and signed fixtures for *both* envelopes now live in `lib/webhook.test.ts` (~11 cases). See §3c.

## 2. Data, domain, DNS, headers, read APIs

- [x] DB: `SELECT count(*) FROM "Element";` → **122** (1..118 + Hbar/-1, Ps/0, Uue/119, DM/999) — ✅ 2026-09-11: prod `/api/stats` reports `elementsTotal:122, claimed:0, unclaimed:122, stakeCount:0, totalStakedUsd:0` (the API reads the same table). A freshly seeded local DB reports the same 122.
- [x] DNS (GoDaddy, do NOT touch mail rows): `A @ → 216.198.79.1`, `CNAME www → periodictable.lol.` — ✅ 2026-09-11, **exact match on both**: `dig +short A periodictable.lol` → `216.198.79.1`; `dig +short CNAME www.periodictable.lol` → `periodictable.lol.` (the trailing dot is the DNS root — that is the correct apex form, not a typo). The `308` apex→www redirect in the row below corroborates it independently: a `308` to `https://www.…` is only issued if the apex really is bound to this project.
- [x] Apex `curl -sI https://periodictable.lol | head -3` → `308` → `https://www.periodictable.lol` — ✅ 2026-09-11
- [x] GoDaddy mail intact; Resend uses different hostnames, no conflict — ✅ 2026-09-11, verified from DNS itself rather than the dashboard. **MX present as described:** `0 smtp.secureserver.net.` and `10 mailstore1.secureserver.net.`, plus root `TXT` → `v=spf1 include:secureserver.net -all`.
  - **Correction to this item's wording: there is no `secureserver._domainkey` record.** Queried on two independent resolvers (`1.1.1.1`, `8.8.8.8`) it comes back `(none)` — so the item asked me to find a row that does not exist, and a literal reading would have failed the check on a correct zone. GoDaddy's mail authentication on this domain is expressed through **DMARC** instead: `_dmarc` → `v=DMARC1; p=quarantine; adkim=r; aspf=r; rua=mailto:dmarc_rua@onsecureserver.net;` — the `rua` points at secureserver, i.e. GoDaddy is running the reporting, which is the real evidence that the mail configuration is live and theirs.
  - **The "no conflict" claim is now proven, not assumed:** Resend lives entirely on its own hostnames — `resend._domainkey` holds a real `p=MIGf…` public key, and `send.periodictable.lol` carries its own `v=spf1 include:dc-fd741b8612._spfm.send.periodictable.lol ~all`. So the root `-all` hard-fail applies to GoDaddy's senders only and never touches Resend's return-path. Combined with `adkim=r; aspf=r` (relaxed alignment), Resend's DKIM on `d=periodictable.lol` satisfies `p=quarantine` DMARC while sending as `EMAIL_FROM=hi@periodictable.lol`. **Do not "fix" the root `-all`** — it is correct for GoDaddy and unrelated to Resend; loosening it would only widen GoDaddy's spoofing surface.
- [x] Security headers on `https://www.periodictable.lol` (prod only): `X-Content-Type-Options: nosniff`, `Referrer-Policy`, `X-Frame-Options: SAMEORIGIN`, `Permissions-Policy`, `Strict-Transport-Security`, `Content-Security-Policy` (see `next.config.mjs`) — ✅ 2026-09-11: all six present in the live prod response.
  - ✅ `img-src` must include `https://*.microlink.io` (the wildcard is required — Microlink hands back a sharded shot host, live vs cached). This was too narrow until 2026-09-10; found by §4b, fixed in PR #4, confirmed in the live prod response headers.
  - ℹ️ CSP ships **production-only** (dev needs webpack `eval()`), so a CSP symptom never reproduces under `npm run dev` — always test the deployed URL.
- [x] All 6 read APIs return 200 on custom domain, <2000ms each — ✅ 2026-09-11: all six `200`, slowest `0.47s` (well inside the 2000ms budget). NOTE: `/api/leaderboard` and `/api/live` do **not** exist — a `404` on those means the URL was guessed, not that the app is broken. As run:
```
for p in /api/stats /api/elements /api/table-order "/api/board?tab=crowns" "/api/activity?limit=6" "/api/search?q=carbon"; do
  echo "== $p"; curl -s -o /dev/null -w "%{http_code} %{time_total}s\n" "https://www.periodictable.lol$p"
done
```
- [x] `GET /api/stats` shape: `{elementsTotal:122, claimedElements, unclaimedElements (=total-claimed), stakeCount, totalStakedUsd}` — summed dollars, not row counts — ✅ 2026-09-11: exact shape observed on prod.
- [x] `GET /api/elements` tiles carry `{symbol, pool, count, leader{domain,logoUrl,amount}|null}` — ✅ 2026-09-11
- [x] `GET /api/elements/Li` detail: stakes ranked desc, `prices{takeLead,joinMin}`, hidden bidders excluded — ✅ 2026-09-11: returns `prices{takeLead:5,joinMin:5}`

## 3. Money / payments (paused now, live last)

### 3a. Paused mode (current prod expectation)
- [x] `POST /api/checkout` (no flags) → `403 {"waitlist":true}` — ✅ 2026-09-11 on prod
  - [x] …**and** the modal shows the Join-waitlist card — ✅ 2026-09-11 in a real browser on prod: tile → "Be the first — from $5" → modal body is the waitlist card, no checkout form rendered
- [x] `BASE_URL=https://www.periodictable.lol bash scripts/rehearse-release.sh paused` → 2/2 pass — ✅ 2026-09-11: `2 passed, 0 skipped` (re-run after the script's webhook-signing fix, so the edited script is itself confirmed)
- [x] `POST /api/waitlist {email}` twice → same `id` (dedupe by email), one DB row — ✅ 2026-09-11: rehearsal asserts "waitlist stores one row per email"

### 3b. Live flows (local sim: NO Stripe keys → dev simulator; WITH keys → real sessions)
```
BASE_URL=http://localhost:3100 bash scripts/rehearse-release.sh live
# full: BASE_URL=… ADMIN_TOKEN=… STRIPE_WEBHOOK_SECRET=… bash scripts/rehearse-release.sh live
```
- [x] First claim $8 → 200 `{paymentId, checkoutUrl}` → `POST /api/dev/pay {pay}` → `paid` → tile leader = payer — ✅ 2026-09-11
- [x] Contested $5 join → lands #2, leader untouched; tie at leader total → `409 TIE` — ✅ 2026-09-11
- [x] Take $9 → `guaranteedTake:true, reservation.reservedTotal:9`; rival take → `409 RESERVATION_CONFLICT`; pay → crown flips — ✅ 2026-09-11
- [x] Reclaim $2 by former leader → restores #1 at $10 (cumulative) — ✅ 2026-09-11
- [x] Same `idempotencyKey` + same payload → returns ORIGINAL `paymentId`; different payload → `409 IDEMPOTENCY_CONFLICT` — ✅ 2026-09-11
- [x] Expired reservation (server `RESERVATION_TTL_MS=2000`, sleep 3s) settles as ordinary stake, no crown — ✅ 2026-09-11
- [x] Provider outage (Whop keys set, API down) → `502`, NO `checkoutUrl` (never a dead URL) — ✅ **2026-09-12, observed live on production, not simulated.** This was the one gate the local rehearsal skips (it needs `WHOP_API_KEY` set, which flips the provider out of dev mode); it is now closed against the real provider instead: with live keys in place, `POST /api/checkout` returned **`502 {"error":"Payment provider unavailable. Try again."}`** with **no `checkoutUrl` in the body**, and the `Payment` row written inside the tx stayed **`pending` with an empty `providerCheckoutUrl`** — never a dead URL handed to a buyer, which is the whole point of the item. The provider-side cause is the `401` permission failure recorded in §6 (see the third launch blocker).

> ✅ **2026-09-11 — live rehearsal is green: 18 passed, 1 skipped** (the skip above). Evidence per gate is the script's own output; run as documented under §3b. Two things are required to reproduce it, and both are easy to get wrong:
>
> 1. **An isolated database.** Migrate + seed a throwaway DB (`ptrehearse` was used) and override `DATABASE_URL` in the shell — shell env beats `.env`, which points at Neon prod. Never rehearse against prod.
> 2. **A *non-production* app env, with no Stripe key at all** — see the 2026-09-14 re-run below. The Whop-era version of this step (`WHOP_WEBHOOK_SECRET` set, `WHOP_API_KEY` deliberately withheld) is **dead in Stripe mode** and reproduces the failure recorded there.
>
> Server env used: `DATABASE_URL=<throwaway>`, `PAYMENTS_LIVE=true`, `NEXT_PUBLIC_PAYMENTS_LIVE=true` (build-time), `RESERVATION_TTL_MS=2000`, `ADMIN_TOKEN=<local key>`, `STRIPE_WEBHOOK_SECRET=<self-generated>`, on port 3100. No production secret is needed for any of it: the script and the local server share a throwaway `ADMIN_TOKEN` value, so §3b costs nobody a keychain lookup.

> ✅ **2026-09-14 — re-run green on the current (Stripe-era) code: 18 passed, 1 skipped.** Against a **fresh throwaway DB** (`ptrehearse` recreated from scratch: 6 migrations, seed = 122 elements / 8 startups / 26 stakes) and a **production build** (`npm run build`; all 122 `/elements/[sym]` pages prerendered from the throwaway DB; then `next start -p 3100`). Same 18 gates as 09-11, the same single skip (provider outage, already closed against the real provider above). Post-run ledger in that DB: **8 `Payment` rows (7 `paid`), 32 stakes, 15 startups** — seed 26 + the 6 the script creates, so the money paths really did write, not just return 200. **`EmailLog` is empty and could not be otherwise:** with no `RESEND_API_KEY` set, mail is queued as an outbox event rather than delivered, and no local worker drains it — so this rehearsal sent **no email to anyone**, which is the property that makes it safe to run at all. Read-API latencies on that local run: `/api/stats` 74 ms, `/api/elements` 85 ms, `/api/table-order` 123 ms, `/api/board?tab=crowns` 116 ms, `/api/activity?limit=6` 207 ms, `/api/search?q=carbon` 105 ms (budget 2000 ms) — local numbers, so no substitute for §2's production timings.
>
> ⛔ **The 09-11 recipe had silently expired, and it fails at the very first checkout.** Re-run as written, `POST /api/checkout` answered **`403 {"error":"Payments are paused — join the waitlist.","waitlist":true}`**. Cause: the Stripe port reads the **same two variables** for both decisions the Whop code split across two — `providerConfigured()` (`lib/flags.ts:10-12`) and `stripeEnabled()` (`lib/stripe.ts:18-19`) both mean "*`STRIPE_SECRET_KEY` **and** `STRIPE_WEBHOOK_SECRET` present*". So the Whop trick (secret set, API key withheld → payments counted live while `getProviderMode()` stayed `"dev"`) is now unreachable: **any** config that makes `paymentsLiveServer()` true in production also flips the provider to `"stripe"`, and `"stripe"` `403`s `/api/dev/pay` (`app/api/dev/pay/route.ts:15`) — which is precisely the endpoint a charge-free money rehearsal depends on. The working recipe leaves **both** Stripe keys unset and moves the server to a **non-production app env**: `getAppEnv()` checks `VERCEL_ENV` **before** `NODE_ENV` (`lib/env.ts:23-28`) and `next start` always sets `NODE_ENV=production`, so `VERCEL_ENV=development` is the one lever that makes `isProduction()` false. Then `paymentsLiveServer()` falls through to its dev branch ("live unless explicitly `false`"), provider mode is `"dev"`, and `/api/dev/pay` answers. Two local-only details: `TURNSTILE_SECRET` must stay **unset** (`verifyTurnstile` returns `true` when it is absent, `lib/abuse.ts:10`) so the script's tokenless checkouts pass, and `STRIPE_WEBHOOK_SECRET` **is** set because the script signs with it — which makes `stripePartiallyConfigured()` log its development warning at checkout, harmless and exactly what the Whop note used to say. **Nothing in production moves:** prod sets `VERCEL_ENV=production` itself, so this lever exists only on a machine that sets it by hand.

### 3c. Webhooks (needs `STRIPE_WEBHOOK_SECRET`; sign like `rehearse-release.sh:sign_post`)
- [x] `payment.succeeded` with matching amount → `applied` once; replay same `id` → `duplicate`/`already-settled` — ✅ 2026-09-11
- [x] Statusless event → `ignored`, settled payment untouched — ✅ 2026-09-11
- [x] `payment.failed` → `failed`; later `succeeded` for same payment → `already-settled` (terminal) — ✅ 2026-09-11
- [x] Amount 999 vs local 7 → `amount-mismatch`, nothing applied — ✅ 2026-09-11
- [x] Missing/bad signature (either envelope) → rejected; unknown `paymentId` → recorded, nothing applied — ✅ 2026-09-11: unsigned → `401 {"error":"bad signature"}`; bogus Standard headers → `401` (proves the new header path executes and rejects cleanly rather than `500`ing); both envelopes accepted when genuinely signed

#### ✅ 2026-09-14 — the edge cases re-proven against **production**, after the Stripe port

The five rows above were established against a local rehearsal and a Whop-shaped payload. The Stripe port changed the envelope, the event names and the outcome vocabulary, so the same cases were re-run as real signed `POST`s to `https://www.periodictable.lol/api/webhooks/stripe` — the only money half reachable by a script in prod (see the `latest` note in §7). Fixtures were placed on **Xe** (empty), and every fixture row was deleted afterwards with the table counts re-verified back at baseline. Four of the five ran as one signed sweep; the signature case was proven separately (it is the one case that must write **nothing**, so it is safe to run at any time, including on a live ledger).

| Case | What was sent | Observed |
| --- | --- | --- |
| A — bad signature | (1) zeroed `v1` hex, (2) a **correctly signed** `t` 900 s in the past | both → **`401 {"error":"bad signature"}`**, and **no** `ProviderEvent` row written — `providerEvent` count was 11 before and 11 after, so the rejection happens before the ledger. Variant (2) is the one that matters: it proves the 300 s tolerance actually rejects rather than being declared and unimplemented |
| B — unrelated event | a validly-signed type the route does not settle | **`200`** → `IGNORED` / `unrelated-event` |
| C1 — expired session | `checkout.session.expired` | **`200`** → `FAILED` / `checkout.session.expired` |
| C2 — paid after failed | `checkout.session.completed` for the C1 payment | **`200`** → `DUPLICATE` / `already-failed`; payment stays `FAILED`, **no stake** |
| D — amount mismatch | `amount_total: 999` against a `$5` payment | **`200`** → `ERROR` / `amount-mismatch:999`; payment left `PENDING`, **no stake** |

Two things worth keeping from running it: the sweep's first pass reported three failures that were an **artifact of the probe, not the route** — it looked up `ProviderEvent` rows by the bare event id while the route keys them **`stripe:<event.id>`** (`lib/stripe.ts:350-355`, falling back to `stripe:sha:<sha256(rawBody)>` when the payload carries no id). The route had written all four rows correctly. Any future probe must key on `stripe:<id>`. And **`charge.refunded` remains the one money path with no live delivery behind it** — it is covered by `lib/stripe.test.ts` and by the port's metadata reasoning above, but no real refund has ever been posted to production.

#### ✅ RESOLVED 2026-09-11 — Whop sends **Standard Webhooks** (`api_version: "v1"`)

The open question was which signature envelope Whop delivers, because the two sign **different bytes** with the same secret: get it wrong and every delivery `401`s silently, the provider retries for days, then auto-disables the endpoint — while the buyer's money is gone. Implementing one and receiving the other cannot be detected by a test suite that signs with the scheme it verifies (it was green for months). **The answer is now known from a real delivery, not a guess:**

- **`api_version: "v1"` ⇒ Standard Webhooks.** The endpoint was registered as v1 and the first genuine delivery from Whop's dashboard returned `200` with `[whop-webhook] verified via standard signature`. Before commit `3f23d62` (deployed 2026-09-11, ~20 min prior) **every real delivery would have `401`ed.**
- **Dual-accept stays, deliberately.** Legacy (`x-whop-signature`, hex over the body alone) is still accepted, because the envelope is fixed when the webhook resource is created — a differently-configured or re-created endpoint would otherwise silently break again. Coverage: a `lib/webhook.test.ts` case plus a rehearsal assertion that re-delivers the same event legacy-signed, so v2/v5 does not regress unnoticed.
- **Instrument:** every delivery logs `[whop-webhook] verified via standard|legacy signature`. That one line answered the whole question — read it in Vercel logs on the first real payment.
- **`payment.succeeded` never 2xx on a non-paid setup:** Whop's dashboard **"Send test event"** fixture is a car-detailing demo (`inv_xxxxxxxxxxxxxx`, "Ceramic Coating Package", `marcus@shinetime.example`, `api_version:"v1"`), and it is sent **twice** (~3 s apart). It carries no `plan.metadata.paymentId`, so it always lands as `IGNORED / no-paymentId` and **can never move money**. Keep it as a negative test: `invoice.paid` *is* in `PAID_EVENT_TYPES` and the fixture says `status:"paid"`, yet the payment-id gate fails closed first — the correct failure mode.
- ✅ **Signature secret confirmed genuine** (the signature verified), and **payment-id location confirmed aligned**: our checkout writes `data.plan.metadata.paymentId` + `metadata.paymentId` (`lib/whop.ts:46,51`), which is exactly what `paymentIdFromWhopPayload` reads first (`lib/whop.ts:151`). Real payments will settle.
- ⏳ **Still unproven:** no *real* (non-test) Whop event has ever been received, so live settlement against actual Whop money is untested. That is what the `$1` claim in §7 exercises — watch for the `verified via standard signature` line and the `applied` outcome on that first one.
- ℹ️ **Unrelated but worth knowing:** `WHOP_API` in `lib/whop.ts:26` still points at the deprecated `https://api.whop.com/api/v2`. This is **independent** of the webhook envelope (the webhook's `api_version` describes the delivery format, not the checkout API), so nothing is broken — but it is a maintenance item to track.
  - **Not proven:** exact live event type names beyond `invoice.paid` (seen in the fixture), and whether failures arrive as `invoice.payment_failed` or another spelling. `PAID_EVENT_TYPES`/`PAID_STATUSES` in `lib/whop.ts:166-175` remain the best available guesses for anything not yet observed.
- ✅ 2026-09-11: signed fixtures for **both** envelopes now exist in `lib/webhook.test.ts` (~11 cases: standard-over-body-alone and legacy-over-signed-form both rejected), so the contract is locked in tests rather than prose.
- ℹ️ Register the webhook as **explicit events** (API) or **All** (dashboard) — a narrower selection can omit a reversal/chargeback type, and a missed reversal never unwinds the stake.

## 4. Email + background jobs

### 4a. Resend live test (manual, NOW — 09-09 key in chat is INVALID, never reuse)
- [x] Resend → API Keys → fresh `periodictable-prod` key (Sending access) exists (2026-09-10: exposed key deleted + recreated after chat exposure)
- [x] Resend → Domains → `periodictable.lol` = Verified (proven: direct-API mail delivered 2026-09-10)
- [x] Vercel → Env Vars → `RESEND_API_KEY=re_…` (fresh) → Save; ⚠️ `EMAIL_FROM` currently `info@`, handoff expects `hi@` — align to `hi@` + redeploy when convenient (both work, same verified domain)
- [x] Deployments → ⋯ → Redeploy (public vars bake at build) — done 2026-09-10
- [x] Direct Resend API test → inbox delivery ✅ (landed in Gmail spam — expected: bare "prod test" content from a fresh domain, no List-Unsubscribe; real receipt/outbid mails carry proper content + one-click headers and will fare better; reputation builds with volume)
- [x] Rotate after any chat exposure: delete + recreate + Vercel update + redeploy — done 2026-09-10
- [x] Full pipeline test (`POST /api/dev/pay` → `POST /api/jobs/outbox` → `EmailLog=sent`) — **done 2026-09-11** locally against Postgres `ptl_dev` (`ptl-local` :55440) with a dev server on :3100 and `DATABASE_URL` **overridden in the shell** (shell env beats `.env`, which points at Neon prod — never run this recipe without the override). NOTE: `/api/dev/pay` is 403 on prod by design (Whop keys set), so this runs locally, never against prod.
  - ⚠️ **Blocker found first:** `ptl_dev` was schema-drifted — migration `0005_refund_reversals` had never been applied, so `POST /api/checkout` returned a bare **500** (`P2022: The column 'Payment.refundedAt' does not exist in the current database`). Fixed with `prisma migrate deploy` (purely additive: one new enum value + one nullable column).
  - ✅ **The chain, as observed:** `POST /api/checkout` (element `Ps`, `path:"take"`, $6, `attest:true`) → `{paymentId, checkoutUrl:"/pay/…", provider:"dev", guaranteedTake:true}` → `POST /api/dev/pay` → `{ok:true,status:"paid",terminal:true,elementSymbol:"Ps"}` → ledger consistent: `acmecorp-test.io` rank 1 `isLeader=true`, prior holder `holo-ps.dev` ($5) demoted to rank 2, `Element.totalPoolUsd` $5→$11, `Payment.paidAt` set, 1 `ProviderEvent`.
  - 🔴 **Correction to this row's own assertion: `EmailLog=sent` is not reachable locally, and `sent` was the wrong thing to assert.** `lib/email.ts` short-circuits `deliver()` to `"logged"` when `RESEND_API_KEY` is unset — deliberately ("sends are logged as EmailLog{status:'logged'} so the pipeline is testable end-to-end locally"). Observed row: `to=pipeline-test@example.org, template=receipt, status=logged, elementSymbol=Ps, amountUsd=6, detail="You're #1 in Ps (Positronium) 🎉"`. The `sent` branch was then proven **separately** through a stubbed `fetch` calling `sendReceiptEmail` directly: exactly 1 POST to `api.resend.com/emails`, `Authorization` present, `List-Unsubscribe: <https://periodictable.lol/api/unsubscribe?token=…>` + `List-Unsubscribe-Post: List-Unsubscribe=One-Click`, `from` = `periodictable.lol <hi@periodictable.lol>` → `EmailLog.status=sent`. So **`logged` proves the pipeline, the stub proves the wire format; only a real key proves delivery** (see §4c / the Resend key rows).
  - ℹ️ **`POST /api/jobs/outbox` returned `claimed:0` — expected, not broken.** `lib/settle.ts` fires `void drainDue()` inline on every settled payment, so all three rows (`RECEIPT_EMAIL`, `STAKE_ANALYTICS`, `PREVIEW_GENERATE`) were already `completedAt`-stamped with `attempts=0` before the explicit call. Anyone replaying this recipe should read `claimed:0` as "the inline drain already won" and verify via `OutboxEvent.completedAt`, not the claim count.

### 4b. Outbox + screenshot workers
- [x] `POST /api/jobs/outbox` without secret in prod → `401`; with a valid bearer header → `{ok:true, claimed, completed, failed}` (2026-09-10: unauthenticated `401` confirmed on prod for both routes)
- [x] `POST /api/jobs/screenshot` same auth; `backfill:true` enqueues ≤50 preview-less VISIBLE startups (2026-09-10: auth confirmed; **backfill run now done and green** — see the delivery-proof row below)
- [x] Schedulers live: `.github/workflows/outbox-tick.yml` every 10 min (free — public repo, unlimited Actions minutes; runs only from `main`, so it activates when this branch merges) + `vercel.json` daily backstop (04:00/04:30) (2026-09-10: merged to `main` via PR #2; main deploy `success`; `GET /api/jobs/outbox` now `401` where it was `405` ⇒ GET aliases Vercel Cron needs are live) — ✅ the timer itself is now proven, see the next row (~7 % cadence)
- [x] `CRON_SECRET` set in Vercel **and** as a GitHub Actions repo secret with the same value → manually run the `outbox tick` workflow → `{"ok":true,…}` (not `401`) for both endpoints (2026-09-10: manual dispatch run `34467721174` green in 10s — `outbox {"ok":true,"claimed":0,"completed":0,"failed":0}`, `screenshot {"ok":true,"checked":0,"updated":0,"failed":0}` ⇒ secret matches across GitHub + Vercel; `claimed:0` = empty queue, expected)
- [x] **Prove the automatic tick actually fires** — **PROVEN 2026-09-11.** 6 `schedule` runs, all `success`, latest `2026-09-11T05:09:52Z`. The 05:09 log shows a *scheduled* run (not a manual dispatch) authenticating to prod and returning `{"ok":true,"claimed":0,"completed":0,"failed":0}` on `/api/jobs/outbox` and `{"ok":true,"checked":0,"updated":0,"failed":0}` on `/api/jobs/screenshot` ⇒ the timer really runs the job end to end, not just startup; `claimed:0` is the expected empty-queue steady state. ⚠️ **But the cadence is ~7 %**: 6 ticks in a 14 h 30 m window that should hold ~87, observed gaps 1 h 53 m – 4 h 38 m. GitHub's scheduler is best-effort and de-prioritised, so a `*/10` expression on a low-traffic repo never holds cadence. **This makes the external pinger a tightening, not a fix:** either accept the daily `vercel.json` backstop (04:00/04:30) as the floor, or add cron-job.org for 10-min retry latency. Not launch-blocking: receipts/outbid mail is sent **inline** at webhook time (the outbox is the *retry* path only) and previews render nowhere in v1, so a late tick delays a retry and never silences first-time mail. **Diagnostics wired to the same tick, 2026-09-11:** the workflow now also `GET`s `/api/jobs/config` and `/api/jobs/reconcile` with the same bearer header, as its last two steps, each guarded by `if: !cancelled()` so neither can suppress the other. Before this, **nothing** called either route — no workflow, no `vercel.json` entry, no script, only tests and docs — so the sole reader for a money contradiction was a URL nobody requested. A non-2xx fails the step, which is the entire alerting channel. **Expect the `config` step red until `TURNSTILE_SECRET` is set** (`required` finding ⇒ 503 is correct, not a broken workflow). This closes the diagnostics gap without cron-job.org, which is now a cadence-only tightening.
- [x] Confirm the Vercel dashboard Cron Jobs tab lists both daily jobs (proves `vercel.json` was ingested) — ✅ **2026-09-11, and this did not need the dashboard at all.** The project object itself carries the ingested schedule: `GET /v9/projects/{id}` → **`crons.enabledAt` = 2026-09-09 18:32:20Z, `disabledAt` = `null`**, and `crons.definitions` = exactly **`{"path":"/api/jobs/outbox","schedule":"0 4 * * *"}`** and **`{"path":"/api/jobs/screenshot","schedule":"30 4 * * *"}`** — a **two-for-two match** with the committed `vercel.json`, with nothing defaulted or dropped. Stronger than a dashboard glance: `crons.deploymentId` = `dpl_J4uiUdEKXuV7vmDHhuNaCtAQkPpE` is the **live production deployment** (its creation time lands the same minute as `crons.updatedAt` 19:26:42Z), so the schedule is bound to the deploy that is actually serving — not to a stale one. `disabledAt: null` rules out the failure mode this line is really guarding (crons silently disabled). Note the definitions' `host` is the **deployment URL**, not `www.periodictable.lol` — that is Vercel's normal behaviour and not a misconfiguration.
- [x] **Re-verified live 2026-09-14 — and the auth model is deliberately *not* uniform, which cost an hour of false alarm.** `GET /api/jobs/outbox` and `GET /api/jobs/screenshot` are literally `return POST(req)`, so a bare `GET` carrying only `?secret=` answers **`401`**: `jobAuth` (`lib/jobs.ts`) takes an `Authorization: Bearer <CRON_SECRET>` header **or** a JSON **body** `secret`, and nothing else. Only `GET /api/jobs/config` reads a query param (`req.nextUrl.searchParams.get("secret")`, `app/api/jobs/config/route.ts:35`) — so "the secret works" is per-route, not global. Measured this run: `outbox` (Bearer) → `{"ok":true,"claimed":1,"completed":1,"failed":0}`; `screenshot` → `{"ok":true,"checked":0,"updated":0,"failed":0}`; `config` → **`200`** carrying the single Upstash `degraded` finding and no `required` finding; `outbox` with the secret removed → `401` (still fail-closed). The drain did real work rather than answering a no-op: it completed `PREVIEW_GENERATE` `dedupeKey: preview-cmu15v4ey0000b4nc6vnvvvrn`, created `11:30:10.559Z`, and `PENDING` outbox rows are now **0**. ⚠️ **Two self-inflicted probe bugs, recorded because both masquerade as application defects:** (a) in PowerShell a URL written `".../api/jobs/$job?secret=$s"` parses `$job?secret` as **one variable name** (`?` is legal in names), so the URL silently collapses to `/api/jobs/` and Next answers its **`404`** page — indistinguishable at a glance from a deleted route; brace it as `"$($job)?secret=…"`. (b) `curl --data '{"secret":"…"}'` from PowerShell line-wraps the JSON and yields a **spurious `401`**; use `--data-binary "@file"` with a file written via `-NoNewline -Encoding ascii`. Both disappeared once the probe was fixed — the app was never wrong either time. **Cadence reminder so this is not re-litigated:** a queue row 28 minutes old is *not* overdue — the GitHub tick is best-effort (~7 % of nominal, above) and the Vercel backstop is daily 04:00/04:30, so multi-hour retry latency is the design, not a broken timer.
- [x] End-to-end delivery proof: enqueue one real row, let the tick drain it, confirm `completedAt` set (proves the robot does real work, not just auth) — **done 2026-09-10 by `-f backfill=true`; this single step found two real prod bugs, both now fixed and re-proven:**
  - 🔴 **What was broken:** the screenshot robot (`PREVIEW_GENERATE`) was failing **every** job in prod. `lib/screenshots.ts` probed `image.microlink.io` — a host with **no DNS record** (NXDOMAIN). Each job threw, backed off, and burned out at `attempts=5`.
  - 🔴 **Second defect in the same code:** the probe downloaded the whole ~1.87MB PNG against an 8s budget, so even a live host would sit near the timeout. And the CSP did not allow the host the API actually returns.
  - ✅ **Fix:** probe Microlink's **JSON** API (`api.microlink.io/?url=…&screenshot=true&meta=false`) and store the returned `iad.microlink.io/<hash>.png` CDN URL; validate before storing (`https:` **and** hostname ends `.microlink.io`) so a third-party response can never inject an arbitrary `<img src>`; `img-src` widened to `https://*.microlink.io`; `scripts/backfill-previews.ts` now shares the same helper. Payload per job: **~300 bytes instead of 1.87MB**.
  - ✅ **Fix, second half (why re-running alone would not have worked):** `POST /api/jobs/screenshot {backfill:true}` now **re-arms** matched rows (`attempts:0, nextAttemptAt:now, lastError:null, completedAt:null`). `claimDueOutbox` filters `attempts < 5` and claim pushes `nextAttemptAt` 5 min forward, so a burned-out row is otherwise permanently stuck even after the cause is fixed.
  - ✅ **Probe latency (measured live):** cold request **6.9s**, warm cached **172ms**; stored CDN URL serves `200 image/png` (1.56MB) in **0.33s**.
  - ✅ **Drain proof — 4 dispatches, queue visibly emptying:** `checked:9, updated:3, failed:0` → `checked:6, updated:4, failed:1` → `checked:2, updated:2, failed:0` → **`checked:0, updated:0, failed:0`**. The shrinking candidate set *is* the evidence the worker does real work: 9 rows enqueued, 9 completed, queue empty. The single transient failure retried cleanly through the new re-arm path.
  - ✅ **Read-path proof:** `GET /api/elements/C` returns `stakes[].preview` = the stored `iad.microlink.io/…png` URL; fetching that exact URL returns `200 image/png` ⇒ the stored value is a real servable image, and the live CSP now permits the browser to render it.
  - ℹ️ **Scope note:** previews are **not rendered anywhere in the v1 UI yet** (no `.tsx` reads `preview`; only `Avatar.tsx` renders `logoUrl`). So this was a backend-only defect — no user ever saw a broken image — but the pipeline is now correct and proven for when v1 adds the surface. Decide explicitly whether v1 shows previews or leaves them for v2.
- [x] Outbox row lifecycle: `attempts<5`, exponential backoff, `lastError` persisted, operator retry endpoint works with `ADMIN_TOKEN`
  - ✅ **10 new tests** in `lib/outbox.test.ts`: claim predicate + lease, `attempts` increments only on failure, backoff shape (`min(1 h, 30 s·2^attempts)`), `lastError` truncated to 500 chars, `completedAt` set exactly once, exhausted rows are never re-claimed, the drain's batch bound, and the operator retry reset path. All DB-backed against a real Postgres.
  - 🔴 **Defect found and fixed (production):** `drainDue()` read the queue with `findMany` and then processed each row by id — and `processOutboxRowById` never consults `nextAttemptAt`, so a row the cron worker had already leased and was mid-delivery could be processed a second time ⇒ **duplicate receipt/outbid email**. `lib/settle.ts` fires `void drainDue()` on every settled payment, so the race was live in prod. `drainDue` now claims through `claimDueOutbox(limit)` (one `UPDATE … FOR UPDATE SKIP LOCKED … RETURNING id`), the same atomic path the worker uses; the row *set* selected is unchanged, so it is a pure race fix. The file doc comment ("concurrent workers never share a row") is now true of every path. Audited the other queue consumers for the same shape: both `/api/jobs/outbox` and `/api/jobs/screenshot` already claim via `claimDueOutbox` before processing, so `drainDue` was the only unclaimed path.
  - ℹ️ **Test-hygiene fix:** five files (`settle`, `moderation`, `routes`, `webhook`, `ledger`) cleaned outbox rows with `dedupeKey: { contains: "…" }`, but every real key is id-based (`receipt-${paymentId}`, `outbid-${paymentId}`, `preview-${payer.id}`), so those filters matched **0 rows** and every run leaked its rows (1848 orphaned rows had piled up, all completed or burned). They now call `purgeSettledOutbox()`, which derives keys from live `Payment` rows — so it must run **before** a test deletes its payments. `vitest.config.ts` also gained `fileParallelism: false`: all DB-backed files share one database and the outbox is a single global queue ordered by `nextAttemptAt`, so parallel files stole each other's fixtures.
  - ⚠️ **No regression test for the atomicity itself, deliberately:** reverting `drainDue` to the unlocked read still passes 10/10, because a serialized single-process test selects the same row *set* either way — only real concurrency differs. Verified instead by reasoning about `claimDueOutbox`'s single `UPDATE … SKIP LOCKED`. Do not add a "proves the lease" assertion to a non-concurrent test; it would be cargo-cult coverage.

### 4c. Receipt / outbid / unsubscribe (hand test with two emails)
- [x] Claim → payer gets receipt with correct `{element, amount, rank, domain, viewUrl, unsubUrl}`; `EmailLog{template:receipt,status:sent}` — ✅ **2026-09-14, against production.** A webhook-settled `$5` on **Xe** wrote `Payment` `PAID` at `11:30:09.918Z`, a leading stake, and `EmailLog{template:"receipt", status:"sent"}` at **`11:30:11.752Z`** — ~2 s after settle, where the pre-fix code parked it behind a ~5-minute lease. Two independent witnesses: the `EmailLog` row *and* the settle webhook answering `200 {"ok":true,"outcome":"applied"}` in 7282 ms. This is the `sent` half only, and only for a **human-opened** delivery: **and the human did look, 2026-09-14: the receipt was found in Gmail, in the inbox (not Spam), rendering normally.** That closes the row — `sent` at Resend, confirmed at the inbox by a person. **Code-level upgrade of that `sent` witness, 2026-09-14:** `deliver()` (`lib/email.ts:34`) returns `"sent"` **only** when `api.resend.com` answers `res.ok`, and `logEmail()` writes its row *after* that return (`lib/email.ts:21`, called at the end of both senders) — so `status:"sent"` means Resend **accepted** the message, not merely that the app tried. That rules out the two states a broken setup actually produces (`"logged"` when no key is set, `"error"` on a rejection or timeout), and because the default sender is `hi@periodictable.lol` rather than `resend.dev`, it also proves the sending domain is verified in Resend. It is still not proof of inbox placement — spam, DMARC alignment and rendering are exactly what the one human look in Gmail is for.
- [x] Outbid → victim gets outbid mail with correct `reclaim = winner+1-victim (min 1)` and `/?el=SYM&stake=N&r=DOMAIN` prefill link — `r` carries the victim's own domain so the homepage fills their listing back in (URL, name, pitch) and quotes the reclaim price before they type anything; no email address in URLs (P1-18)
  - ✅ **The prefill link works, confirmed by the human, 2026-09-14.** The outbid mail's reclaim URL was opened in a browser and it landed on **Stripe's hosted checkout with the listing already filled in** — which is the whole point of the `r=` parameter (review item: the earlier behaviour asked the victim to retype everything, which the human explicitly rejected). Arithmetic cross-checked against the production ledger: winner `$9`, victim `$8` → `max(1, 9+1-8)` = **`$2`**, matching `lib/email.ts:83` and the URL actually emitted.
  - ✅ **Closed by the human, 2026-09-14 — the same gap the receipt row had:** the outbid mail was found **in Gmail, in the inbox (not Spam)**, reading *You were knocked off C 👑* and quoting *Reclaim it for $4.00*. Subject, `reclaim` figure and button target are therefore confirmed from the delivered message itself, not only from the URL being openable. Both §4c mail rows now rest on a person having read the mail.
- [x] `List-Unsubscribe` + `List-Unsubscribe-Post` headers present; `GET /api/unsubscribe?token=` renders a confirm form and **never mutates** (a link scanner prefetching the URL must not unsubscribe anyone) — the `POST` behind it clears the email and redirects to `/?unsub=done`. An unknown or missing token also lands on `?unsub=done`: the route deliberately returns the same shape so the endpoint is not an oracle for which tokens exist, which means the client's `?unsub=unknown` branch (`app/page.tsx:118`) is **unreachable from the real flow** (verified: no server producer exists). **Verified end to end** — headers in code (`lib/email.ts:50-51`, that they survive delivery rides along with the real send in the two lines above), and every branch of the contract live + in `lib/unsubscribe.test.ts`. The one-click path was **broken** and is now fixed; see J7 below.
- [x] **v1 product decision: a listing is set at checkout and is final** (settled 2026-09-10, after finding the manage flow had no UI). `findOrCreateCheckoutStartup` writes title/pitch/url/link/email from the checkout form; a later stake on the same domain only adds stake and never mutates the profile. So there is nothing for an owner to "manage" in v1, and no UI is missing by accident. Consequences, all verified in code:
  - [x] Listing edits are **not shipped in v1** — the magic-link backend (`lib/manage.ts`, `/api/manage/*`, `PATCH /api/startups/[domain]`) stays implemented, tested and **dormant/unreachable**. Production intentionally does not email the link (`console.warn` + TODO(v2)); non-prod still returns `debugToken` for dev convenience.
  - [x] `POST /api/manage/request` is non-committal: always `200`, same shape, rate-limited, message no longer promises a link ("Listing management is not enabled").
  - [x] No user-facing surface promises editing: grepped every `.tsx` — no manage/edit UI text exists. The receipt email's "Manage your spot →" button (which pointed at the read-only `/s/<domain>`) is now **"View your spot →"** (`emails/receipt.tsx`, prop `manageUrl`→`viewUrl`).
  - [x] Docs corrected so nobody re-adds the promise: `README.md` "Ownership", `doc/ARCHITECTURE.md` §3, `HANDOFF.md` item 5 (now deferred to v2).
  - ⚠️ v2 scope when we do ship it: 3 pages (request link, land/verify, edit form) + turn the prod email send on + make sure the receipt button points at the edit flow. Not a launch blocker.

## 5. Trust, abuse, admin, frontend honesty

- [x] Turnstile: `TURNSTILE_SECRET` + `NEXT_PUBLIC_TURNSTILE_SITEKEY` from Cloudflare set; without them bot checks silently pass (`lib/abuse.ts`) — must not launch without
  - [x] both vars present in Vercel production, 2026-09-11
  - [x] **values verified byte-for-byte, 2026-09-11** — `vercel env pull --environment=production` returns both in the clear (neither is marked Sensitive), and they match the Cloudflare pair exactly: sitekey 24 chars, secret 35 chars, both `0x4AAAAAA…` shaped, `==` comparison true for each. This closes the "present but could be a placeholder" gap: an empty or stale `TURNSTILE_SECRET` is the *only* difference between a working bot check and one that returns `true` for everything.
  - [x] …**and** the widget actually renders on a live checkout — ✅ **closed 2026-09-14 by the live run, not by inspection.** The human's own purchase attempt on `https://www.periodictable.lol` (element tile → `$5` CTA → solve → submit) reached Stripe's hosted payment page, which is only reachable through a Turnstile challenge that both rendered *and* was accepted: the checkout POST is server-gated by `verifyTurnstile` (`lib/abuse.ts`), and a missing/blank token is exactly the `400 {"error":"Bot check failed."}` of blocker 1, so no session can exist without a widget-produced token. It also proves the build-time-baked `NEXT_PUBLIC_TURNSTILE_SITEKEY` is the right one — a stale or placeholder sitekey renders a widget that never issues a token, and the checkout would have been refused the same way. (The build-time note still stands for future edits: `NEXT_PUBLIC_TURNSTILE_SITEKEY` is **baked at build time**, so correcting it later needs a redeploy, not just an env edit.)
  - ⚠️ **`TURNSTILE_SECRET` is not marked Sensitive in Vercel** — anyone with project read access can pull it in plaintext with one CLI call. Mark it Sensitive (and see the `CLICK_SALT` note below for why that one matters more).
  - ⚠️ `CLICK_SALT` is **also not Sensitive, and it is the one that actually matters.** It is correct and strong — 64 chars, non-empty, not a dev value (`lib/env.ts:86` rejects the `DEV_SALT_VALUES` list, so `PROD_ENV_VALIDATORS` would fail the build otherwise) — but `hashIp()` is `sha256(ip + ":" + CLICK_SALT)` (`lib/clicks.ts:14-16`), and IPv4 is only 2³² addresses. **With the salt in hand, the click attribution is reversible by enumeration**, which is precisely the privacy property line 174 asks this variable to provide ("attributed via sha256… never raw IP"). Task Order doc and §7's GO/NO-GO both lean on that anonymization. Marking the variable Sensitive is a one-click fix and restores defense-in-depth for anyone who can read the project's env.
- [x] Upstash: `UPSTASH_REDIS_REST_URL` + `UPSTASH_REDIS_REST_TOKEN` — ✅ **SETTLED 2026-09-14: the pair is genuinely unwired** (the `degraded` finding is present — verbatim response below), accepted as **non-blocking** because the store fails open. ⚠️ The state before it was settled: the names are present in Vercel production (both `Encrypted`), but production logs *contradict* that they are usable. `lib/rateStore.ts:76` warns `rate-limit: no shared store configured (UPSTASH_REDIS_REST_URL/TOKEN) — using instance-local memory` whenever either var is falsy, and that exact line appears in production logs at epoch `1789211890270` (2026-09-12 11:18:10Z) — **after** the variables were added; `lib/env.ts:121` only reports them missing when falsy, so the runtime believes at least one of them is an empty string. The counter-argument is that this warning is `console.warn`-gated **once per instance** (`warnedNoUpstash`), so a single stale instance from an older deployment makes vanishing incidences meaningless evidence too — absence proves nothing.
  - **Why it stayed open for a day rather than being decided:** the values cannot be read back at all. `vercel env pull` writes an **empty string for every Sensitive var**, including ones that provably work (`DATABASE_URL` pulls as `len=0` and the site's DB queries succeed), so a pulled length is not evidence of anything, and the raw `vca_…` CLI token `403 invalidToken`s against `api.vercel.com` because it expires hourly. The report that *would* settle it (`GET /api/jobs/config` → `getProdConfigReport()`) is bearer-gated by `jobAuth`, so it needs `CRON_SECRET` — it is not a free oracle.
  - **The settling probe (one command, any machine):** `curl -s "https://www.periodictable.lol/api/jobs/config?secret=$CRON_SECRET"` — a `degraded` finding naming `UPSTASH_REDIS_REST_URL + UPSTASH_REDIS_REST_TOKEN` means the store is not wired; no such finding means the vars are usable and the log line was a stale instance. Either answer is decisive, unlike reading logs.
  - ✅ **Run 2026-09-14, before the keys landed, against production, from the release machine — the `degraded` finding IS present, so the store is not wired.** `GET https://www.periodictable.lol/api/jobs/config?secret=…` → **`503`** with `{"ok":false,"env":"production","findings":[…]}` containing, verbatim: `{"key":"UPSTASH_REDIS_REST_URL + UPSTASH_REDIS_REST_TOKEN","severity":"degraded","detail":"Upstash is unconfigured: rate limits fall back to per-instance memory and fail open (lib/rateStore.ts)"}`. Two things this settles that reading logs could not: (a) the "stale instance from an older deployment" counter-argument above is **refuted** — `getProdConfigReport()` reads `process.env` inside the *serving* instance and still sees the pair as unset, matching the `lib/rateStore.ts:76` warning rather than merely being consistent with it; (b) the log line at epoch `1789211890270` was the real behaviour, not residue. The same response named `STRIPE_SECRET_KEY` and `STRIPE_WEBHOOK_SECRET` as `required` (see §7), which is why the status is `503` and not `200` — `503` is the only channel an external pinger sees, and it is doing its job. `jobAuth` answered `401` to the same URL with the secret removed, so the endpoint leaks nothing to an anonymous caller. **Status has since flipped to `200`** — re-run after the `STRIPE_*` keys landed later the same day (§7) it returns `200` with the Upstash `degraded` finding unchanged and no `required` finding at all; the `503` above is kept as the record of the pre-keys state.
  - **Impact, now that it is confirmed unusable: not a launch blocker, and not a security hole.** `rateLimitAsync` **fails open** by design (`lib/rateStore.ts:89`), so live checkouts keep working; the only loss is that the `checkout:` limiter falls back to per-instance memory, i.e. the effective limit becomes `5 × (number of warm instances)` per IP. Turnstile + the honeypot + the `paymentsLiveServer()` gate remain in front of it, and the cost of abuse is a `$5` unpaid `pending` row, so this degrades under load rather than breaking.
- [x] `ADMIN_TOKEN` long random set; all `/api/admin/*` → `403` without it; report → triage → HIDE → verify → restore round-trip (see hand journey J6)
  - [x] **Verified against production, 2026-09-11.** `ADMIN_TOKEN` is set and is 64 chars. The check is `adminAuth()` (`lib/jobs.ts:34`), which requires **`Authorization: Bearer <ADMIN_TOKEN>`** — *not* `x-admin-token`. That distinction cost a false alarm: the wrong header returns the same 403 as a bad token, so a perfectly good token looks revoked. All five route/method pairs answer **403** with no header *and* with a wrong bearer — `POST /api/admin/outbox/retry`, `GET /api/admin/reports`, `PATCH /api/admin/reports/[id]`, `POST` and `GET /api/admin/startups/[domain]/moderate`.
  - [x] …**and the positive control**, because "403 for everything" is also what a wall looks like. The real bearer passes auth and reaches route logic: `GET /api/admin/reports` → **200** with `[]` (the prod queue is empty), `PATCH /api/admin/reports/abc` → **404**, `GET /api/admin/startups/example.com/moderate` → **404**, the `POST` variants → **400** on body validation. Going **403 → 400/404/200** is what proves the gate is a gate rather than a blanket block. Auth is timing-safe and fails closed *even in development* (`lib/jobs.ts:31-33`), so a leaked dev DB is never one missing header from mutation.
  - [x] report → triage → HIDE → verify → restore round-trip: **done** — the full journey ran on a local seeded DB (`pt_j6`); see J6. Prod holds 0 stakes, so it is unreachable there.
- [x] Kill switch: `PAYMENTS_LIVE=false` + `NEXT_PUBLIC_PAYMENTS_LIVE=false` → checkout flips to waitlist in <2min via env-only redeploy (see `ops/rollback.md`) — ✅ 2026-09-11, verified **against live production**, not staging. `POST /api/checkout` → `403 {error:"Payments are paused — join the waitlist.", waitlist:true}` in **0.32s**; the guard is the **first statement** of the handler (`app/api/checkout/route.ts:102`), above rate limiting, honeypot, Turnstile and every provider call, so a probe costs nothing and cannot reach the DB. The client half is stronger than "flips": while paused the checkout form is **compiled out of the bundle**, not hidden by a runtime branch — 0 occurrences of `Continue to checkout`, `cf-turnstile`, `No refunds` and `I agree` across the HTML + all 8 JS chunks (690KB scanned), with `Join waitlist` / `Payments are paused` present. So it cannot be re-revealed by tampering with client state. ⚠️ **Two clauses of this item were misleading and are corrected here.**
  - **"<2min" is measured, but tight.** An env change only takes effect in a *new* deployment, so the old one keeps serving the old flag while it rebuilds. 12/12 production builds on 2026-09-11 ran **1.1–1.6 min (median 1.3)** — under 2 min, but with well under a minute of headroom for the human step. **This is why the switch is not the emergency lever:** if payments must stop *now* (runaway price, exploit), redeploying is the wrong tool — disabling the plan/product in the Whop dashboard is provider-side, immediate, and stops the charge rather than the button. Recorded in `ops/rollback.md` under the kill-switch heading.
  - **Fail-closed by *absence*, not by the string `false`.** Production has **no `PAYMENTS_LIVE` var at all** (`NEXT_PUBLIC_PAYMENTS_LIVE` is present but empty), so what holds the line is `flags.ts:17` `process.env.PAYMENTS_LIVE === "true"` → `undefined === "true"`. Worth knowing because the two directions are different code paths: `"false"` short-circuits at `:15`, absence falls through to the strict comparison. Both yield false, and neither is reachable by a truthy-but-unintended value.
  - **The webhook is deliberately outside the switch** — and that is correct, not a gap. There are exactly two ways money or a stake can enter, and `paymentsLiveServer()` is called in only one: `/api/checkout` (gated) and `/api/webhooks/<provider>` (**ungated**). A payment already taken must still settle, or we keep the money and deliver nothing. Forging a delivery requires the webhook secret: `verifyStripeSignature` returns `false` when `STRIPE_WEBHOOK_SECRET` is missing or no `v1` candidate matches, and the route hard-`401`s on `false` — fail-closed, timing-safe (`digestMatches`), with a 300s freshness window checked *before* the HMAC so an observed delivery cannot be replayed later (`lib/stripe.ts:152,168-199`). **Boundary in one line: the switch stops new checkouts; an in-flight payment still completes.** ⚠️ **Provider names updated 2026-09-14:** the Whop route no longer exists (`app/api/webhooks/whop/route.ts` was deleted in `dd2c8c3`), so the ungated endpoint is now `/api/webhooks/stripe` — measured live, below.
  - **The remaining rehearsal is blocked, not skipped.** `ops/rollback.md` asks for off → assert → back on → assert. The *off* half is now proven on production itself; the *on* half needs live Whop keys, so it is the go-live ladder (Deploy order steps 2-3), and it is the moment to re-check `WHOP_WEBHOOK_SECRET` — that var is in `REQUIRED_PROD_ENV` but has **no validator in `PROD_ENV_VALIDATORS`**, so literal garbage passes validation and then fails every delivery as a `401`. (Also: `ops/rollback.md` says "on staging", but there is no separate staging environment and previews share production's Neon DB.)
- [x] Grid/stats failure honesty: block API (offline/devtools) → error panels, NEVER fake all-`Unclaimed $5`/zeros. Reference pattern: `TerritoryView.tsx` error panel vs `app/page.tsx` — ✅ **already fixed and browser-verified 2026-09-11; HANDOFF records it under the identical heading, so this box was simply never ticked.** The bug was wider than the line suggests: with `/api/stats` down the page asserted *"0 elements live · $0 in bids · 122 unclaimed"* as fact, and the `?? 122` was the sneaky part because it is **plausible** — nothing looked broken. With `/api/elements` down, `tiles ?? []` emptied the `claims` map so every tile fell back to `claim?.price ?? 5`, i.e. **the whole table advertised as free**. Fixed by teaching the page to distinguish *unknown* from *zero*: `lib/liveState.ts` maps `(hasData, error)` → `loading | ok | stale | unavailable`; `StatsCard` renders `—` until real numbers exist (the `= 0` / `= 122` defaults are gone); `LiveDataNotice` blocks the grid with a Retry panel when tiles are unavailable and shows a "last known" marker when SWR serves last-good data after a failed refresh. 10 tests in `lib/liveState.test.ts` pin the truth table and three source-level anti-regression checks (`?? 0`, `?? 122`, `= 0`). **Verified in a real browser, which mattered** — the first placement sat at the same z-layer as `FooterBar.tsx`, so the legal links painted over the Retry button and ate its clicks; only geometry catches that and this repo has no jsdom. Method to redo: `next dev` on the **local** DB + `agent-browser network route "**/api/elements*" --abort` (and `/api/stats`); abort on first load ⇒ `unavailable`, abort *after* a healthy load and wait out the 30s refresh ⇒ `stale`. ⚠️ Note this only ever bit on **first load or hard failure** — SWR retains last-good data on a refresh error, which is exactly why `stale` is a distinct state from `unavailable`.
- [x] **HIDDEN leaks** — ✅ 2026-09-11, all four surfaces verified against source + the DB-backed suite. ⚠️ **One clause of this item was wrong and is corrected below — hidden money is deliberately *kept* in the aggregates. Do not "fix" that.**
  - **Activity feed excludes HIDDEN** — `app/api/activity/route.ts:24-29` resolves the HIDDEN domains, then filters `domain: { notIn: hiddenDomains }` **inside the query** so the requested page size stays exact (filtering after `take` would shrink the page). The feed publishes domain + city + amount, which is precisely why it cannot carry a concealed listing. UNLISTED is included **on purpose**: it is already on tiles and element detail, so omitting it would conceal nothing.
  - **Discovery filters VISIBLE; the tile *face* never shows HIDDEN** — `lib/moderation.ts` defines `DISCOVERABLE_STATES = ["VISIBLE"]` and `DIRECT_STATES = ["VISIBLE","UNLISTED"]`. Boards (`app/api/board/route.ts:19,40,68`), table order (`app/api/table-order/route.ts:14`) and search (`app/api/search/route.ts:28`) all filter VISIBLE. `/api/elements` filters its **leader** to `DIRECT_STATES` **and** `amountUsd > 0`, so neither a concealed nor a fully-reversed stake can own a tile face.
  - **⚠️ CORRECTED — "stats must not sum hidden money" is the OPPOSITE of the contract.** `lib/moderation.ts` says it outright: *"Financial history (stakes, payments, aggregates) is NEVER deleted — hidden money stays in pool/count totals."* The rule is **conceal the listing, keep the ledger** — hiding must not be a way to erase money, and HIDDEN is reversible. The test asserting it is named **"hide removes the listing everywhere but keeps the money"** (`lib/moderation.test.ts:96`). Two things a future reader must not undo: (a) `pool`/`count` on `/api/elements` stay hidden-**inclusive**, while `count` is deliberately **never read by the UI** — a badge driven by it would reveal that a concealed stake exists (`app/page.tsx:30-31` declares both; nothing renders either); (b) `app/elements/[sym]/page.tsx:45-51` **counts HIDDEN stakes on purpose**, so the page can explain the row-count/pool gap instead of leaving it as an inference channel.
  - **Hidden profile 404s** — `app/s/[domain]/page.tsx:48` `return notFound()`. UNLISTED stays link-reachable, by design.
  - **`/go/<stakeId>` refuses hidden — and does it *before* counting** — `app/go/[stakeId]/route.ts:20-22` 404s **above** the `shouldCountClick` block at `:27`, so a concealed listing gets neither a redirect nor a click attribution. (It also 404s a deleted stake or one with no `startup.url`.)
  - **Surfaces checked beyond the filter** (the item names four, but a leak could hide in one that has *no* `moderationState` call at all — so every public read surface was enumerated, not just the ones matching the grep): `app/sitemap.ts` **excludes `/s/[domain]` profiles on purpose** ("unbounded, user-generated, frequently churned") and does no DB query, so a hidden listing cannot be enumerated through the sitemap; `app/robots.ts` allows `/s/` but a hidden profile answers **404**, so a crawler drops it; `app/api/startups/[domain]/route.ts` is a **PATCH-only, owner-gated** profile update (401 without a management session for that exact domain) and reads nothing public; `app/api/emails/preview/route.ts` returns **404 whenever `NODE_ENV === "production"`**. Minor hygiene, not a leak: `robots.ts` disallows `/pay/`, `/api/dev/`, `/api/jobs/`, `/api/manage/` but **not `/api/admin/`** — those are token-gated (`403`) so a crawl yields nothing, it just adds log noise if you want it tidier.
  - **Why this is settled without touching prod:** `lib/moderation.test.ts` is DB-backed and asserts the feed holds no HIDDEN startup as a **window-independent invariant** (whatever rows come back, none may be HIDDEN — lines 130-138), and it passed in the 320-test gate on 2026-09-11. Prod has **0 stakes**, so it cannot demonstrate this at all; the test is the stronger evidence, and it also pins the pricing consequence: with a hidden $40 stake present, the advertised take-lead price must derive from the **visible** $12 leader, or the buyer's money buys a mere join instead of the lead the page advertised.
  - [x] hide clears `previewImgUrl` — **and nothing can put it back**. Fixed 2026-09-10. The moderate route always cleared it, but the *worker's* write was unconditional and a Microlink probe takes seconds: a `PREVIEW_GENERATE` job that started before a takedown landed **after** it and silently re-attached a preview to a HIDDEN listing, undoing the retention clear. Now `lib/outbox.ts` writes through `attachPreview()`, an `updateMany({ where: { id, moderationState: "VISIBLE" } })` — moderated rows update 0 rows and the outbox row completes without retry (no retry storm, no resurrected preview); `scripts/backfill-previews.ts` has the same guard plus a `moderationState: "VISIBLE"` target filter. Locked in by `lib/moderation.test.ts` → "preview writes respect moderation" (HIDDEN and UNLISTED both refuse, VISIBLE accepts). **How it surfaced:** this is the exact assertion that turned `main`'s CI red the first time previews started actually working (see §4b) — the DB-dependent test had always passed *because* the probe failed, so the leak was invisible until the pipeline was fixed.
- [x] Boards: Table Order sorts totalSpent desc → crowns → domain; By-Element biggest leader stake; deterministic tie-breaks; Early-Adopter tile correct per `FirstClaim` — ✅ 2026-09-11. All four surfaces were read before anything was touched: `app/api/table-order/route.ts` (**all** stakes, not leaders — correct, since `totalSpent` is cumulative spend and the crowns tie-break merely *counts* `isLeader` rows), `app/api/board/route.ts` (three distinct metrics), `lib/boards.ts` (the pure comparators), `components/Modals.tsx:455-481` (the rendering). The module header's claim — *"All orders are total (deterministic tie-breaks)"* — **holds**: every comparator ends in a unique key (`domain` or `id`), so no two rows can compare equal. Two things were hunted for and did **not** exist:
  - **`by-element` + `take: 20` cannot truncate a board.** Not an assumption: `lib/ledger.test.ts` proves `assertLedgerInvariants` throws `/1 leader/`, so **at most one leader exists per element** — 20 rows are 20 *distinct* elements, and the `take` is a deliberate top-20 rather than a silent cut. The DB `orderBy [{amountUsd desc}, {createdAt asc}, {id asc}]` mirrors `rankByElement`'s primary keys, so the 20 fetched rows are the same top set the full in-memory sort would yield. (Rows tied on *both* amount and time could in principle order differently between Postgres' `id` collation and `localeCompare` — but those rows tie on every displayed metric and `localeCompare` is itself deterministic, so neither the board's content nor its stability is affected.)
  - **`aggregateTableOrder` needs no moderation filter of its own.** It feeds the cumulative **Table Order** rail, not a tile face, and its stakes are already VISIBLE-filtered upstream (`app/api/table-order/route.ts:14`) — see the **HIDDEN leaks** item above for why hidden money stays in aggregates by design.
  - **🔴 FIXED WHILE CHECKING THIS: the Early-Adopter tile could name an element its own date contradicted.** The `early` branch ran an **unordered** `findMany` and kept whichever `FirstClaim` row Postgres happened to return first for a domain, while `firstClaimedAt` was computed as the **minimum** over that domain's claims. So `elementSym`/`elementName` could belong to a claim that was *not* the one the date named — and `components/Modals.tsx:478` renders exactly that pair as **`"first on {elementSym} {elementName}"`**, so the tile asserted a "first" its own date refuted. Two defects in one: **order-dependent** (the named element could change between renders) and **factually wrong**. Reproduced by replaying the old loop on the regression fixture — it emits `first on C Carbon` with a first-claim date of **2024-01-01**, when the startup's earliest claim was **Au**. Now extracted into `aggregateEarlyAdopters(claims)` in `lib/boards.ts` — the only boards logic that had *not* already been pulled into the pure, testable module — which attributes both element and date to the **earliest** claim (identical timestamps tie-break on element symbol) and deliberately consumes **unordered** input so the caller's query order never becomes a contract. `rankEarlyAdopters` was always correct; the defect was entirely in the route's aggregation.
    - **Locked by four tests** (`lib/contracts.test.ts`): medals count + earliest-claim attribution; **the element follows the earliest claim when rows arrive newest-first** (the regression itself, proved to have teeth by the replay above); the `aggregateEarlyAdopters ∘ rankEarlyAdopters` composition is invariant under input order; identical timestamps tie-break by element symbol. Gate **323 → 327 tests**.
    - Also removed a lying field: `SpendRow.elementName` was **dead** (`aggregateTableOrder` only ever reads `elementSymbol`) yet `app/api/table-order/route.ts:31` was passing `s.element.symbol` into it — a *symbol* in a field named `name`. Deleting it made `tsc` flag all five stale literals in the tests, which is the type doing its job rather than a chore.
- [x] Clicks: `/go/<stakeId>` attributes via sha256(ip+`CLICK_SALT`) only (never raw IP); `CLICK_SALT` private random in prod — ✅ 2026-09-11. `hashIp()` (`lib/clicks.ts:14-16`) is `sha256(ip + ":" + salt)`, and on the click path the **only** write is `app/go/[stakeId]/route.ts:28` `hashIp(ip)` — the raw header value never reaches the database. The salt default is `"ptl-dev-salt"`, and prod cannot silently fall back to it because `PROD_ENV_VALIDATORS.CLICK_SALT` (`lib/env.ts:86`) rejects `""`, `ptl-dev-salt`, `change-me-in-prod`, and `change-me` outright.
  - **Honest caveat — "never raw IP" is true of *persistence*, not of *process*.** `shouldCountClick` (`lib/clicks.ts:19-23`) keys its two limits on the raw value (`s:${stakeId}:${ip}`, `ip:${ip}`), so the raw IP does transit to Upstash as a rate-limit key with a ≤1h TTL. That is deliberate (the hash would work equally well as a key, but the limit must survive a salt rotation), and it is transient state rather than a stored record — worth knowing before repeating the claim as an absolute.
  - **🔴 FIXED WHILE CHECKING THIS: `app/api/waitlist/route.ts:43` stored a raw IP next to an email.** `audit({ action: "WAITLIST_JOINED", detail: email, actorRef: ip })` wrote the raw value into `AuditLog.actorRef` — a plain `String?` column (`prisma/schema.prisma:301`) — so the waitlist was the one place in the repo where a raw IP was **persisted**, and it was persisted *joined to a PII email in the same row*. Clicks hashed, reports hashed (`app/api/report/route.ts:36`), waitlist did not. Now `actorRef: hashIp(ip)`, which keeps the correlation value (same IP → same hash, so one-IP-many-emails is still detectable) while dropping the raw address. No test asserted the raw value, so nothing caught it and nothing broke on the fix; `npx tsc --noEmit` and the gate both pass. **A regression test now exists** — `lib/phase5.test.ts` → *"IP privacy — the address is never persisted raw"* hashes the invariant down three ways: a source scan that `app/api/waitlist/route.ts` contains `hashIp(ip)` and **not** a bare `actorRef: ip`, the same scan for the click route, and a behavioural check that `hashIp()` is a 64-char digest, deterministic for the same address, distinct across addresses, and distinct across salts. Gate is now **323 tests** (was 320). **Not retroactive:** rows written before this keep their raw IPs; there were 0 waitlist rows in prod at the time, so in practice there is nothing to scrub.
  - **`CLICK_SALT` is now marked Sensitive in Vercel** (it had been `encrypted`, i.e. readable in the clear via `vercel env pull`), as is `TURNSTILE_SECRET` — see the finding in HANDOFF. Every production variable is now `sensitive` except `NEXT_PUBLIC_APP_URL` and `NEXT_PUBLIC_TURNSTILE_SITEKEY`, which are public by design (the Turnstile *sitekey* is embedded in the rendered HTML; only the *secret* is secret).

## 6. New-user hand journeys (do all, record evidence)

- [x] **J1 Browse:** open `/` desktop + mobile → table pans/zooms, tiles show claim state/price/leader, no layout break at 360px
- [x] **J2 Search:** open search → type `carbon` → keyboard navigates, Enter opens Li drawer; screen-reader names intact
- [x] **J3 Claim (paused):** click money action → waitlist card (not checkout); join waitlist → success toast
- [ ] **J4 Claim (live sim):** fill title/pitch/URL → checkout → pay → `?paid=SYM` toast + tiles/ranks refresh, receipt email arrives
- [ ] **J5 Outbid/reclaim:** second user takes tile → victim outbid email → victim clicks reclaim link → prefilled amount → pays → crown returns
- [x] **J6 Report/moderate:** report a stake → confirm modal → admin triage → HIDE → tile/profile/`/go` hide → restore → reappears — **full round-trip verified on a local seeded DB** (`pt_j6`); prod has 0 stakes so this journey is unreachable there
- [x] **J7 Unsub (v1):** unsubscribe → emails stop, the stake still counts, homepage shows the `?unsub=done` toast (an unknown token lands on the same `?unsub=done` by design, not on `?unsub=unknown`) — **verified on a local seeded DB** (`pt_j6`); prod has no listings to unsubscribe from. **There is no manage journey in v1** — a listing is final at checkout; `POST /api/manage/request` answers politely and emails no link (§4c). Listing edits are v2.
- [x] **J8 Reduced-motion + keyboard:** `prefers-reduced-motion` → static but legible; Esc closes modals/drawers in order; 44px touch targets; no toast-behind-modal; axe E2E clean — **axe + reduced-motion + Esc order + 44px targets all done**

> **Journeys J1/J2/J3/J8 run against live paused production, 2026-09-11** (agent-browser, desktop 1280×577 + mobile 360×640); **J6 ran against a local seeded database** the same day, because production has no stakes. Evidence:
> - **J1 ✅** Real `role="grid"` of 122 `gridcell`s, every tile `unclaimed` (prod has 0 stakes), SR names `"C Carbon, unclaimed"`. Zoom out/in + Fit table present. **360px: zero horizontal overflow** (`innerWidth` 360 = `documentElement.scrollWidth` 360).
> - **J2 ✅** Search is a proper ARIA 1.2 combobox: focus **stays on the input** while `aria-activedescendant` moves `search-hit-1→2→3` **and resolves to a real element** (the check that usually fails silently); exactly one `aria-selected="true"`; ArrowUp reverses; `ca` → 6 results; Enter opens the **arrow-selected** result (Scandium), not the first.
> - **J8 ✅ (axe + reduced-motion)** axe `wcag2a,wcag2aa` → **0 violations / 22 passes**. The single "incomplete" was `color-contrast` on 314 decorative `.bg-sym-tw` watermark nodes; measured manually → **13.13–20.17 ratio, 0 failures**, and the layer root is `aria-hidden` (`components/BackgroundSymbols.tsx:88`), so it is correctly hidden from AT. Reduced-motion verified live via CDP media emulation (`matchMedia` true, `animationName: none`, `transform: none`, still visible at `opacity: .125`). Esc-closing order and 44px touch targets were open here and are recorded below.
> - **⛔ J3 found a real launch-blocking bug — fixed in `components/Modals.tsx`.** Clicking a money action correctly rendered the paused waitlist card (no `Continue to checkout`, no `cf-turnstile`, no `No refunds` anywhere in the DOM) and joining wrote a row with `source: "checkout-paused"`. **But `joinWaitlist()`'s success path called `onDone(...)` + `onClose()` and never `setWaitBusy(false)`** — relying on unmount to clear the flag. `CheckoutPreview` is mounted **once** at `app/page.tsx:364` and `<Modal open>` never unmounts it, so after **one** successful join the button stayed `"Joining…" [disabled]` **permanently and site-wide** — `if (waitBusy) return;` swallowed every later submit, plus the typed email leaked to other elements. Reproduced live on prod (joined Scandium, then Carbon showed the dead button); verified fixed on a local server — two elements, two successful joins. **This was the only conversion action on the site while paused.**
> - **⛔ Going live exposed a second launch blocker — the Turnstile script was never loaded (fixed: `components/TurnstileWidget.tsx`, new).** With `PAYMENTS_LIVE=true` the form rendered its `.cf-turnstile` div, but the loader was absent: `challenges.cloudflare.com` appeared **only** in the server-side `siteverify` call (`lib/abuse.ts`), and `next/script`/`<Script` is used nowhere in the repo, so `api.js` never ran, no challenge rendered, and no `cf-turnstile-response` field existed in the DOM. `turnstileEnabled()` is true whenever `TURNSTILE_SECRET` is set — it is, in production — so `verifyTurnstile(undefined)` returned `false` and **every** checkout answered `400 "Bot check failed. Try again."`. The payment path was dead on arrival: no user could have paid. Emitting the script tag alone would **not** have fixed it — `CheckoutPreview` mounts through `createPortal` into `document.body` and `Modal` returns `null` while closed (`components/Modal.tsx`), so the widget element does not exist when the loader's implicit DOM scan runs, and the `.cf-turnstile` class was an implicit-render marker with nothing to trigger it. The fix loads `api.js?render=explicit` on demand (memoised per page load, retried after a failure) and calls `window.turnstile.render()` from an effect once the container is committed, removing the widget on unmount. `next.config.mjs` **already** allowlisted `https://challenges.cloudflare.com` in both `script-src` and `frame-src`, which is what identified this as a forgotten tag rather than a policy decision. Verified on the **live origin after deploy** (`be1d5f8`): `window.turnstile` is an object, the script is in the DOM *under the production CSP* (the earlier localhost run could not test that — CSP is production-only), and the checkout modal contains `[name="cf-turnstile-response"]` (`cf-chl-widget-204od_response`). The sitekey is correctly scoped: the same `render()` answers **`110200` (domain not allowed) for `localhost`** but renders the challenge on `periodictable.lol`. An automated browser is then answered *"Verification failed"* — correct behaviour against a bot, and the reason the final solved-token proof is the human checkout in **J4** rather than something this evidence set can assert.
> - **⛔ A third launch blocker — every live checkout answered `502 "Payment provider unavailable. Try again."` — and it is a Whop-side key *permission* gap, not our code and not a missing key.** The checkout modal reached the route fine; the provider call failed. A first attempt to name the reason was inconclusive because **no attempt had been made since the diagnostics deploy**: the two failed tries visible in prod `Payment` (07:49/07:50 UTC, both `provider=whop`, `pending`, $5) predate `dpl_9VU2iPNi3xHUDpEY6WWgrSxrmPJy` (ready 07:56:32 UTC, sha `e359c65`). Once a real attempt was made *after* that deploy, the route's own warning named it on the first try: `whop: checkout_sessions rejected (HTTP 401, key=apik) — {"error":{"status":401,"message":"This API key does not have the necessary permissions to access the create_checkout_session endpoint. You can enable permissions on the developer settings page."}}`. So the key **is** present, correctly shaped (`apik_`) and recognised by Whop — it simply lacks the `create_checkout_session` scope. That also kills the two standing hypotheses: *"`WHOP_API_KEY` is missing in production"* was already refuted before the attempt (a local run has no Whop vars, so `provider=whop` rows can only come from prod), and *"our request body shape is wrong"* cannot produce an auth error — Whop validates the key **before** the body, so a body-shape problem would have surfaced as a 400, never a 401. **Fix is dashboard-side, and it is TWO steps — step 2 is the one that silently does nothing if skipped:** (1) Whop → Developer → the app → **Permissions** → **Add permissions** → enable checkout-session creation (plans/payments/checkout) → save; (2) Whop → **Settings → Authorized apps** → the app → **Re-approve** → approve the new scope. Whop's own docs are explicit: *"API calls that need the new permission fail until each creator re-approves."* Only then do §6's `401` and §3b's live `502` clear. If the key *value* changes, also re-paste `WHOP_API_KEY` in Vercel **Production** and redeploy (the value change alone does not grant scope). Until then the `502` is *correct*: the gate refuses to hand the buyer a dead `checkoutUrl`.

> **2026-09-12 11:15Z — the key was rotated and the permission was still missing, proven twice.** Timeline from Vercel + prod logs: `WHOP_API_KEY` `updatedAt = 1789211742328` (11:15:42Z) → deployment `dpl_5CuekhUAkbUMztWvkS84SCjGhe9z` (`sha e359c65`) created 11:15:47Z → aliased to `periodictable.lol` **and** `www.periodictable.lol` at 11:17:18Z (checked via `GET /v4/aliases?projectId=…`, so the alias was verified to point at the *new* deployment rather than assumed). A fresh probe at 11:18Z produced a **byte-identical** `401` to the pre-rotation one — same endpoint, same message. Two conclusions, both useful: the new key value **is** live (a wrong/missing key answers `401` with a *different* message about the key itself, not a message about the endpoint), and the app scope was not changed, because rotating a key never re-grants permissions.
>   - **The failed attempts are visible in the database, which pins down where the `502` fires:** production `Payment` now holds **four** rows, all `whop`/`pending`/`$5`, created `07:49:34`, `07:50:02`, `11:13:19` and `11:18:12.801` UTC — the last one matching the 11:18Z probe to the second. So the request **passed** `paymentsLiveServer()` → rate limit → JSON/honeypot/attest → `verifyTurnstile` → field validation, **committed** its `Payment` row in the transaction, and only then failed at the provider call. The `502` is therefore the **fresh** path after the tx (≈ line 346), not the replay path and not a pre-tx gate — which also means the four rows are inert orphans (`providerCheckoutUrl` empty, no money moved, no session at Whop) rather than a stuck queue: `GET /api/jobs/reconcile` is read-only and filtered to `status: "PAID"` (`app/api/jobs/reconcile/route.ts:47`), so nothing sweeps them and nothing can settle them into a stake.
> - **✅ Two of the three defects are now proven fixed against live production traffic, by the same probe that found the third.** Driving a **real Chrome** over the live origin end-to-end (territory panel → checkout modal → solved challenge → submit) produced the **first successful server-side Turnstile pass ever recorded on production**: the widget minted a token that *changed* after an explicit `reset()` (`changed=true`, 794 chars) and `POST /api/checkout` **stopped** answering `400 "Bot check failed."` — the siteverify gate (`lib/abuse.ts`) accepted it. (Immediately before that, submitting a **stale** token still returned exactly `400 Bot check failed. Try again.`, which is the gate failing closed as intended — a good negative control, and the reason a solved token must be minted and used inside its 300s TTL.) The subsequent `502` therefore comes from the *provider call alone*, with every earlier gate in `app/api/checkout/route.ts` (live flag, rate limit, JSON, honeypot, attest, Turnstile, field validation, tx) passing on real traffic. Only the Whop permission toggle stands between this and a live claim.
> - **J8 ✅ (Esc order)** Verified live on prod with real key presses. Search open → Esc: combobox `1 → 0`. Drawer open → Esc: drawer closed. **Stacked modal-over-drawer → Esc: `data-modal-open` `true→false`, `#app-root[inert]` cleared, drawer still open** — proving the sole-Esc-owner hand-off in `components/Modal.tsx:63-66` (`stopImmediatePropagation()`) absorbs the first Escape; a second Esc then closed the drawer underneath, so the page cascade (`app/page.tsx:157-166`) resumes correctly. (The element drawer is **not** `role="dialog"`, so the cascade runs by component state, not focus trap.)
> - **J8 ✅ (44px touch targets)** The 44px guarantees are gated behind `@media(pointer:coarse)`, so a headless mouse reports `coarse:false` and the styles never apply — a viewport-only measurement is meaningless. Measured under real coarse emulation via CDP (`Emulation.setTouchEmulationEnabled` + `setEmulatedMedia {pointer:coarse, hover:none}`): **two genuine defects found and fixed** — `components/HeroCard.tsx:73` claim CTA was `202×40` (now `202×44`) and the three `components/FooterBar.tsx` legal links were `98×17 / 93×17 / 40×17` (now `98×44 / 93×44 / 44×44`). The footer's `py` is dropped under the same media query so the pill grows to exactly 44px and its top edge lands at **64px from the bottom — still clear of the stale marker parked at 72px**. Result: **zero non-tile touch targets under 44px anywhere in the app**; desktop is byte-for-byte unchanged (`202×40`, `98×17`, nav top 49px). The 122 grid tiles stay `22×23` at every breakpoint by design — pinch-zoom is the recovery path and is real touch code (`components/TableCamera.tsx:255`).
> - **J6 ✅ (report → triage → hide → restore)** Prod has **0 stakes**, so this journey was run against a **local seeded DB** (`pt_j6`, cloned from `ptrehearse`; the stale `rehearse-take.dev` reports were deleted first so a zero-row assertion meant something). Intake: **Cancel sent nothing and wrote 0 rows**; Confirm sent exactly one POST → exactly one report (`status: OPEN`, the Hbar stake, `reason: "reported from profile"`). Triage `PATCH /api/admin/reports/:id` → `TRIAGED` ✅; negative paths **400** (bad status), **404** (unknown id), **403** (no token). Hide `POST /api/admin/startups/:domain/moderate` → **400 `REASON_REQUIRED`** with no reason, **400** on a bad state. With `HIDDEN` the takedown reached **every** public surface: profile **404**, `/go/<stakeId>` **404 `{"error":"Not found."}`** (the redirect is genuinely blocked, not just unlinked), and the listing vanished from table-order, search, board, the element API and the element page. Because a bare zero is weak evidence, a **control test** was run: searching `rehearse` returned **5** sibling listings with the hidden one **absent**. The element page renders *"1 listing is hidden from this table by moderation"* while `pool`/`count` **still include** the hidden stake — intentional, since financial history is never deleted. Restore flipped all six surfaces back (profile **200**, `/go` **302 → target**, listed again) and set `restoredAt`. **Financials survived the whole cycle untouched: 2 stakes, $18, 2 payments** — the moderation path's core safety claim. Audit trail: `REPORT_TRIAGED` plus two `PROFILE_MODERATED` rows carrying operator `j6-operator` and the reason. Prod's `/api/admin/*` returns **403** without a bearer token (checked on live).
> - **J6 — two findings.** *(1) a11y — fixed:* `components/ReportListingButton.tsx` swapped its visible text to `reported ✓` on success but kept a static **`aria-label` = "Report …"** that overrode it for AT, and announced the async result nowhere. Both fixed: the visible text is now the accessible name in **every** state (no `aria-label`, so it cannot drift — WCAG 2.5.3), and the result goes out through an `sr-only` `role="status" aria-live="polite"` region — the pattern `components/Modals.tsx` already used. Investigating it surfaced a **third defect the report button did not cause**: `send()` closed the modal and flipped to `pending` in the same render, so `Modal`'s focus-restore targeted a genuinely `disabled` button and **focus silently fell to `<body>` for the whole round-trip** (`components/Modal.tsx:91`, `:94`). `disabled` is now `aria-disabled` — the existing click guard already made double-submit impossible, so nothing was given up — and focus returns to the trigger again. Locked by a static contract in `lib/a11y.test.ts`, proven to fail against the old component. *(2) ops, by design:* restoring a listing does **not** bring back the preview that hiding clears (a documented retention policy — hiding a phishing site shouldn't leave you retaining its screenshot). The only paths back are a new **paid stake** (`lib/settle.ts:229`) or an explicit `{backfill: true}` on the screenshot job, and the **daily cron never passes `backfill`** (`vercel.json` → `GET`, no body), so a *mistaken* hide leaves that tile preview-less until an operator runs `scripts/backfill-previews.ts`. Worth adding to the takedown runbook. The `attachPreview` guard from §5 (`moderationState: "VISIBLE"`) was independently re-read and re-confirmed: an in-flight Microlink probe cannot resurrect a hidden listing's preview.
> - **J7 ✅ (unsubscribe)** Run against the same local seeded DB — prod has no listings to unsubscribe from — where all 15 startups had an `unsubToken` but **none had an `email`**, so one was set as a precondition. Contract confirmed: `GET /api/unsubscribe?token=` renders the confirm form and **does not mutate** (email still set afterwards), while the `POST` behind it clears `email`, **keeps `unsubToken`** so the link stays reusable and idempotent, and **307s to `/?unsub=done`**. The homepage toast *"You're unsubscribed. Past stake still counts."* rendered at t≈2s, the query was stripped via `history.replaceState`, and it auto-dismissed by t≈7s (so poll at sub-second intervals — a one-shot check after a 5s sleep misses it). The `?unsub=unknown` control renders *"Already unsubscribed or unknown link."* correctly. **"The stake still counts" holds:** profile `200`, still in search, still on the element API, and **still `currentLeaderId` for both Hbar and Li**. Financials untouched: **2 stakes / $20 / 4 payments**.
> - **⛔ J7 found a silent-failure bug — fixed in `app/api/unsubscribe/route.ts`.** The route advertised RFC 8058 one-click (`lib/email.ts:50-51` sends both `List-Unsubscribe` and `List-Unsubscribe-Post: List-Unsubscribe=One-Click`) but read the token **only from the request body**, with the query-string fallback parked in the `catch`. A one-click client posts the token **in the URL** with body `List-Unsubscribe=One-Click` — which parses as *valid* form data, so that fallback never ran, `form.get("token")` returned null, and the request became a **no-op that still redirected to `?unsub=done`**. Reproduced live (307 + success toast, email unchanged): the Gmail/Yahoo native **Unsubscribe** button would tell the user they had unsubscribed and then keep mailing them — and one-click is a stated bulk-sender requirement. Fixed by taking the query token first and the body as fallback, and covered by a new `lib/unsubscribe.test.ts` (5 tests) **which fails on the pre-fix code (2 of the 5) and passes on the fix** — verified by restoring the old route temporarily. Reassuringly, `clearEmail("")` early-returns `"unknown"`, so the malformed request was a silent no-op and never a mass-unsubscribe.
> - **⚠️ Checklist drift, corrected:** no code path ever produced `?unsub=unknown`. The route returns the same shape for unknown tokens on purpose (no oracle) and always redirects to `?unsub=done`, so the client branch at `app/page.tsx:118` is unreachable in the real flow. The old text also claimed `GET` clears the email — the one thing it must never do. Both fixed in §4c.
> - Existing tests cover the waitlist **API/DB** (`manage.test.ts`) but **nothing covered the client component's state**, which is why the suite never caught this. Consider a component-level test for the paused join path.

## 7. GO / NO-GO (all must be GO)

| Gate | GO criteria |
| --- | --- |
| Codebase | lint + typecheck + test:ci (0 skipped) + audit:prod green |
| Data/domain | 122 elements, 6 APIs 200 <2s, DNS + 308 + headers correct |
| Money | paused 403 now; live rehearsal green in sim; Stripe contract proven with fixtures |
| Email/jobs | fresh Resend key, inbox receipt received, cron draining outbox+screenshots |
| Trust/frontend | Turnstile + ADMIN_TOKEN live, no HIDDEN leaks, honest error panels, kill switch rehearsed; Upstash decided 2026-09-14 (unwired, fails open, accepted — §5) |
| Flip (LAST) | `PAYMENTS_LIVE=true` + `NEXT_PUBLIC_PAYMENTS_LIVE=true` + Stripe live keys + webhook `https://www.periodictable.lol/api/webhooks/stripe` registered → redeploy → $1 live claim → refund/keep → announce |

### Where the release stands, 2026-09-14 (latest) — Stripe is live; the site is empty and ready to announce

**Read this entry instead of the 2026-09-14 "Where it stands" paragraph below**, which describes a commit where neither `STRIPE_*` var existed yet. Everything here is runtime behaviour read from the running site, never a pulled env file.

- **The provider is live and out of dev mode.** `POST /api/dev/pay {}` → **`403 {"error":"Disabled when Stripe is enabled."}`** — the check the paragraph below asks for. It is the first statement of the handler (`app/api/dev/pay/route.ts:15`), above body parsing, so it fires before anything else and proves `getProviderMode() === "stripe"` (`lib/stripe.ts:28`): **both `STRIPE_SECRET_KEY` and `STRIPE_WEBHOOK_SECRET` are set in Vercel Production.** The old `400 {"error":"paymentId required"}` reading — the evidence for dev provider mode — is gone.
- **The env half of the flip gate is complete, independently confirmed.** `GET /api/jobs/config?secret=…` → **`200 {"ok":true,…}`** whose only finding is the Upstash one settled in §5. It was `503` listing `STRIPE_SECRET_KEY` and `STRIPE_WEBHOOK_SECRET` as `required` when the 09-15 entry was written, so the flip to `200` is a second, independent witness that the keys are present and usable.
- **The third blocker is closed — by the migration, not by a Whop toggle.** Driving the **real site** (element tile → `$5` CTA → solved Turnstile → submit) reaches **Stripe's hosted payment page**. The `502 "Payment provider unavailable."` and the `401` permission diagnosis below are now purely historical.
- **§3b re-run green on the current code — and its documented recipe had expired.** Local sim (throwaway `ptrehearse` + a production build): **18 passed, 1 skipped**, the skip still being the provider-outage case that is already closed against the real provider above. The 09-11 recipe 403'd at the *first* checkout: `providerConfigured()` (`lib/flags.ts:10-12`) and `stripeEnabled()` (`lib/stripe.ts:18-19`) now read the **same** `STRIPE_*` pair, so withholding the API key while setting the webhook secret can no longer keep the dev simulator reachable; the lever that does is `VERCEL_ENV=development` (`lib/env.ts:23-28`). Evidence and the corrected procedure are in §3b.
- **§4c is closed by a person, not by a status field.** Both mails — the receipt (*You're #1 in C (Carbon) 🎉*) and the outbid notice (*You were knocked off C 👑*, quoting *Reclaim it for $4.00*) — were found **in the Gmail inbox, not Spam**. That was the last open item in §4c.

### Pre-announce cleanup applied, 2026-09-14 — production is now genuinely empty

**The blocker described in the next paragraph is closed.** It read, live: `/api/stats` = `122` elements, **`1` claimed, `2` stakes, `$17` staked**, `/api/board` crown #1 = `rehearse-first.dev`, and `totalPoolUsd: 17` on Carbon — our own J5 crown-flip rehearsal being served to visitors. All of it is gone. Recorded as before/after, because the failure mode here is silent:

- **Before** — `7` `VISIBLE` startups; exactly `2` stakes, both on `Element 6`, `$9` leader `rehearse-first.dev` / `$8` `rehearse-join.dev`; `1` `FirstClaim`; `Element 6` at `stakeCount: 2, totalPoolUsd: 17, currentLeaderId` set. Live: crown board non-empty, `/s/rehearse-first.dev` `200`, `/api/search?q=a` listed it.
- **After** — one `$transaction`: delete `FirstClaim` → null `Payment.stakeId` on the `3` rows pointing at those stakes → delete both `Stake` rows → set all `7` startups `moderationState: "HIDDEN"` → reset `Element 6`. Guards in the same script throw unless exactly `7` fixtures exist and `0` stakes / `0` `VISIBLE` startups remain. Printed: `stakes=0 visibleStartups=0 claimedElements=0 payments=11 providerEvents=11 firstClaim=0 hidden=7`.
- **Verified live, not in the database** — `/api/stats` `{"elementsTotal":122,"claimedElements":0,"unclaimedElements":122,"stakeCount":0,"totalStakedUsd":0}`; `/api/board?tab=crowns` `[]`; `/api/activity?limit=20` `[]`; `/api/search?q=a` elements only; `/s/rehearse-first.dev` `404`; `/api/elements/C` `pool:0, count:0`, no stakes.

**Why hiding alone could not do it, and why the stakes had to be unwound.** `lib/moderation.ts` is explicit that hidden financial history is **never deleted** and that hidden money **stays in pool/count totals** — so `HIDDEN` cannot clear `/api/stats`, which ignores moderation entirely and counts `stakeCount > 0`. A hard delete was never available either: `Payment.startupId`/`elementId` are required and every relation is `Restrict` (no cascades), and a `payment.groupBy` proved every startup carries payments. So the startups were hidden (killing the profile, search, `/go` and board surfaces at once), while the two stakes — the only thing the public counters read — were deleted outright. Activity rows stay concealed *because* their domain still resolves as `HIDDEN`: deleting the startup row would have re-published them.

**The near-miss worth remembering: `Element` has THREE denormalized aggregates, and zeroing two of them lies.** The first pass cleared `currentLeaderId` and `stakeCount` but left `totalPoolUsd` at `17`. Nothing threw — the page simply rendered `pool: 17` next to `count: 0`. `lib/recompute.ts`'s `rerankElementTx` writes all three, and `assertLedgerInvariants` (asserted before commit) requires `totalPoolUsd === sum(ranked stakes)`, so production was sitting in a state that would **throw on the next settle**. It was caught by reading `/api/elements/C`, not by querying the database — **any manual ledger surgery must recompute all three, and be checked from the API.**

**Stale prerender is self-healing here, so no redeploy was owed.** `app/elements/[sym]/page.tsx` sets `export const revalidate = 60`, so the cached `/elements/C` HTML still carrying a fixture name healed within a minute on its own. `app/page.tsx` is `"use client"` + SWR and was never stale; its `claimed` matches are `245` instances of the ordinary `Unclaimed · $5` tile label.

**Kept deliberately, and one open decision.** `11` `Payment`, `11` `ProviderEvent`, `11` `OutboxEvent`, `7` `EmailLog`, `3` `ActivityLog`, `25` `AuditLog` all survive — they are the money-path and mail evidence the launch rests on. The open decision is the `4` legacy `WHOP`/`PENDING` rows plus the `2` `STRIPE`/`PENDING` rows with no `providerRef`: inert by construction (`/api/jobs/reconcile` is read-only and `PAID`-only, and no money moved), but in any ledger view they are indistinguishable from a genuine unpaid `$5`. Clearing them is optional and reversible-by-record; if left, the operator run must say so up front.

**The blocker that replaced it, found by the same live reading: the site is not empty — it publicly shows our own rehearsal.** *(⚠️ SUPERSEDED — fixed by the cleanup above; kept as the record of how it was found.)* `/api/stats` returned `{"elementsTotal":122,"claimedElements":1,"stakeCount":2,"totalStakedUsd":17}`, and `/api/board` (crowns tab) returns `rehearse-first.dev` as crown #1 with `total:1, totalSpent:9`. Those are the two `rehearse-*.dev` fixtures from the J5 crown-flip rehearsal, still **holding Carbon**, visible to any visitor — and they make the J6 line above ("prod has 0 stakes") false as of today. Announcing in this state shows the first visitor a fake startup wearing a crown, so this is a pre-announce cleanup item, not cosmetics. **Exactly what the cleanup has to cover, enumerated from the database rather than estimated:**

- **The public leak is only the two staked listings.** `/s/rehearse-first.dev` returns `200` and `/api/search?q=a` lists it, so both are genuinely reachable. The `Startup` table holds **7** `VISIBLE` rows, but the other five are **not reachable anywhere public** — `app/s/[domain]/page.tsx:47` returns `notFound()` whenever `stakes.length === 0`, so `/s/facebook.com`, `/s/github.com`, `/s/keys.lol`, `/s/probedia.social` and `/s/rehearse-live.dev` all return **404** (verified live). They are DB-only residue that would surface the instant anyone staked them, not a live exposure.
- **`Payment` holds 11 rows in four distinct shapes:** **4** legacy `WHOP`/`PENDING` from 09-12 with no `providerRef` (the ones documented below); **2** `STRIPE`/`PENDING` with no `providerRef` (the two checkouts that never reached Stripe, one under `danil.gorbunov@ibrconsulting.cz`); **2** `STRIPE`/`PENDING` carrying a real `cs_live_…` session with no stake behind them; and **3** `STRIPE`/`PAID`, all carrying a `cs_live_…` reference **and** a stake (the two crown-flip fixtures at `$5`/`$8` plus the `$4` reclaim). No money moved on any of them, the three `PAID` ones are not revenue, and none can be swept into a stake (`/api/jobs/reconcile` is read-only and `PAID`-only) — but all 11 are indistinguishable from a genuine unpaid `$5` in any ledger view, which is why they go before announce.
- Other non-zero residue to decide on in the same pass: `ProviderEvent` 11, `OutboxEvent` 11, `EmailLog` 7, `ActivityLog` 3, `AuditLog` 25, `FirstClaim` 1. (`Report`, `WaitlistEntry`, `ClickEvent`, `ClaimReservation`, `ManageToken`, `ManageSession` are all **0**.)

**Webhook edge cases are now proven against production itself, not only in the local rehearsal** — four cases, fixtures on empty Xe, all fixture rows deleted afterwards and the table counts verified back at baseline (§3c carries the table). Alongside that, the mail fix from `4d24b8b` was proven live: a webhook-settled `$5` on Xe flipped `Payment` to `PAID` at `11:30:09.918Z`, wrote a leading stake, and delivered the receipt `EmailLog` row `sent` at `11:30:11.752Z` — **~2 s after settle** against the old ~5-minute lease park, with the webhook answering `200 {"ok":true,"outcome":"applied"}` in 7282 ms. That closes §4c's receipt item and the J4 half of it that code can close.

**What is actually left:**

1. ~~**Clear the rehearsal content** (the fixture startups/stakes above plus the probe `Payment` rows).~~ **DONE, 2026-09-14** — see "Pre-announce cleanup applied" at the top of this section. 0 stakes, 0 `VISIBLE` startups, 0 claimed, `$0` staked, crown board and activity feed empty, verified from the live API; the `11` `Payment` rows were kept as money-path evidence (`12` as of the re-read below), with the `6` dead `PENDING` ones left as an optional pre-announce decision.
2. **§3b's live rehearsal cannot be re-run against production**, and the instruction to do so should be treated as retired rather than pending: `/api/dev/pay` `403`s whenever Stripe is fully configured (`app/api/dev/pay/route.ts:15`, just demonstrated) and `POST /api/checkout` verifies Turnstile unconditionally (`app/api/checkout/route.ts:132`), so both money gates are shut to a script. Its webhook half is what the four-case sweep above covers instead.
3. **J4 for real** — the one gate that needs an actual card: a live `$5` through Stripe, then confirm `Payment.status = PAID`, the tile lights, and the receipt lands in a real inbox. Until then, `Money` is GO on contract evidence and the receipt row is `sent` without a human having seen it.
4. **Two `WHOP_*` vars still sit in Vercel Production.** They are unused by the code (the enum value survives only to deserialize the four legacy rows) and Stripe is now proven, which was the stated precondition for deleting them.
5. ~~`Codebase` needs a fresh `lint + typecheck + test:ci` run before announce~~ **DONE, 2026-09-14 — the whole `Codebase` gate is green.** `npm run lint` clean, `npm run typecheck` clean, `npm run test:ci` **363 passed / 24 files / 0 skipped**, `npm run audit:prod` green. Two things came out of running it that are worth keeping:
   - **`audit:prod` could not run on Windows at all, and now can.** `execFileSync("npm", …)` dies with `ENOENT` because npm is a `.cmd` shim (and `execFileSync("npm.cmd", …)` dies with `EINVAL` — CreateProcess will not launch a batch file), so the gate was silently unrunnable on the operator's machine and only ever passed in CI on Linux. It now uses the single-string `execSync` form, the one shape that behaves identically on both platforms without Node 22's `DEP0190`.
   - **The audit is genuinely red underneath, and the acceptance was stale.** `npm audit --omit=dev` reports `next` at **critical**, range `9.5.0 - 15.5.23`, with no 14.x fix (only `next@16.3.5`, semver-major + React 19). `npm audit` still exits 0 through the gate because the entry pins `package` + `version`, but the entry's own text claimed `high` and scheduled its migration *"before live payments"* — which is now in the past. Both criticals were assessed **individually by reachability, and neither is reachable here**: the first is RCE on *Windows-hosted* servers (production is Vercel Linux, `iad1`); the second is RCE in the Image Optimization API *when AVIF is used*, and `next/image` is imported **0** times while `next.config.mjs` declares **no `images` key** — so no `remotePatterns`, and formats stay webp-only. Verified live, not from defaults: `/_next/image` with a remote url → **`400 Bad request`**, and the same endpoint sending `Accept: image/avif` → **`200 content-type: image/webp`**, so the AVIF branch is never taken. Four more advisories are unreachable because the app declares **0** `"use server"` directives, ships **no `middleware.ts`**, and configures no rewrites/i18n/WebSocket upgrades. What remains reachable is unauthenticated DoS against App Router RSC plus moderate cache-confusion — availability only. The allowlist entry now says all of this, `severity: critical`, and carries an explicit **tripwire**: the acceptance holds only while `images` stays absent from `next.config.mjs` and hosting stays Linux; either changing makes both criticals live. The Next 16 migration is now recorded as post-launch debt.

**Production inventory re-read, 2026-09-14 12:30 UTC (read-only, `psql` against Neon through `DATABASE_URL`).** Whole-table counts, not samples: `Payment` `12`, `ProviderEvent` `11`, `OutboxEvent` `11`, `EmailLog` `7`, `AuditLog` `26`, `ActivityLog` `3`, `Startup` `7` (all `moderationState = hidden`), and `Stake` / `FirstClaim` / claimed elements all `0` with every pool `$0` — the cleanup's printed guards still hold hours later, so nothing has regenerated a stake on its own. Three corrections to the cleanup paragraph above, all from this read:

- **`Payment` is `12` rows, not `11`,** and the shapes are five, not four. The new row is `stripe`/`PENDING`, **`$25`**, `cs_live_a1sKsz…`, created `2026-09-14 12:27:13` — the operator's own live checkout a few minutes before this read. `$25` is not a pricing bug: the modal's amount field is free-entry, so that figure is simply what was typed. The other four shapes are unchanged: `4` `WHOP`/`PENDING` (legacy, no `providerRef`), `2` `STRIPE`/`PENDING` with no `providerRef`, and now `3` — not `2` — `STRIPE`/`PENDING` carrying a real `cs_live_…` session.
- **The `3` `PAID` rows are stake-less by construction, not by corruption.** Each has `stakeId = NULL` while `appliedAt` remains (`09:57:05.964`, `09:57:12.016`, `11:08:37.147`), because nulling the link and deleting the stakes *is* what the cleanup did — the `appliedAt` timestamps are the settle proof, and they are deliberately left standing. This is the shape `lib/webhook.test.ts` treats as a corrupted ledger when the app produces it, so it should never be read as reachable-by-bug: only manual surgery makes it, and `/api/stats` + `/api/board` both report empty.
- **The lowercase-enum trap, for the next operator who queries this database by hand.** The Prisma enums are `pending|paid|failed|refunded|canceled` and `visible|hidden|unlisted`, while the API/JSON layer uppercases both. A query written from the API's vocabulary (`status = 'PAID'`, `moderationState = 'VISIBLE'`) throws `invalid input value for enum` instead of returning rows — and under PowerShell that error is easy to skim past. The DB is lowercase; the API is uppercase; the two disagree by design.

### Provider migration, 2026-09-14 — Whop → Stripe (supersedes the third blocker below)

The third blocker was not fixed, it was **designed out**. The Whop app scope was never granted, and rotating the key could not grant it, so payments moved to Stripe outright. The 2026-09-14 section below is kept verbatim as the record of how the `401` was diagnosed; its two dashboard steps and its Whop env names are **obsolete**.

What replaced what:

| Was | Now |
| --- | --- |
| `lib/whop.ts` | `lib/stripe.ts` (**both files deleted/replaced**, not wrapped) |
| `POST /api/webhooks/whop` | `POST /api/webhooks/stripe` |
| `WHOP_API_KEY`, `WHOP_WEBHOOK_SECRET` | `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET` |
| `whopEnabled()` / `getProviderMode() === "whop"` | `stripeEnabled()` / `getProviderMode() === "stripe"` |
| legacy HMAC webhook envelope | `stripe-signature: t=…,v1=…` over `${t}.${body}` |

Money-path rules the port had to get right, each one a way to double-credit or lose a stake if missed:

- `unit_amount` is integer **cents** (`Math.round(amountUsd * 100)`), `mode=payment`.
- One purchase emits `checkout.session.completed` **and** `payment_intent.succeeded` **and** `charge.succeeded`. Only the session event settles; the siblings are ignored, or one $5 buys two top-ups.
- `metadata.paymentId` is written to **both** the session and the payment intent. Reversals arrive as a `charge`, which carries only the intent's metadata — without it every refund files `IGNORED/unknown-payment` and the buyer keeps both the refund and the stake.
- `charge.refunded` is matched **by event type**, because a refunded charge's own `status` stays `succeeded`. `payment_intent.payment_failed` is not a failure (the session stays payable) and `refund.created`/`refund.updated` are not reversals (pending, can still fail).
- `v1` signatures need an explicit 300s tolerance; the secret has no validator in `lib/env.ts`, so the only real check remains a live delivery returning `200` (§3c).

`WHOP` is retained in the `PaymentProvider` enum (`STRIPE @map("stripe")` added by migration `0006`) **only** so the four legacy prod `Payment` rows still deserialize — Prisma throws when it reads an enum value it cannot map. Those four rows are unchanged and still inert.

Evidence at this commit: migration `0006` applies cleanly to an empty database; `npm run typecheck` and `npm run lint` clean; `npm run test:ci` **357 passed / 0 failed** (23 files, up from 337) against the throwaway Postgres, including a new `lib/stripe.test.ts` and the ported webhook/reconcile/env/idempotency suites.

**Where it stands, pre-keys, measured by runtime behaviour rather than a pulled env file — ⚠️ SUPERSEDED, read the 2026-09-14 `latest` entry above instead.** Both readings below have since inverted and are kept only as the record of the dev-mode state: `GET /api/jobs/config` now answers `200` (its only finding is the Upstash one), and `POST /api/dev/pay` now answers `403 {"error":"Disabled when Stripe is enabled."}` rather than `400 {"error":"paymentId required"}` — i.e. `getProviderMode()` is **`"stripe"`**, not `"dev"`. The reasoning that follows about *how* to read those two responses is still correct and still the right way to tell the modes apart, which is why it is not deleted. Original text: `GET /api/jobs/config` reports `STRIPE_SECRET_KEY` and `STRIPE_WEBHOOK_SECRET` as `required`/missing (`503`), and `POST /api/dev/pay` answers `400 {"error":"paymentId required"}` — *not* the `403 "Disabled when Stripe is enabled."` that its own first line returns in Stripe mode (`app/api/dev/pay/route.ts:15` — the first statement of the handler, above body parsing), because `getProviderMode()` (`lib/stripe.ts:28`) is `"dev"` unless **both** vars are set (`lib/stripe.ts:18`). So production is in **dev provider mode**, and no checkout can leave the paused state until both keys exist, whatever `PAYMENTS_LIVE` says: the gate is `paymentsLiveServer()` (`lib/flags.ts:14`), which ANDs the flag with `providerConfigured()` (`lib/flags.ts:10-12`). That same reading is why the exposed simulator is inert rather than a hole — no `DEV` payment can be minted while checkout is paused, and the route refuses any non-`DEV` row — and it is the check to re-run the moment the keys land (expect `403 "Disabled when Stripe is enabled."`).

**Client half and webhook endpoint, 2026-09-14 — also measured, not inferred.** The deployed bundle is built with `NEXT_PUBLIC_PAYMENTS_LIVE="true"`: the page chunk (94,585 bytes) carries the whole live checkout branch (`Continue to checkout`, `Stake on`, `cf-turnstile`, the Turnstile widget) and **zero** `Payments are paused`, i.e. the *waitlist* branch is the dead code now (see the correction in the 2026-09-11 audit below). So **no client-side work is owed by the flip** — after the keys land the only remaining change is server env, and the redeploy is needed because Vercel applies an env change in a *new* deployment, not because anything is baked. Two consequences worth holding onto:

- **Do not announce or soft-link the site before the keys land.** For the first time the client and server disagree in the *visible* direction: the client ships the live checkout branch, while the server is still effectively paused (dev provider mode ⇒ `paymentsLiveServer()` false). A visitor today can open the checkout modal on any tile, solve Turnstile, submit — and gets `403 "Payments are paused — join the waitlist."` rendered as a form error (`components/Modals.tsx:194-209`), with **no** waitlist capture form on screen, because that is the branch that got compiled out. The only visitor-facing path that exists right now is a form that always fails.
- **The endpoint to register is live and fails closed.** `POST https://www.periodictable.lol/api/webhooks/stripe` answers **401 `{"error":"bad signature"}`** in 0.66s both with no `Stripe-Signature` header and with a bogus `t=1,v1=deadbeef`, and `/api/webhooks/whop` answers **404** — the old provider's route is gone (`dd2c8c3`), not merely unused. Register `https://www.periodictable.lol/api/webhooks/stripe` for `checkout.session.completed`, `checkout.session.expired`, `charge.refunded` and `charge.dispute.*`.

Still to do before announce — all ops, no code: ~~register `https://www.periodictable.lol/api/webhooks/stripe` for `checkout.session.completed`, `checkout.session.expired`, `charge.refunded` and `charge.dispute.*`~~ **DONE and verified, 2026-09-14** — the live destination `we_1UFVsvQ8DCQQIpW5GtOvFfws` (`periodic-table`, `Active`, URL `/api/webhooks/stripe`) carries **exactly the seven types the handler matches**, read from its own "Listening to" list rather than inferred from the count of `7` (a `payment_intent.*` selection of the same size would look identical): `checkout.session.completed`, `checkout.session.async_payment_succeeded`, `checkout.session.expired`, `checkout.session.async_payment_failed`, `charge.refunded`, `charge.dispute.created`, `charge.dispute.funds_withdrawn`. That closes the one reversal path nothing had ever confirmed — a refund or chargeback for an unsubscribed type never unwinds the stake and never errors. `0` deliveries so far, so line 117's "`charge.refunded` has no live delivery behind it" still stands as *evidence*, not as a gap in configuration; set `STRIPE_SECRET_KEY` (live) and `STRIPE_WEBHOOK_SECRET` in Vercel Production **and redeploy** (Vercel applies env changes only in a new deployment; nothing on the client side is owed — see the paragraph above) — **both set and proven by runtime behaviour**: the live `$5` checkout reaches Stripe's page with the previous key expired (see the 2026-09-14 `latest` entry above); delete the now-unused `WHOP_*` vars **after** Stripe is proven — Stripe is proven, so this is the one op left on this line; then re-run J4 for real (a live $5 that reaches Stripe's payment page, completes, flips `Payment.status` to `PAID`, lights the tile, and lands the receipt email). **Migration `0006` is no longer one of those ops steps:** the production build applies it (`scripts/migrate-if-production.mjs`, `VERCEL_ENV=production` only — §0 item 14), so merging to `main` is what puts it on Neon, and a build that cannot migrate fails rather than deploying.

### Where the release stands, 2026-09-14 — read this first on a fresh machine

> **Superseded in part by the 2026-09-14 entry above** — the third blocker's Whop dashboard steps no longer apply, and `WHOP_WEBHOOK_SECRET` is now `STRIPE_WEBHOOK_SECRET`. Everything about Turnstile, §5, and the four orphan rows still stands.

Payments are **live** in production (`PAYMENTS_LIVE=true` and `NEXT_PUBLIC_PAYMENTS_LIVE=true`, both re-confirmed 2026-09-14). The site is healthy and empty: `/api/stats` = `122` elements, `0` claimed, `0` stakes; `POST /api/checkout` with an empty body answers `400` (validation), not `403` (paused), so the live gate is open. Three launch blockers were found by going live. Two are fixed and proven against real production traffic; the third is one Whop-dashboard action.

| Blocker | State |
| --- | --- |
| Missing Turnstile loader → every checkout `400 "Bot check failed."` | **FIXED** (`be1d5f8`, `components/TurnstileWidget.tsx`) — proven with a real solved token passing siteverify on live prod (§6) |
| Turnstile failing silently, no server-side reason recorded | **FIXED** (`23dfe74`, `lib/abuse.ts`) — the reason is now logged, and the gate is proven to fail *closed* on a stale token |
| Every live checkout `502 "Payment provider unavailable."` | **DIAGNOSED, NOT FIXED** — Whop app permission missing; two-step dashboard fix in §6's third blocker |

**The one action that unblocks launch — dashboard only, no code and no deploy:**
1. Whop → **Developer** → app → **Permissions** → **Add permissions** → enable checkout-session creation (+ plans/payments) → **Save**
2. Whop → **Settings → Authorized apps** → the app → **Re-approve** (Whop: *"API calls that need the new permission fail until each creator re-approves."*)

**Then, in order:**
1. **Verify the fix by using the site** — any element tile → the `$5` CTA → solve/auto-solve Turnstile → submit. Landing on Whop's payment page means the `502` is gone; the API key is proven good, only the scope was ever wrong. Completing that payment **is** journey **J4** above, so check in the same pass that the `Payment` row flips to `paid`, the tile shows a stake, and the receipt email arrives — that closes J4 and §4c's receipt item together.
2. **Settle §5 in one call:** `curl -s "https://www.periodictable.lol/api/jobs/config?secret=$CRON_SECRET"`. A `degraded` finding naming `UPSTASH_REDIS_REST_URL + UPSTASH_REDIS_REST_TOKEN` means the rate-limit store is genuinely unwired (fails open, not a blocker — reasoning in §5); no such finding closes it.
3. Close **J5** (outbid/reclaim) and the remaining §4c mail items, then re-run §3b's live rehearsal against `BASE_URL=https://www.periodictable.lol` with the real `ADMIN_TOKEN` / `WHOP_WEBHOOK_SECRET`.
4. Everything above is non-code. The only open decision that *would* touch code is §5's Upstash store, and only if step 2 reports it degraded.

**Expect four orphan rows in production `Payment`, and do not read them as revenue:** all four are `provider=whop`, `status=pending`, `$5`, `providerCheckoutUrl` empty, created 2026-09-12 at `07:49:34`, `07:50:02`, `11:13:19` and `11:18:12` UTC — our own probes, each one committing its row inside the checkout transaction and then being orphaned when Whop answered `401`. No money moved and no checkout session exists at Whop. They are inert by construction, not merely by luck: `GET /api/jobs/reconcile` is read-only and filtered to `status: "PAID"` (`app/api/jobs/reconcile/route.ts:47`), so nothing sweeps them, settles them into a stake, or hands out a crown. The 11:18:12 row is the useful one — it timestamps the post-rotation probe and proves the request reached the provider call, which is what places the `502` on the post-transaction path (§6).

**Repo state at this handoff:** `main`, clean, pushed. The commits that carry the two fixes are `be1d5f8` (Turnstile loader), `23dfe74` (siteverify diagnostics), `e359c65` (Whop self-reporting warnings), plus this checklist. Nothing else is half-finished, and no local-only state is needed to continue — every secret lives in Vercel, so `git pull` on any other machine is a complete handoff.

### Live-state audit of production env, 2026-09-11

Read from the Vercel API, not from memory. Two of these contradict what this
checklist said earlier, so they are recorded here rather than fixed quietly.

- **`NEXT_PUBLIC_PAYMENTS_LIVE` exists but is EMPTY** (`""`, length 0). It is
  not unset and not `false` and not `true` — it is a zero-length value.
  `lib/flags.ts` compares it with `=== "true"`, so an empty value is `false`:
  the client is paused. **The server has no `PAYMENTS_LIVE` at all**, so it is
  paused too. Both halves are consistent today, which is why nothing looks
  broken.
- **The consequence is bigger than a flag.** `components/Modals.tsx:121` is
  `const paused = !paymentsLiveClient()`. With `NEXT_PUBLIC_PAYMENTS_LIVE`
  inlined at build time as `""`, `paymentsLiveClient()` folds to `false`, so
  `paused` folds to `true` and the bundler drops the live checkout branch as
  dead code. Proof from the deployed chunks for `26f2d5e` (confirmed as the
  deployed commit via `meta.githubCommitSha`): the page chunk 85,449 bytes
  contains `How claiming works` and `waitlist` (×4) but **zero** occurrences of
  `cf-turnstile`, `Continue to checkout`, `No refunds`, or `0x4AAAAA…`. The
  attest checkbox and the Turnstile widget are not shipped to the browser at
  all right now.
  - **⚠️ Superseded 2026-09-14 — the deployed bundle has since flipped.** The same measurement against the current deployment (page chunk now **94,585 bytes**) counts **1** `Continue to checkout`, **2** `Starting checkout`, **2** `Stake on`, **1** `cf-turnstile` and **9** `turnstile`, and **0** `Payments are paused`. So `NEXT_PUBLIC_PAYMENTS_LIVE` is inlined as `"true"` in the commit that is serving today, and it is now the **waitlist** branch that is dead code. The 2026-09-11 reading describes `26f2d5e` and no longer describes production; the two flag clauses below are kept as history, not as current fact.
- This is why the Turnstile box in §5 can only be closed after launch: there is
  no live checkout to inspect until the flip rebuilds with the flag set.
- **The flip must therefore include a redeploy.** `NEXT_PUBLIC_*` is inlined at
  build time; changing the value without rebuilding ships the old bundle and
  the site keeps showing the waitlist card forever.
- The failure mode to watch for: if `PAYMENTS_LIVE` went live while
  `NEXT_PUBLIC_PAYMENTS_LIVE` stayed empty, no widget renders, no token is ever
  sent, and `verifyTurnstile` returns `false` — so every real checkout 400s with
  "Bot check failed." with no server-side signal that anything is wrong.
- `UPSTASH_REDIS_REST_URL` and `UPSTASH_REDIS_REST_TOKEN` **are** present in
  production. Corrections applied to §5 and the appendix below.
- **Correction to that correction, 2026-09-14:** §5 has since been re-opened.
  The names being present is all this audit proved; production logs show the
  runtime treating the pair as unset (see §5 for the log line, why a pulled
  value can never settle it, and the one call that does). The audit above is
  left standing as written so the correction trail is visible.

## Appendix — copy-paste cheat sheet

```
npm ci
npx prisma migrate deploy
npm run lint; npm run typecheck
TEST_DATABASE_URL=postgresql://… npm run test:ci
npm run audit:prod
VERCEL_ENV=production NODE_ENV=production node scripts/check-prod-env.mjs
BASE_URL=https://www.periodictable.lol bash scripts/rehearse-release.sh paused
BASE_URL=http://localhost:3100 ADMIN_TOKEN=… STRIPE_WEBHOOK_SECRET=… bash scripts/rehearse-release.sh live
curl -s https://www.periodictable.lol/api/stats | python3 -m json.tool
curl -s "https://www.periodictable.lol/api/jobs/config?secret=$CRON_SECRET" | python3 -m json.tool   # §5 re-run: expect 200 and only the Upstash degradation (the pre-keys reading was 503 + both STRIPE_* vars required)
```
A note on the env vars below: they exist in Vercel, but **their values cannot be read back** — `vercel env pull` returns `""` for every Sensitive variable (including `DATABASE_URL`, which demonstrably works), and the CLI's raw `vca_…` token 403s against `api.vercel.com` because it expires hourly. So treat *presence* as the only fact obtainable from `vercel env ls`, and prove *usability* through runtime behaviour (`/api/jobs/config` for Upstash, `/api/stats` for the DB), never through a pulled length. And never copy a pulled file back up as env: it would write empty strings over working secrets.
Env vars (values in Vercel only): `DATABASE_URL`, `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `NEXT_PUBLIC_APP_URL=https://www.periodictable.lol`, `RESEND_API_KEY`, `EMAIL_FROM`, `CLICK_SALT`, `TURNSTILE_SECRET`, `NEXT_PUBLIC_TURNSTILE_SITEKEY`, `CRON_SECRET`, `PAYMENTS_LIVE`, `NEXT_PUBLIC_PAYMENTS_LIVE`, `ADMIN_TOKEN`, `UPSTASH_REDIS_REST_URL`, `UPSTASH_REDIS_REST_TOKEN`, `NEXT_PUBLIC_PLAUSIBLE_DOMAIN` (optional). All of these exist in production as of 2026-09-11 except `PAYMENTS_LIVE`, which is deliberately absent while paused — and `NEXT_PUBLIC_PAYMENTS_LIVE`, which exists but is an empty string. See the live-state audit in §7. The two `WHOP_*` vars are still present from the frozen provider; they are unused by the code and should be deleted from Vercel once Stripe is proven.
