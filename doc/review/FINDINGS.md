# Findings register

Every finding from every phase doc, in one place. The phase doc is the work; this is the index over it. A finding's text is written in its phase doc first and mirrored here — never only here.

**IDs.** `R<phase>-<n>`, two-digit phase, e.g. `R04-3`. Ids are append-only: a fixed or rejected finding keeps its id forever, because the ledger and the commit history reference them. Decisions are `D<phase>-<n>` and live under **Operator decisions**, below the phase tables: a question put to the operator rather than a defect in the tree, so it carries no severity and no `Fixed in`.

**Severity.** P0 blocks announce (money can be lost, created or misrepresented; data can leak; legal exposure; a silent undetectable failure). P1 must fix before announce. P2 first week. P3 backlog. Full definitions in `00-REVIEW-PLAN.md` §4.

**Category.** `money` · `correctness` · `security` · `privacy` · `legal` · `ux` · `a11y` · `perf` · `ops` · `content` · `seo` · `data` · `testing`

**Status.** `open` · `fixed` · `fixed in part` · `wontfix` · `accepted-risk` — `fixed in part` means the half the tree can do is done and the cell says which half is not; the last two require a reason in the phase doc's open-questions section. A `Fixed in` cell names the files rather than a commit while a fix pass still sits in the working tree; the phase doc's change log says which pass it was.

**Rules.** No row without evidence: a `file:line` or a dated probe recorded in the phase doc's live-evidence section, and a reproduction someone else can run. Where no evidence is yet possible the row is `UNKNOWN` in the evidence column together with the exact command or dashboard step that would settle it. Secrets are never recorded here.

## P0

| ID | Phase | Category | Summary | Evidence | Status | Fixed in |
| --- | --- | --- | --- | --- | --- | --- |
| R07-1 | 07 | money | The dev simulator grants permanent stakes in a production-flagged process whenever Stripe is half-configured (key present, webhook secret absent), so an announced deployment can mint free stakes | `doc/review/07-payment-provider-integration.md` §5.3, §7 R07-1; `lib/stripe.ts:18-19,28`; `app/api/dev/pay/route.ts:12-15`; probe 2026-09-15 with `VERCEL_ENV=production`, `PAYMENTS_LIVE=true` — `POST /api/dev/pay` → 200 writing `probe_prod_free_1 · dev · paid · $5 · Fr`, and `/pay/probe_prod_free_1` → 200 serving the simulator | fixed | `lib/flags.ts` (`devSimulatorEnabled`), `app/api/dev/pay/route.ts`, `app/pay/[paymentId]/page.tsx`, `lib/phase7.test.ts` |
| R07-2 | 07 | money | A preview-flagged deployment serves the dev simulator and writes real stakes to the database it shares with production | `doc/review/07-payment-provider-integration.md` §5.2, §7 R07-2; `lib/flags.ts:12-20`; `doc/PROD-READINESS-CHECKLIST.md` l.16 (`DATABASE_URL` scoped `Production, Preview`); probe 2026-09-15 on 3207 (`VERCEL_ENV=preview`) — checkout answers `provider:"dev"` and the $11 stake on `Si` applies `paid` with no card | fixed | `lib/flags.ts` (`devSimulatorEnabled`), `app/api/checkout/route.ts` (`resumeCheckoutUrl`), `lib/phase7.test.ts` |
| R10-2 | 10 | security | An unverified manage token takes over any listing on a non-production deployment: the token is minted for whatever address the caller supplies and handed back whenever the app is not in production | `doc/review/10-email-and-notifications.md` §5.4 (chain M1-M5), §7 R10-2; `lib/manage.ts:46-61,64`; `lib/env.ts:31-33`; live 2026-09-15 — a token for `ladder-b.dev` → session cookie → `PATCH` 200 rewriting the listing's title/email/url, audit reads `owner` | fixed | `lib/manage.ts` (the requested address must equal `Startup.email` before a token is minted `:62-72`; `debugToken` only in a `development` app env `:82`; `email` out of the PATCH writable set), `lib/env.ts`; test `lib/manage.test.ts` |
| R14-1 | 14 | security | A free stake is one missing environment variable away: `getProviderMode()` returns `dev` whenever either Stripe key is absent, and `/api/dev/pay` requires only that, so an unconfigured production deployment mints real stakes for free — while the route's own docstring claims it "can never grant stakes for free in production" | `doc/review/14-security.md` §7 R14-1; `lib/stripe.ts:28`; `app/api/dev/pay/route.ts:9-16`; production answers 403 today with both keys present, and the same binary answers 200 locally as soon as one is removed (`11` §5.10); `07` §7 R07-1 registers the same fail-open from the Stripe-configuration side. **Amended 2026-09-15:** the phase 07 fix pack closes this fail-open in the working tree (`07` §5.9 — the simulator now requires *zero* Stripe variables and a `development`/`test` environment, so removing one key no longer reaches it); this row's own status is left to doc 14's pack.| fixed | `app/api/dev/pay/route.ts:45-47` (the environment gate runs **first**: `404` in every production environment, before the flag or any credential is read — the four rows of `14` §5.3's matrix all move), `lib/moneyPath.ts` (new: `checkMoneyPath()` — "intends to charge" × "can service a charge", truth table in `lib/moneyPath.test.ts`, 7 cases) and `app/api/checkout/route.ts:159,183-197` (`503` naming the missing variables, logged once per process, ahead of the paused-provider guard); `lib/phase7.test.ts` 7 → 9; §5.16, §7 R14-1 |
| R16-1 | 16 | privacy | There is no privacy policy, and the only data statement is one paragraph on the About page — while the product collects a payer email, a listing email, a waitlist email, an IP-hashed click log, report notes, and keeps mail-log and audit rows | `/legal/privacy` → **404** (`16` §5.1); `generateStaticParams` lists exactly `about\|rules\|contact` (`app/legal/[slug]/page.tsx:130`); data categories `16` §5.4 | fixed | `lib/legalDocs.ts` (the privacy document), `lib/legal.ts` (`LEGAL_SLUGS`), `app/legal/[slug]/page.tsx`, `components/FooterBar.tsx`, `lib/sitemapData.ts`, `lib/legalContent.test.ts` |

## P1

| ID | Phase | Category | Summary | Evidence | Status | Fixed in |
| --- | --- | --- | --- | --- | --- | --- |
| R06-1 | 06 | ux | An untouched or handle-only form submits nothing, silently: the early return carries no message and the only visible gate is the disabled button | `doc/review/06-checkout-before-payment.md` §5.2, §7 R06-1; `components/Modals.tsx:228` | fixed | `lib/checkoutFace.ts` (`submitBlocked`), `components/Modals.tsx` |
| R08-1 | 08 | correctness | A webhook replay overwrites the record of the delivery that applied the money, so the replay audit trail loses the only row that says the settle happened | `doc/review/08-settlement-and-ledger-integrity.md` §5.4, §7 R08-1; `lib/settle.ts:89-105`; dump 2026-09-15 — `evt2_1 · duplicate · already-paid` against `evt2_16 · applied` | fixed | `lib/settle.ts` (`providerEventUpdate`, `recordProviderEvent`), `app/api/webhooks/stripe/route.ts`, `lib/phase8.test.ts`, `lib/reconcile.test.ts` |
| R10-1 | 10 | correctness | A failed delivery is marked delivered, and the documented retry refuses it: `deliver` swallows the provider error, so the outbox stamps `completedAt` and the retry route's throw-only path is unreachable | `doc/review/10-email-and-notifications.md` §5.2, §7 R10-1; `lib/email.ts:68,70-72,123-131`; `lib/outbox.ts:130-144`; live 2026-09-15 — `POST /api/admin/outbox/retry` for the errored receipt → 409 `ALREADY_DONE`; `ops/takedown.md:36-41` documents that route | fixed | `lib/email.ts` (`deliver` reports the failure instead of swallowing it `:422-471`), `lib/outbox.ts` (`assertDelivered` `:143`, backoff `:231-248`, `failedMailHealth` `:203`), `app/api/admin/outbox/retry/route.ts` (the revival predicate `:38-56`), `app/api/jobs/config/route.ts` (`mail.failedCount`/`oldestUnretriedKey` `:77-80`); test `lib/outbox.test.ts` (13) |
| R13-1 | 13 | correctness | A mail send that failed is marked complete and can never be retried: `deliver()` returns `"error"` without throwing, the drain takes the success branch and stamps `completedAt`, and the admin retry answers `409 ALREADY_DONE` | `doc/review/13-jobs-and-cron.md` §7 R13-1; `lib/email.ts:66,68`; `lib/outbox.ts:126-134,128`; `app/api/admin/outbox/retry/route.ts:25-26`; `10` §7 R10-1 registers the same defect from the delivery-contract side. **Amended 2026-09-16:** the phase 10 fix pack closes this from the delivery side in the working tree (`10` §5.7 — `deliver` reports its failure, `lib/outbox.ts` leaves `attempts > 0` + `lastError` and retries, and the retry route refuses only what was actually delivered), so a failed send is no longer stamped complete; the drain route's own authentication defect (`R10-10`) is this doc's row.| fixed | `lib/outbox.ts:147-153` (`assertDelivered`, landed in `10`'s pack `6dcd955`), `app/api/admin/outbox/retry/route.ts:60-84` (`delivery` on both 409 arms); §5.15, §7 R13-1 |
| R14-2 | 14 | security | Every per-IP abuse control (checkout, search, waitlist, report, manage) is keyed on a caller-written `cf-connecting-ip`/`x-forwarded-for` value, so each limiter is defeated by one header and its logs are attacker-shaped | `doc/review/14-security.md` §7 R14-2; `lib/ip.ts:14-20`; §5.6 (five checkouts from one spoofed identity → `429`; five different values → five fresh windows) | fixed | `lib/ip.ts` (new, `clientIp()` at `:40-52` / `usableHop()` at `:33`: `cf-connecting-ip` only behind a `cf-ray` proof, then the rightmost usable `x-forwarded-for` hop, then `x-real-ip`, then one shared `0.0.0.0` bucket), `lib/rateStore.ts:93-140` (`StoreErrorPolicy` + `onStoreError` as a per-call argument), `onStoreError: "closed"` at `app/api/waitlist/route.ts:44,116`, `app/api/checkout/route.ts:275`, `app/api/report/route.ts:28` and `lib/jobs.ts:247`; `lib/ops.test.ts:75-111` (ladder) and `lib/routes.test.ts:283-300` (the probe fixture had to carry a `cf-ray`); §5.16, §7 R14-2 |
| R14-3 | 14 | security | `/api/waitlist` will send attacker-chosen text from the product's own domain to the operator's inbox, with no account, no cost and no reply path — an anonymous third-party mail relay that is invisible from our side | `doc/review/14-security.md` §7 R14-3; `app/api/waitlist/route.ts:20,29,60`; the same pattern in `app/api/report/route.ts` (`10` owns the chain, this owns recipient authority) | fixed in part | `app/api/waitlist/route.ts:24-25,74,84,113-116` — `WAITLIST_NEW_RECIPIENTS = 5`/24 h module-private, mail only for an address that is neither known nor suppressed (the `known` lookup), cap limiter with `onStoreError: "closed"`; `lib/intakeMail.test.ts` 11 → 12. Double opt-in deliberately not taken: a first mail to an arbitrary address is still possible, capped at five per source per day (§5.16, §7 R14-3) |
| R15-3 | 15 | ops | A database outage answers a 0-byte `500` on twelve surfaces, and with the provider unconfigured the money route instead says "Payments are paused" — neither a customer nor an operator watching the page can tell an outage from a product decision | `doc/review/15-performance-concurrency-cost.md` §5.10, §7 R15-3; `11` §5.11; `14` §5.7 | fixed | `app/s/[domain]/page.tsx` (both reads behind one `loadProfile()`; `generateMetadata` catches and answers a generic title; missing/empty/hidden → `notFound()`, a throw → a product-authored outage shell + `{"scope":"page","page":"profile",…,"reason":…}`), on top of `11` R11-3's boundary (`lib/route.ts`: all 28 routes answer `503 {"error":"Service unavailable."}` with an id, a path, a status and a duration logged); `lib/boundaries.test.ts` asserts the split, the copy and the log lines |
| R15-8 | 15 | money | Eight equal-amount winners on one element are all told "You're #1": the receipt copy is unconditional, and the displaced leaders generate only 4 `OUTBID_EMAIL` rows in total — **owned by `10` and `04`**, recorded here because this doc produced the concurrency that exposed it | `doc/review/15-performance-concurrency-cost.md` §5.9, §7 R15-8; `emails/receipt.tsx:17,25`; `lib/settle.ts:206-220,282`; `10` §7 R10-3 and `09` §7 R09-2 are the same defect from the mail and ownership sides. **Amended 2026-09-16:** both halves are closed in the working tree — the receipt copy is rank-aware (`10` §5.7: the subject and the body read the settled rank, so four of the eight winners no longer read "You're #1"), and a dethroned holder with no listing address is mailed at the funding payment's address or recorded as `OUTBID_UNNOTIFIED` (`09` §5.6), so the four-row count is explained rather than silent; this row's own status is left to doc 15.| fixed (`10`, `04`) | not fixed here: closed by the docs that own it — `emails/receipt.tsx` (`rank` prop derived from the settled `Stake`; `receiptSubject` reads `p.rank ?? 1`) per `10` §7 R10-3 and `04` §7 R04-1/R04-2, verified in the file in this pass; the displaced-leader notice is `10`'s. See `15` §5.14 |
| R15-9 | 15 | money | One unpaid checkout holds an element for `RESERVATION_TTL_MS = 15 min` and refuses every competitor: 12/12 take-quotes answered `RESERVATION_CONFLICT` while one hold lived — **owned by `09`** | `doc/review/15-performance-concurrency-cost.md` §5.9, §7 R15-9; `lib/reservations.ts:14-20,34-49`; `app/api/checkout/route.ts` (TAKE refusal); `09` §7 R09-1 owns the hold. | open (`09`) | not fixed here: the TTL is a product decision, not an implementation one, so it stays with the doc that owns the hold (`09` §7 R09-1 under `09` §9 Q2) — this pass measured the behaviour under load (§5.9) and records the cross-reference; on the visibility side the hold is now answerable from the log line the next item adds (`lib/route.ts`, R15-11) |
| R15-10 | 15 | correctness | The CDN can serve the 11,778-byte no-database fallback shell for an element page while the build's own document is 13,259 bytes, and it serves it as `x-nextjs-cache: STALE` | `doc/review/15-performance-concurrency-cost.md` §5.7, §7 R15-10; `app/elements/[sym]/page.tsx:9-14,53-80` | fixed in part | `app/elements/[sym]/page.tsx` — `if (!el) notFound()` stops an unknown symbol being answered with a real element's name (and stops a stale copy repeating it), the `catch` became `catch (err)` logging `{"scope":"page","page":"element","symbol":…,"reason":…}` and rendering a shell that says it is one, and the `revalidate = 60` rationale is a comment rather than tribal knowledge. **Not** done: no explicit `s-maxage` on this route (the window policy is §9 Q4, owned by `12`), and whether a stale document for a *valid* symbol should ever be served is the same question (§5.14) |
| R15-11 | 15 | ops | Nothing in the product measures its own spend, latency or 5xx rate: no counter, no route timing, no request id on the outage path, so every figure in §5.12 and every degradation in §5.10 stays invisible until an invoice or a customer | `doc/review/15-performance-concurrency-cost.md` §5.12, §7 R15-11 (`18` owns the telemetry gap; `13` §7 R13-3 owns the alerting half) | fixed | (1) `lib/route.ts` — `withContract` logs one line per **origin** invocation on both the success and the error path (`{"scope":"api","id":…,"method":…,"path":…,"status":…,"ms":…}`, path only because query strings carry `?token=`/`?me=`), so the 503s are measured too; (2) `app/api/jobs/config/route.ts` — a best-effort `cost` block (`settlements` day/week, `outboxDue`, `analyticsRows`) on the surface an operator already polls without a database-independent dependency; `lib/ops.test.ts` asserts it. **Not** done: platform invocations and ISR writes (the host's to count, `U15-8`) and the alerting rules themselves, which stay with `13` §7 R13-3 / `18` (§5.14) |
| R15-12 | 15 | money | A rejecting provider leaves orphaned `Payment` rows (`status = pending`, `providerRef = NULL`, no `ProviderEvent`) that both pollers miss because both are driven by `ProviderEvent` — **owned by `07`/`08`** | `doc/review/15-performance-concurrency-cost.md` §5.8, §7 R15-12; `13` §5.7 (poller inputs); `06` §7 R06-7 owns the buyer-side half, `08` §7 R08-5 the unreachable `CANCELED` status.| fixed (`06`, `08`, `12`) | not fixed here: closed by the docs that own it — the buyer-side half is `06` §7 R06-7, the settlement-state half `08` §7 R08-5, the polling half `12` §7 R12-5 — and verified in the working tree by this pass; §6's after-fix table records it as unchanged by this pack. See `15` §5.14 |
| R16-2 | 16 | privacy | The "Data & cookies" paragraph is wrong in both directions: it promises the city/country the payment provider associates with the transaction (never read, only seeded) and omits every email/click/report/log field and six of the eight processors | `app/legal/[slug]/page.tsx:45`; `automatic_tax\|billing_address_collection\|billing_details\|address_city\|address_country` → 0 hits (`16` §5.2); `prisma/seed.ts:103`, `prisma/launch-seed.ts:113`; `16` §5.4-5.5 | fixed | `lib/legalDocs.ts` (About → "Data, cookies and third parties"), `lib/legalContent.test.ts` (`about` digest) |
| R16-3 | 16 | privacy | Pages call Google (favicons, on every staked listing) and can call Microlink, Turnstile and Plausible from the visitor's browser with no disclosure and no consent mechanism; production today ships no analytics script, no cookie and no consent string, so this is the design, not current traffic | `lib/startups.ts:43`; `lib/screenshots.ts:10-11,17-36`; `app/layout.tsx:29-31`; `next.config.mjs:7,14-16`; `16` §5.1 (0 `plausible.io`, 0 `Set-Cookie`) | fixed | `lib/screenshots.ts` (`upstreamFaviconUrl`, `faviconFor`), `app/api/favicon/route.ts`, `components/Avatar.tsx`, `next.config.mjs` (`img-src 'self' data:`), `lib/legalDocs.ts` (the analytics disclosure), `lib/readCache.test.ts`, `lib/routes.test.ts` |
| R16-4 | 16 | legal | Nothing in the product decides, collects, displays or records tax: no rate, no tax line on the receipt, no tax column on `Payment`, no `automatic_tax`, no invoice — the only tax-adjacent string is a Stripe `tax_code` | `lib/stripe.ts:99`; `emails/receipt.tsx`; `prisma/schema.prisma` (`Payment`); `16` §5.2, §5.6 | fixed in part | `lib/legalDocs.ts` (`TAX_POSITION_SENTENCE`, the refunds paragraph), `components/Modals.tsx` (checkout), `emails/receipt.tsx`, `lib/receiptLegal.test.ts` — the registration facts are D2/U16-1 |
| R16-5 | 16 | legal | The statement descriptor is unset, unpublished and unverifiable, so the "contact us before disputing" promise presupposes a charge the buyer cannot recognise; the operator side has no payer lookup either | `statement_descriptor` → 0 hits in `lib/stripe.ts` (`16` §5.2); `app/legal/[slug]/page.tsx:89,116`; `doc/PROD-READINESS-CHECKLIST.md:42,181` | fixed in part | `lib/operator.ts` (`descriptorIsValid` bounds, `operatorAdvisories`), `lib/legalDocs.ts` (refunds), `emails/receipt.tsx`, `lib/operator.test.ts` — the value is D7/U16-1, the lookup is `17`'s |
| R16-6 | 16 | legal | The 18+ attestation and the no-refund acknowledgment are a checkbox the API validates as a boolean, and nothing records that the attestation was made — no text, no version, no timestamp on `Payment` | `components/Modals.tsx:470-473,485`; `app/api/checkout/route.ts:130-131`; `lib/abuse.ts:50-51`; `16` §3.3 | fixed | `prisma/schema.prisma` + `prisma/migrations/0012_payment_consent`, `lib/consent.ts`, `app/api/checkout/route.ts`, `lib/routes.test.ts` |
| R16-7 | 16 | legal | Terms are accepted by a sentence rather than an act, and the version cannot be identified: one hard-coded "Last updated: September 2026" string serves three documents, no version id exists, and no purchase records which text applied | `components/Modals.tsx:490`; `app/legal/[slug]/page.tsx:12,54,104`; `16` §5.6 | fixed | `lib/legal.ts` (`LEGAL_REVISIONS`, `LEGAL_REVISION_LOG`), `lib/legalDocs.ts`, `app/api/checkout/route.ts` (`409 RULES_UPDATED`), `lib/legalContent.test.ts` (the digest tripwire) |
| R16-8 | 16 | legal | The takedown path is not DMCA-shaped: no designated agent, no postal address, no counter-notice procedure, no repeat-infringer statement, no perjury clause — on a site that sells link placements | `app/legal/[slug]/page.tsx:107-110,123`; `16` §5.1 probe of `/legal/contact` | fixed in part | `lib/legalDocs.ts` (contact → abuse and takedowns) — the agent and the servable address are D7, execution is `17`'s |
| R16-9 | 16 | legal | The launch seeder creates `Stake` and `ActivityLog` rows for real companies with no `Payment` behind them — ranked and published exactly like paid listings — while the About page states every link was submitted by whoever paid for it | `prisma/launch-seed.ts:1-10,101-115`; `:25`; `lib/pricing.ts`; `16` §7 R16-9 | fixed in part | `lib/demoLabels.ts` (`isDemoListing` direction), `app/api/board/route.ts` (`.map(mark)` after ranking), `app/api/table-order/route.ts`, `app/api/activity/route.ts`, `app/api/elements/route.ts`, `components/Tile.tsx`, `components/ActivityCard.tsx`, `components/Modals.tsx`, `lib/demoData.test.ts`, `lib/routes.test.ts`, `lib/legalDocs.ts` — the seed policy is D5/`19` |
| R17-1 | 17 | ops | There is no runbook for a payment that took money without settling, and the check that would find it (`/api/jobs/reconcile`) is on no schedule — its only cadence is a GitHub Action measured at 46 runs against 720 nominal | `vercel.json:3-6` (outbox and screenshot only, neither reconcile); `.github/workflows/outbox-tick.yml:88-97`; `17` §5.2 | fixed in part | `ops/payments-stuck.md`; `ops/README.md`; the reconcile gap read in `app/api/jobs/reconcile/route.ts` — the schedule and the human it would page are `18`'s, and the on-call decision is `D17-1` |
| R17-2 | 17 | ops | A webhook failure or a stale signing secret has no runbook, no overlap window and no way to count the deliveries that were rejected, because the 401 precedes any persistence | `lib/env.ts:42`; `app/api/webhooks/stripe/route.ts:40-42`; `ops/rollback.md:22-27`; `17` §5.8 | fixed in part | `ops/webhooks.md`; `lib/stripe.ts` (`webhookSecrets()`, `STRIPE_WEBHOOK_SECRET_OLD` overlapping the same 300 s tolerance); `lib/env.ts` (rotation-overhang advisory); `lib/webhook.test.ts`; `.env.example` |
| R17-3 | 17 | ops | Refund, chargeback and dispute-evidence runbooks do not exist; the entire operator literature for reversals is one line | `ops/rollback.md:54` ("refund via provider dashboard (Stripe)"); `17` §3.7 | fixed in part | `ops/refunds-and-disputes.md` — dispute clock, refund timing, evidence pack, ledger lookups; who may approve is `D17-5` and the window is `D16-3` |
| R17-4 | 17 | ops | No secret has a rotation procedure, and two traps are unwritten: rotating `CLICK_SALT` severs IP-hash continuity mid-incident, and `CRON_SECRET` lives in two consoles so rotating one half turns the tick red | `lib/clicks.ts:14`; `.github/workflows/outbox-tick.yml:31-36`; `17` §5.8 | fixed in part | `ops/secrets.md` (per-secret procedures, the `CLICK_SALT` hash-continuity trap, the two-console `CRON_SECRET`); `ops/README.md` |
| R17-5 | 17 | ops | `launch-seed --fresh` unconditionally deletes twelve tables — payments, provider events, audit log, stakes, reports, clicks — against any database it is pointed at; its only guard is a comment | `prisma/launch-seed.ts:16,63-86`; `confirm\|readline\|NODE_ENV\|VERCEL_ENV` → 0 hits; `ops/rollback.md:40` puts the same script in the normal seed step | fixed | `lib/seedGuard.ts`; `lib/seedGuard.test.ts`; `prisma/launch-seed.ts:96-135`; `prisma/seed.ts` — who may authorise a destructive run is `D17-2` |
| R17-6 | 17 | ops | One shared bearer token covers moderation and mail re-send, attributes every action to a self-declared string, and cannot be revoked per person | `lib/jobs.ts:31-41`; `app/api/admin/reports/[id]/route.ts:31`; `app/api/admin/startups/moderate/route.ts:35`; `ops/takedown.md:17,28,32,33`; `17` §5.9 | fixed in part | `lib/jobs.ts` (`AdminAuth`, `ADMIN_TOKENS`, `operatorTokens()`, `operatorIdentity()`); `app/api/admin/outbox/retry/route.ts`; `lib/ops.test.ts`, `lib/moderation.test.ts`, `lib/outbox.test.ts` — custody and revocation are `D17-3` |
| R17-7 | 17 | ux | A paying customer whose listing needs changing has no path: the manage-session client exists nowhere in `app/`, `components/` or `emails/`, and the server half answers JSON with no page to land on | `lib/manage.ts:10-11`; `app/api/startups/[domain]/route.ts:16-66`; `app/api/manage/verify/route.ts:27-28`; `doc/PROD-READINESS-CHECKLIST.md:42,181` | fixed in part | `ops/takedown.md` ("A buyer asks for a profile change", four steps); `ops/README.md`; the R14-9 comment in `app/api/admin/outbox/retry/route.ts` — the client half stays `09` §7 S10 L1 |
| R18-1 | 18 | ops | No error tracking: a 03:00 crash leaves nothing durable to read, and no SDK, sink or server boundary exists — the only two handlers are client-side | nine-vendor grep → 1 false positive (`lib/searchCombobox.test.ts:9`); `app/error.tsx:20`; `app/global-error.tsx:16`; `18` §5.1-5.2, §5.8 | fixed | `lib/errorReport.ts`; `lib/errorLimits.ts`; `app/api/internal/error/route.ts`; `instrumentation.ts`; `lib/route.ts`; `components/ClientErrorReporter.tsx`; `lib/clientError.ts`; `prisma/migrations/0013_error_report`; `lib/errorReport.test.ts`, `lib/clientError.test.ts` — the vendor and the plan are `D18-1` |
| R18-2 | 18 | money | Webhook verification failures and terminal provider events are invisible: a bad signature returns 401 before any write, the retryable 500 leaves one expiring line, and `ProviderEvent` has no production reader | `app/api/webhooks/stripe/route.ts:40-42,126-169,190-193`; `lib/settle.ts:298`; `18` §5.2 (22 `providerEvent.` rows, 2 production reads) | fixed in part | `app/api/webhooks/stripe/route.ts` (throttled `webhook-bad-signature` line, `recordTerminal()`); `lib/webhook.test.ts` — the log half is fixed and tested; the `ProviderEvent` row for a refused delivery is deliberately not written (`18` §8 box 2, unticked) |
| R18-3 | 18 | money | Mail failures are unlogged and undiagnosable: `lib/email.ts` has no `console.*` at all, `deliver()` discards the provider's reason, and every `EmailLog.detail` is the subject line | `lib/email.ts:66,69,106,138,164,178`; `18` §5.1-5.2 (4 `emailLog.` rows, 1 production write) | fixed | `lib/email.ts` (`failureDetail()`, one `send-failed` line per failure); `lib/emailFailure.test.ts` (non-2xx and timeout against a stubbed provider); `lib/opsMetrics.ts` (`mail` block) |
| R18-4 | 18 | money | No surface distinguishes live from test mode, and the config report cannot see advisories: a `sk_test_` key in production passes every check | `lib/stripe.ts:18,28,33`; `app/api/jobs/config/route.ts:7-40`; `lib/env.ts:84-87,128-144`; `18` §5.3 (one `degraded` finding, `ok:true`) | fixed | `lib/stripe.ts` (`stripeKeyMode()`); `app/api/jobs/config/route.ts` (the `required` test-key finding, `providerMode`, `stripeKeyMode`); `lib/ops.test.ts` |
| R18-5 | 18 | ops | The audit trail and refund/reversal history have no reader, and there is no operational history view: nine actions are written and zero production code reads them | `lib/audit.ts:17-26,41`; `18` §5.2 (`auditLog.` = 12 rows in 8 files, 1 production write, 10 test rows); `17` §3.1 (four admin routes, none listing) | fixed | `app/api/admin/audit/route.ts` (`?action=`, `?startup=`, `?payment=`, `?before=`); `lib/audit.ts` (`AUDIT_ACTIONS`, `isAuditAction`); `ops/takedown.md`; `ops/alerts.md` |
| R18-6 | 18 | ops | A database outage is indistinguishable from a broken route: no health surface, no reachability signal, and the per-attempt retry warning is the only trace | `lib/txn.ts:47-52`; no `/api/health` in the tree (`18` §5.2) | fixed | `lib/health.ts`; `app/api/health/route.ts` (200/503, `db`, `ms`, `no-store`); `lib/health.test.ts`, `lib/healthRoute.test.ts` — the monitor that polls it is `D18-5`, its existence `U18-3` |
| R18-7 | 18 | money | No surface answers "how many stakes were actually paid for": `/api/stats` and `reconcile` return opposite-looking numbers by construction | `18` §5.4 live probes (`stakeCount:0,totalStakedUsd:0` vs `paidTotal:3`); `app/api/stats/route.ts:11-21` | fixed | `lib/opsMetrics.ts` (`money`, one definition); `app/api/admin/ops/route.ts`; `lib/api.ts`, `app/api/stats/route.ts`, `components/StatsCard.tsx` (`moneyScope: "board"`) |
| R18-8 | 18 | ops | Outbox depth and ageing are unreadable anywhere: no aggregate query exists, and `{"claimed":0}` is ambiguous between "empty" and "nothing due" | `outboxEvent.(count\|aggregate\|groupBy)` → 1 test row (`lib/settle.test.ts:255`); `app/api/jobs/outbox/route.ts`; `18` §5.2-5.3 | fixed | `lib/outbox.ts` (`OutboxHealth`); `app/api/jobs/outbox/route.ts`; `app/api/jobs/reconcile/route.ts`; `lib/opsMetrics.ts` (`outbox`, `alarmsFor()`); `lib/opsMetrics.test.ts` |
| R18-9 | 18 | ops | Every failure class in `18` §6 has no owner and no alarm; the only automated escalation is a red GitHub Action at 6.4 % of its nominal cadence | `18` §5.3 (46 runs vs 720 nominal), §3.10; `.github/workflows/outbox-tick.yml:31-97` | fixed in part | `ops/alerts.md` (URL list with expected statuses, six-class threshold table, alarm codes, stale-tick bounds); `lib/opsMetrics.ts` (`alarmsFor()`) — the threshold half only: channel and recipient are `D18-2`, the monitor `D18-5`/`U18-3`, on-call `D18-6` |
| R19-1 | 19 | ops | The launch is unmeasurable: no Plausible script reaches the served page, so pageviews and all seven funnel events are silent no-ops | `doc/review/19-launch-and-marketing-readiness.md` §5.6 (0 `plausible` hits in the 152029-byte home document, three probe sets, 2026-09-15), §7 R19-1; `app/layout.tsx:25,29-31`; `lib/analytics.ts:17-28`; `doc/PROD-READINESS-CHECKLIST.md:437` | open (operator) | not fixable in the tree — the switch is the Vercel Production variable `NEXT_PUBLIC_PLAUSIBLE_DOMAIN` (`U19-3`). The pass made the launch-morning check honest instead: the script is consent-gated (`lib/analyticsConsent.ts`, `R18-14`) and the money funnel is server-side (`/api/admin/ops` → `funnel`, from `ClickEvent` and `Payment` rows), so a silent browser costs pageviews and seven event names, not the funnel |
| R19-2 | 19 | content | The announce's central claim is undecided and unrecorded: seeded provenance or an empty board — production is empty while the launch set's goal line says a table that "already looks alive" | §5.1, §5.5, §7 R19-2; `doc/phase-5-launch/README.md:4`; `doc/phase-5-launch/01-seed-instrument.md:6-9,17`; `doc/PROD-READINESS-CHECKLIST.md:272,296`; decision due in §9 Q1 | fixed in part | `D19-1` (seed before announcing) plus the three documents that disagreed — `doc/phase-5-launch/README.md:4`, `01-seed-instrument.md` (which half the repository implements) and `doc/review/00-REVIEW-PLAN.md:152` (the stale pointer, now pointing at the review that asks the question). Running `prisma/launch-seed.ts` is the operator's T-30 m step, with `db:clear-demo-data --apply` as the abort path, and the seeded rows stay labelled `demo` (`R16-9`) |
| R20-15 | 20 | legal | What happens to holders' money if the site is shut down has no answer anywhere — no wind-down clause in the legal pages or any runbook, while `/legal/rules` promises stakes are final and permanent | `doc/review/20-post-launch-and-debt.md` §5.8, §7 R20-15; `app/legal/[slug]/page.tsx:88,96`; `ops/takedown.md` (headings end at Comms `:48`); decision due in §9 Q2, and the one row that changes `19` §8 (CR-20.4) | fixed (`D20-3`; ticks CR-20.4) | `SERVICE_TERM` (`2027-09-09`, extendable with the new date published, 30 days' notice with checkout off from the notice day, stakes not refunded on wind-down and never expiring) in `lib/legal.ts`, rendered in the rules' new section and FAQ item 10 (`lib/legalDocs.ts`, `lib/faqDocs.ts`) and beside the price on both element-page branches, with a wind-down notice template in `ops/comms.md` and `19` §8's CR-20.4 box ticked |

## P2

| ID | Phase | Category | Summary | Evidence | Status | Fixed in |
| --- | --- | --- | --- | --- | --- | --- |
| R01-1 | 01 | seo | The home page ships no social metadata at all (0 `og:*`, 0 `twitter:*`, no canonical, no JSON-LD), so every share of the product's own URL renders a text-only link with no card image | `doc/review/01-discovery-and-unfurl.md` §5.1 (0 og/twitter/canonical in the served 149431-byte document); `app/layout.tsx:17-22` | fixed | `lib/shareMeta.ts` (layout metadata); `app/og/home/route.tsx` |
| R01-2 | 01 | seo | The share image is served as `image/svg+xml`, which the platforms that render cards do not accept, so element unfurls have no image | `app/og/[sym]/route.tsx:6,19`; §5.8 (`Content-Type: image/svg+xml`); §5.5 (platform card-image support) | fixed | `app/og/[sym]/route.tsx`, `lib/ogCardImage.tsx`, `lib/ogCard.ts` |
| R01-3 | 01 | seo | No element page has an inbound link from anywhere in the product; the board's tiles are `<button>`s, so all 122 element pages are reachable only via the sitemap | `components/Tile.tsx:34-41`; §5.1 href census (0 occurrences of `/elements/` in the home document) | fixed | `components/Tile.tsx`, `components/PeriodicGrid.tsx` |
| R02-1 | 02 | ux | `notFound()` off a bad legal slug returns a blank page: 404 status, `<html id="__next_error__">`, no visible element at all | §5.2 (probe of `/legal/nope`); `app/legal/[slug]/page.tsx:129-130`; §5.7 (no `app/not-found.tsx`) | fixed | `app/not-found.tsx`, `lib/boundaryChrome.tsx`, `lib/boundaries.ts` |
| R02-2 | 02 | ux | A route matching nothing falls through to Next's default 404 — no wordmark, no nav, no way back | §5.3 (`/zzz-not-a-page` and `/legal` → 404/7335 B, stock `<title>404: This page could not be found.</title>`) | fixed | `app/not-found.tsx` |
| R02-3 | 02 | ux | No error boundary anywhere: `error.tsx`, `global-error.tsx` and `loading.tsx` are all absent, so a client render throw or an unguarded server read reaches Next's default surface with no brand and no recovery | §5.7 (absence audit under `app/`) | fixed | `app/error.tsx`, `app/global-error.tsx`, `lib/boundaryChrome.tsx`, `lib/boundaries.ts` |
| R02-4 | 02 | content | The served document advertises 122 elements at `$5 · unclaimed` before hydration — the claim `lib/liveState.ts` exists to prevent | §5.6 (149431 B document, `unclaimed` ×123, tile titles); `app/page.tsx:216-233,197-203`; `lib/liveState.ts:24-27`; §5.10 (`unclaimed` ×1, `$5` ×1 after) | fixed | `lib/tileFace.ts`, `components/Tile.tsx`, `components/PeriodicGrid.tsx`, `app/page.tsx` |
| R02-5 | 02 | ux | An empty `/api/activity` feed renders `loading…` pulsing forever above `0 recent` | §5.5 (`[]`, 200, 2 B); `components/ActivityCard.tsx:36-38,70-74`; §5.10 (empty state live) | fixed | `lib/activityFace.ts`, `components/ActivityCard.tsx`, `app/page.tsx` |
| R04-1 | 04 | money | A reclaim link's pre-filled amount is set once from the quote and never reconciled with the board, so after a rival bid the older amount is charged as an ordinary top-up that retakes nothing while the modal prints the live delta beside it | `doc/review/04-element-detail-and-pricing.md` §5.8, R04-1; `app/page.tsx:116-123,127-130`; `components/Modals.tsx:154-170,378`; `app/elements/[sym]/page.tsx:68-71` | fixed | `lib/stakeQuote.ts` (+ `lib/stakeQuote.test.ts`), `app/page.tsx`, `components/Modals.tsx`, `lib/detailFace.test.ts` |
| R05-1 | 05 | a11y | Error text misses the project's own contrast budget: ten `text-red-500` sites and two `ink/60` labels measure 3.49–3.95:1 against the 4.5:1 the token table documents | `doc/review/05-accessibility-and-content.md` §5.1, §5.3, §5.4 (recomputes `ink/60` at 4.14:1), R05-1; `components/Modals.tsx:284,331-372,421-422`; `components/TerritoryView.tsx:81`; `components/WorldOrder.tsx:36`; `tailwind.config.ts:17-48` | fixed | `components/Modals.tsx` (red-700 at the seven rendered sites), `components/TerritoryView.tsx` + `components/WorldOrder.tsx` (`ink/70`), `lib/a11y.test.ts` |
| R05-2 | 05 | a11y | The only focus indicator is a 1.43:1 `cta` ring and the search field has none, against 1.4.11's 3:1 | §5.1, §5.4, R05-2; `components/IcyInput.tsx:11`; `components/SearchPill.tsx:117`; `components/IconBtn.tsx:23`; `components/PeriodicGrid.tsx:258`; `tailwind.config.ts:30` | fixed | `components/IcyInput.tsx`, `components/SearchPill.tsx`, `components/IconBtn.tsx` (`outline-ink`), `components/Modal.tsx`, `components/Tile.tsx` (`outline-white`), `lib/a11y.test.ts` |
| R06-2 | 06 | money | The retry of a payment the buyer already owns is quota-gated: the rate limiter runs before the idempotency-key lookup, so a replay of a paid key can be answered 429 | `doc/review/06-checkout-before-payment.md` §5.5, §7 R06-2; `app/api/checkout/route.ts:116-119` precedes `:175-176`; probe — after 5 attempts from one IP, the replay of a **paid** key → 429 `Too many checkout attempts. Try again later.` | fixed | `app/api/checkout/route.ts` (lookup before the limiter), `lib/routes.test.ts` |
| R06-7 | 06 | money | A provider failure leaves a `Payment` row the buyer cannot reach: the row is written before the provider call and the minted key lives only in that request | `doc/review/06-checkout-before-payment.md` §5.6, §7 R06-7; `app/api/checkout/route.ts:287-300,363-366`; probe 2026-09-15 — 502 `Payment provider unavailable. Try again.` with the row already written and no `paymentId` in the body | fixed | `app/api/checkout/route.ts` (502 carries `paymentId`), `components/Modals.tsx` (attempt key) |
| R06-8 | 06 | money | The Stripe cancel URL is written and read by nothing, so a buyer who cancels is dropped back on `/` with a parameter no code looks at | `doc/review/06-checkout-before-payment.md` §5.8, §7 R06-8; `app/api/checkout/route.ts:66`; `app/page.tsx:100-161`; probe — no reader in `app/`, `components/` or `lib/` | fixed | `app/page.tsx` (`?canceled` branch), `components/Modals.tsx` |
| R07-3 | 07 | ops | Nothing validates the production environment: `requireProdEnv()` is defined and called from nowhere, and the required list omits `ADMIN_TOKEN` | `doc/review/07-payment-provider-integration.md` §5.6, §7 R07-3; `lib/env.ts:164,39`; `grep -rn requireProdEnv` → its own definition, its test and a comment | fixed | `lib/env.ts` (`ADMIN_TOKEN` in `REQUIRED_PROD_ENV`, `reportProdEnvAtStartup`), `lib/prisma.ts`, `lib/env.test.ts` |
| R07-4 | 07 | money | A syntactically present but invalid key passes configuration and 502s every buyer, and nothing alerts on it | `doc/review/07-payment-provider-integration.md` §5.4, §7 R07-4; `lib/stripe.ts:130-141,18-19`; `lib/flags.ts:12-14`; live 2026-09-15 — `stripe: checkout/sessions rejected (HTTP 401, key=sk_test)` and every checkout 502 | fixed | `lib/stripe.ts` (`probeStripeKey`, `redactSecrets`, `logCheckoutRejection`), `lib/env.ts` (`credentialFinding`), `app/api/jobs/config/route.ts`, `lib/stripe.test.ts`, `lib/ops.test.ts` |
| R08-2 | 08 | money | A reversed payment sends the buyer nothing: the unwind writes the ledger rows and enqueues no outbox row | `doc/review/08-settlement-and-ledger-integrity.md` §5.7, §7 R08-2; `lib/settle.ts:317-417`, comment at `:389-392` ("Deliberately no buyer email") | fixed | `lib/settle.ts` (enqueue inside `MONEY_TX`, drain after), `emails/refund.tsx`, `lib/email.ts` (`sendRefundEmail`), `lib/outbox.ts` (`REFUND_EMAIL`), `lib/phase8.test.ts` |
| R08-3 | 08 | ops | Reconcile is not scheduled and nothing alerts on a terminal rejection: `vercel.json` runs only the outbox and screenshot jobs, and `/api/jobs/reconcile` has no caller | `doc/review/08-settlement-and-ledger-integrity.md` §5.8, §7 R08-3; `vercel.json` crons `0 4 * * *` and `30 4 * * *`; `app/api/jobs/reconcile/route.ts` | fixed | `app/api/jobs/reconcile/route.ts` (`unapplied`, `stale`, 503), `.github/workflows/outbox-tick.yml` (body checks, `::error::`), `lib/reconcile.test.ts`, `lib/phase8.test.ts`; the finding's "nothing polls reconcile" premise was wrong — `outbox-tick.yml` has polled it since `7ce73a9` |
| R08-4 | 08 | correctness | The failure-cleanup helper exists and is wired to nothing; `lib/idempotency.ts` and `lib/reconcile.ts` are the subject of tests and were never written | `doc/review/08-settlement-and-ledger-integrity.md` §5.9, §7 R08-4; `lib/applyPayment.ts:24-29`; `lib/idempotency.test.ts`; `lib/reconcile.test.ts` | fixed | `lib/applyPayment.ts` deleted; the sweep arm landed instead of `markFailed` on the 502 (`lib/abandonedCheckouts.ts`, `app/api/jobs/abandoned-checkouts/route.ts`); `doc/review/00-REVIEW-PLAN.md` L3 and §2 corrected; both test files kept, their modules exist |
| R09-1 | 09 | money | A live take hold can close an element to every bidder, and the rejection it produces recommends the one amount the hold refuses | `doc/review/09-ownership-and-competition.md` §5.3, §7 R09-1; `lib/reservations.ts:38-52`; `app/api/checkout/route.ts:248-267`; `lib/pricing.ts:28-35` | fixed | `app/api/checkout/route.ts` (the conflict decided before classification; `joinHint`/`joinBlocked`), `lib/stakeQuote.ts` (`joinSpace`, `heldByOther*`), `lib/reservations.ts`, `lib/checkoutFace.ts`, `lib/api.ts`, `components/Modals.tsx`; tests `lib/routes.test.ts`, `lib/checkoutFace.test.ts`, `lib/stakeQuote.test.ts` |
| R09-2 | 09 | money | `guaranteedTake: true` expires silently: a take that settles after its hold lapses applies at its stale amount, lands behind a newer leader, and its receipt still says #1 | `doc/review/09-ownership-and-competition.md` §5.6 (run W on `Er`), §7 R09-2; `lib/settle.ts:158-174`; the $6 take settled at rank 2 | fixed | `lib/settle.ts` (`lapsedTakeTotal`, `TAKE_LAPSED` with `take-lapsed:<rank>`, receipt payload), `lib/audit.ts`, `lib/outbox.ts`, `lib/email.ts`, `emails/receipt.tsx` (rank-aware subject + lapse paragraph); tests `lib/settle.test.ts` |
| R09-3 | 09 | correctness | The tie rule is a pre-commit check, not an invariant: two simultaneous claims at the same amount both settle and the second buyer pays the tie price for rank 2 | `doc/review/09-ownership-and-competition.md` §5.7 (run V on `Ho`), §7 R09-3; `lib/pricing.ts:136-138,166-192` | fixed | `lib/pricing.ts` (tie copy reworded), `lib/pricing.test.ts`, `lib/ledger.test.ts` (two equal claims, forced identical `createdAt`, id tie-break), `lib/stakeQuote.test.ts` |
| R10-3 | 10 | content | The receipt subject says "#1" to buyers who are not #1: the subject is built without the rank while the body prints the true one | `doc/review/10-email-and-notifications.md` §5.3, §7 R10-3; `emails/receipt.tsx:16-18,27`; `lib/email.ts:114` against `:121`; live — `e2@d9-e.dev` and `v2@d9-v.dev` settled at rank 2 | fixed | `lib/email.ts` (`receiptSubject` derived from the settled rank, omitted when there is none), `emails/receipt.tsx` (subject and body read the same figure); tests `lib/listingMail.test.ts`, `lib/settle.test.ts` |
| R10-4 | 10 | content | A reclaim receipt reports the top-up as the stake, so the mail under-reports the holding it bought | `doc/review/10-email-and-notifications.md` §5.3, §7 R10-4; `lib/settle.ts:218`; `emails/receipt.tsx:27`; DB read 2026-09-15 — `z@d9-z.dev · Rn · paid $2 · stake total $7 · path reclaim` | fixed | `lib/settle.ts` (the receipt payload carries `topUpUsd` only when it differs from the stake `:315-321`), `emails/receipt.tsx` (the top-up sentence `:44-46`); test `lib/settle.test.ts` (the `T5 = 9985` tile) |
| R10-5 | 10 | ops | No suppression list: unsubscribe is irreversible, partial and invisible, and the mailer's own header claims a suppression the code does not implement | `doc/review/10-email-and-notifications.md` §5.6, §7 R10-5; `lib/email.ts:4`; `app/api/unsubscribe/route.ts:31,50,66`; `lib/settle.ts:206-212,222-223` | fixed | `lib/email.ts` (`SuppressionReason`, `UNDELIVERABLE_REASONS`, `suppressEmail`/`unsuppressEmail`, `refuses` — the mailer's header claim is now implemented, and a later consent reason cannot undo an undeliverable one), `lib/outbox.ts` (the refusal branch records the attempt), `app/api/unsubscribe/route.ts` (`?resubscribe=1` `:93`), `prisma/schema.prisma` (`EmailAddress.reason`/`unsubscribedAt`/`tokenHash`); test `lib/suppression.test.ts` (15) |
| R10-6 | 10 | ops | The waitlist confirmation has no opt-out — no token, so no `List-Unsubscribe` — and its hour-bucketed key mails the same address again an hour later | `doc/review/10-email-and-notifications.md` §5.6, §7 R10-6; `lib/email.ts:170,47-54`; `app/api/unsubscribe/route.ts:33-38`; `app/api/waitlist/route.ts:61` | fixed | `lib/email.ts` (`addressToken`/`unsubUrlFor`; the confirmation mails the address's own token and carries `List-Unsubscribe`), `emails/waitlist.tsx` (a visible opt-out line), `app/api/waitlist/route.ts` (a suppressed address's re-join is stored and tracked but not mailed `:45-52`); tests `lib/unsubscribe.test.ts` (8), `lib/suppression.test.ts` |
| R10-7 | 10 | ops | A bounce or complaint has nowhere to land: no provider webhook, `deliver` reads only `res.ok`, and `EmailLog` stores no provider message id | `doc/review/10-email-and-notifications.md` §5.2, §7 R10-7; `lib/email.ts:46,68`; `prisma/schema.prisma:365-376` | fixed | `app/api/webhooks/resend/route.ts` (new: rate limit, Svix signature verify, `bounce`/`complaint` → suppression + audit, unknown types answered 200), `lib/email.ts` (`RESEND_EVENT_TYPES`/`resendEventEffect`, `WebhookVerify`), `prisma/schema.prisma` (`EmailLog.providerMessageId`/`providerStatus`, `EmailAddress.reason`), `prisma/migrations/0008_email_notifications/`; test `lib/suppression.test.ts` |
| R11-4 | 11 | security | The operator, job and money routes have no limiter at all — `/api/admin/*` (4 routes), `/api/jobs/*` (4), `/api/dev/pay`, `/api/unsubscribe`, `/api/emails/preview`, `/api/webhooks/stripe` and the five read routes; 50 consecutive admin retries all answer 200 | `doc/review/11-api-contracts.md` §5.2, §5.6, §7 R11-4 (`14` owns exploitability) | fixed | `lib/jobs.ts` (`adminGate` — credential first, then 60/min `:111`; `jobGate` — 30/min `:118`; the limits `:68-71`; the salted per-credential key `callerKey` `:81`; fail-open `throttle` `:91`), all 9 admin/job route files call a gate instead of the raw helper, `app/api/dev/pay/route.ts` (per-IP limiter `:45-46`, `400 BAD_JSON` `:55`); tests `lib/ops.test.ts` (4), `lib/contracts.test.ts` (the wiring case). The unauthorised half stays open where it was filed: `14` R14-1, `13` R13-2 |
| R12-1 | 12 | data | No constraint backs any numeric promise in the schema: `pg_constraint` holds 0 check constraints, so a negative `amountUsd`, a negative pool or an overstated `stakeCount` is refused only by JS on one code path | `doc/review/12-data-layer.md` §5.3, §7 R12-1; `prisma/schema.prisma` (bare `integer NOT NULL`); `lib/settle.ts` (the one JS throw) | fixed | `prisma/migrations/0009_data_invariants/migration.sql` — six hand-written CHECKs (`Stake_amountUsd_nonnegative`, `Payment_amountUsd_nonnegative`, `Payment_providerAmount_nonnegative`, `Payment_refundedAt_matches_status` as a biconditional, `Element_totalPoolUsd_nonnegative`, `Element_stakeCount_nonnegative`), with the pre-flight queries and the deploy-risk note in the header and the backing constraint named above each column in `schema.prisma`; guard `lib/schema.test.ts` (3 cases, asserts the six and re-runs a violation); the trio *sum* is deliberately left to R12-2 (§5.15) |
| R12-4 | 12 | privacy | Every PII column can be found and none can be deleted: no route, script or admin surface deletes or exports a customer row — the only `deleteMany` calls exist to clear demo data and refuse any row that has acquired a customer signal | `doc/review/12-data-layer.md` §5.11, §7 R12-4; `scripts/clear-demo-data.ts:172-200`; `lib/demoData.ts:33-47` (`16` owns the retention policy) | fixed | `lib/erasure.ts` (12 scopes, dry run first, `confirm: true` required by the type, one `SUBJECT_ERASED` audit row with counts and no address), `scripts/erase-subject.ts` + `npm run db:erase-subject`, `lib/audit.ts:49`; tests `lib/erasure.test.ts` (8). Scrub-in-place only — rows and ids survive, per §5.7's RESTRICT reading. `--days` is a required argument and the window is still unpoliced (§9 Q2); `16`/`17` own the policy and runbook |
| R13-2 | 13 | security | Job authentication fails open outside production: a missing `CRON_SECRET` — or any `NODE_ENV` that is not production — means allow, and unauthenticated `GET /api/jobs/outbox`, `/reconcile` and `/screenshot` all answer 200 | `doc/review/13-jobs-and-cron.md` §5.8, §7 R13-2; `lib/jobs.ts:25-28`; `10` §7 R10-10 registers the mail-queue half.| fixed | `lib/jobs.ts:40-57` (fails closed when not production **or** the database is not loopback), `lib/env.ts:60-73` (`isLocalDatabase`), `lib/ops.test.ts` (both arms, incl. dev + remote `DATABASE_URL` → 401); §5.15, §7 R13-2 |
| R13-3 | 13 | ops | The maintenance channel runs at 5.1 % of its nominal cadence (34 runs in 110.12 h, median gap 3 h 12 m), alerts one human, and cannot notice its own death; inside the queue, no failure notifies anyone | `doc/review/13-jobs-and-cron.md` §5.6, §7 R13-3; `.github/workflows/outbox-tick.yml:5-6`; `U13-1`; `08` §7 R08-3 owns the reconcile schedule and the terminal-rejection alert.| fixed in part | `lib/jobHeartbeat.ts` (`stampHeartbeat`, `heartbeatReport`, bounds derived from §5.6), `prisma/migrations/0010_job_heartbeats/`, `app/api/jobs/config/route.ts:109-118`, all five job routes stamping; **detection closed, notification open** (U13-4, `08` R08-3); §5.15, §7 R13-3 |
| R13-4 | 13 | ops | Both workers' worst-case work equals their `maxDuration` exactly: a 20 s deadline checked between rows plus a 10 s in-flight ceiling is 30 s, with no allowance for the claim's own database time | `doc/review/13-jobs-and-cron.md` §5.10, §7 R13-4; `app/api/jobs/outbox/route.ts:6,25,31`; `app/api/jobs/screenshot/route.ts:7,56,62` | fixed | `lib/jobBudget.ts` (`PLATFORM_CEILING_MS` 30 s, `ROW_CEILING_MS` 10 s, `JOB_WORK_BUDGET_MS` 24 s, `canStartRow`), `lib/outbox.ts:377,388`, both routes' `maxDuration` read out of the source and pinned by `lib/jobsAndCron.test.ts`; §5.15, §7 R13-4 |
| R13-5 | 13 | ops | The queue's retry cadence is the tick, so a backlog drains at the cadence rather than catching up: 25 rows per outbox run and 10 per screenshot run, against a median 3 h 12 m tick | `doc/review/13-jobs-and-cron.md` §5.7, §7 R13-5; `lib/outbox.ts:154-168`; `vercel.json:3-5` | fixed | `lib/outbox.ts:349-410` (`drainInBatches` with a budgeted re-claim loop, `dueOutboxCount` for `remaining`), `lib/jobBudget.ts:60-71` (`jobLimit`), `.github/workflows/outbox-tick.yml:62-94` (four-call re-loop, breaks on `remaining: 0`), `vercel.json` paths gain `?limit=25`/`?limit=10`; §5.15, §7 R13-5 |
| R14-4 | 14 | security | Two mail templates interpolate attacker-influenced text without escaping; the only guard is a charset check three modules away, and one regression would put HTML into customer mail | `doc/review/14-security.md` §7 R14-4; `emails/outbid.tsx`, `emails/receipt.tsx` (no `esc()`); `emails/escape.ts`; `lib/validate.ts`; `10` §7 R10-9 registers the same defect from the mail side.| fixed | the documentation arm, chosen over adding `esc()` where it would encode the body twice: `emails/escape.ts`'s docblock now states that the charset guard is the boundary and that a subject is a header value, never a body to escape, and `lib/listingMail.test.ts` 3 → 4 asserts the subject line instead of the templates (§5.16, §7 R14-4) |
| R14-5 | 14 | security | The validated URL is thrown away and the raw one is stored, so a "validated" listing payload is a stored third-party request beacon for whoever renders it | `doc/review/14-security.md` §7 R14-5; `lib/validate.ts:153-157` (`normalizeUrl` used as a truthiness test, then `return base`) | fixed | `lib/validate.ts:146,153-170` (`logoUrl` returned in the ok arm, `LOGO_URL_MAX = 2048`, `normalizeLogoUrl()` rejecting control characters rather than stripping them) and `app/api/startups/[domain]/route.ts` storing `input.logoUrl`; `lib/ownership.test.ts` 12 → 14 (§5.16, §7 R14-5) |
| R14-7 | 14 | security | A non-production deployment hands a management token to any caller: `POST /api/manage/request` returns the raw 256-bit token in its body whenever `isProduction()` is false, which is enough to write listings | `doc/review/14-security.md` §7 R14-7; `lib/manage.ts:44,47`; Vercel SSO is the only gate and it is a project setting, not repository code (`14` §9 Q1); `10` §7 R10-2 owns the listing-takeover chain this token enables.| fixed | `lib/manage.ts:61-63,68,99` — `devManageTokenEnabled()` requires the app env to be literally `development` **and** `DEV_MANAGE_TOKENS=1` (a Vercel preview reports `"preview"`), with `__devToken` in the payload type and the mint branch; plumbing at `app/api/manage/request/route.ts:40` and `lib/api.ts:209-211`, flag documented in `.env.example`; `lib/manage.test.ts` 7 → 10 (DB-gated); §5.16, §7 R14-7 |
| R14-8 | 14 | security | The ops secret is accepted from the query string on the job surfaces, so the credential that can force the mail pipeline and the screenshot worker is exposed to logs, history and referrers | `doc/review/14-security.md` §7 R14-8; `app/api/jobs/reconcile/route.ts:47`; `app/api/jobs/config/route.ts:35` | fixed in part | both reads deleted — `app/api/jobs/reconcile/route.ts` and `app/api/jobs/config/route.ts` now take `jobGate(req, …)`, so the ops secret arrives by bearer header or JSON body only; `lib/phase7.test.ts` 7 → 9, and `doc/review/13-jobs-and-cron.md` §5.3 is marked pre-fix. **Not** done: rotating `CRON_SECRET`, which has been in URLs and access logs since 2026-09-15 and only an operator can replace (§5.16, §7 R14-8) |
| R14-11 | 14 | security | `next@14.2.35` ships nine advisories, two of them RCE, on an enforced acceptance with a dated expiry | `doc/review/14-security.md` §7 R14-11; `package.json:21`; `ops/accepted-advisories.json` (TRIPWIRE); `npm audit --omit=dev` → `{"critical":1,"total":1}` | fixed | verified rather than extended: `ops/accepted-advisories.json` (still expiring 2026-12-31) + `scripts/audit-prod.mjs` + `.github/workflows/ci.yml:52` re-run — `npm audit --omit=dev` still reports the same nine advisories, two of them RCE, so the acceptance and its tripwire stand and the upgrade (Next 16 + React 19) remains scheduled rather than done; the next change to that file should delete the entry rather than extend it (§5.16, §7 R14-11) |
| R15-1 | 15 | perf | The browser is never allowed to cache anything — every document and read API is `max-age=0, must-revalidate` or `no-store` — so the CDN's 10 s window is the only absorber and every client repeats the whole request | `doc/review/15-performance-concurrency-cost.md` §5.3, §7 R15-1; `lib/route.ts:20-32`; production `/` reads `public, max-age=0, must-revalidate` with `age: 7567` | fixed | `lib/route.ts:45-49` (`READ_CACHE` is now `public, max-age=5, s-maxage=10, stale-while-revalidate=30` — a browser window sized under the 30 s poll interval every consumer of these routes uses; the stale doc-comment claiming the edge rewrites the browser directive was deleted), `lib/readCache.test.ts` (asserts `max-age × 1000 < 30_000` against the client's own literal and fails on a three-digit window); the two OG card routes audited and left CDN-only |
| R15-2 | 15 | perf | `/` ships 152 kB of uncompressed document and never preloads its own font: 12 assets, 608,994 B raw / 157,366 B brotli, with a single `<link rel="preload">` for the webpack runtime | `doc/review/15-performance-concurrency-cost.md` §5.1-§5.4, §7 R15-2; `app/layout.tsx`; `app/fonts/*.woff2` (68,856 B basic-latin). **Amended 2026-09-16:** both premises are wrong for the build in the repo and the correction is the outcome — `app/layout.tsx` is `next/font/google`, so the build emits **two** `<link rel="preload" href="/_next/static/media/…-s.p.woff2" as="font" crossorigin="" type="font/woff2"/>` tags (plus the webpack runtime's `fetchPriority="low"` script preload, three in total, not one), and `content-length: 152029` is the **uncompressed** document, which is 11,620 B at brotli quality 11. See `15` §5.14 | wontfix (reason in `15` §9 Q1) | not fixed: the preload already happens (framework-emitted, verified in the emitted `index.html`), and the remaining size claim is inventory-scaled markup — a product decision owned by `12` §9 Q1, recorded in `15` §5.14 |
| R15-5 | 15 | perf | `/api/activity` is five round trips joining 14 relations for one request, and neither inner lookup is bounded by the caller's `limit`: 27 ms at `limit=6`, 77 ms at `limit=20` | `doc/review/15-performance-concurrency-cost.md` §5.5, §7 R15-5; `app/api/activity/route.ts:16` | fixed | `app/api/activity/route.ts` (one `prisma.$queryRaw` instead of five statements; the hidden-domain exclusion is a `WHERE NOT EXISTS` inside the query so it resolves before `LIMIT` and the page size stays exact; the stake lookup rides the join and is bounded by the unique `(domain, symbol)` pair; response shape unchanged and asserted by `lib/routes.test.ts` + `lib/moderation.test.ts`) |
| R15-6 | 15 | perf | The money path is the one route a paying reader can cold-start: 805 ms for the first request after server start with the provider configured, 56-128 ms warm, against 58 ms when the gate short-circuits | `doc/review/15-performance-concurrency-cost.md` §5.8, §7 R15-6; `app/api/checkout/route.ts:109-110,204` | accepted-risk (reason in `15` §9) | not changed: the pass made no structural change, as the finding's own proposed fix said; the number is now a log line rather than a hand-timed probe, because every origin invocation logs its status and duration (`lib/route.ts`, R15-11) |
| R15-7 | 15 | money | Thirteen simultaneous settlements on one element all succeed with a 3.8 s tail (median 2,480 ms), and the retry budget inside one invocation is 250 s against `maxDuration = 60` — a 4× overshoot | `doc/review/15-performance-concurrency-cost.md` §5.9, §5.11, §7 R15-7; `lib/money.ts` (`MONEY_TX` 25 s, `TXN_MAX_ATTEMPTS = 10`); `app/api/webhooks/stripe/route.ts:17,21` (`08` owns settlement semantics) | fixed | `lib/txn.ts` (`TXN_BUDGET_MS = 40_000`, `TxnRetryOptions`, `ATTEMPT_RESERVE_MS`; `withTxnRetry` refuses another attempt when `elapsed + ATTEMPT_RESERVE_MS > budgetMs` and rethrows the retryable error after a `{"scope":"txn","msg":"budget-exhausted",…}` warn — checkout needs no new handling: `502` with a code, Stripe redelivers, `ProviderEvent` + the payment guard keep the re-run idempotent), `lib/ledger.test.ts` (two budget tests with an injected clock) |
| R15-13 | 15 | data | Every settled dollar writes eleven rows across eight tables and nothing trims them: `OutboxEvent` +3, `AuditLog` +2, and one row each in `EmailLog`, `ActivityLog`, `ProviderEvent`, `FirstClaim`, `Payment`, `Stake`, `Startup`, plus the `Element` aggregate | `doc/review/15-performance-concurrency-cost.md` §5.9, §7 R15-13; 15-table `pg_stat_user_tables` snapshot diff, confirmed over 11 settlements (OutboxEvent +33, AuditLog +22) | fixed in part | `app/api/jobs/config/route.ts` — the `cost` block adds `analyticsRows`, the exact size of the one trail that is neither money nor mail (`STAKE_ANALYTICS` rows are written per settlement and never read), so the growth is countable; `lib/ops.test.ts` asserts it. **Not** done: no retention window — the tables are not equivalent (only `OutboxEvent` is trim-able without discarding history the erasure path treats as permanent) and the window is `12`'s to state (§5.14, §7 R15-13) |
| R16-10 | 16 | legal | "Own an element", "cumulative equity" and "take the crown" imply a legal interest the product never conveys; the mechanical truth is a ranked ad slot that ends on the next outbid | `app/legal/[slug]/page.tsx:18,68`; `lib/pricing.ts:9-10`; `components/Modals.tsx:490` ("it's an ad buy, not a bet"); `16` §5.9 | fixed | `lib/legalDocs.ts` (`OWNERSHIP_SENTENCE`, `STAKE_SENTENCE`), `lib/legalContent.test.ts` |
| R16-11 | 16 | legal | No governing law, no venue, no liability carve-out and no operator identity anywhere on the site; the only address is offered "on request" with no channel | `app/legal/[slug]/page.tsx:39,123`; `16` §5.8 | fixed in part | `lib/operator.ts`, `lib/env.ts:306` (`operatorAdvisories`), `app/api/jobs/config/route.ts`, `lib/legalDocs.ts` (the Operator block), `lib/operator.test.ts` — the values are D1/D7 |
| R16-12 | 16 | content | Four promises with no surface, no metric and no notification: 72-hour report triage, announcements of material changes on the site, 2-3 day replies, and a "we can only discuss billing with the payer" procedure | `app/legal/[slug]/page.tsx:96,110,116,122`; `app/api/admin/reports/route.ts`; `16` §5.7 | fixed in part | `lib/moderation.ts` (`TRIAGE_PROMISE_HOURS`, `reportQueueAge`), `app/api/admin/reports/route.ts` (the five headers), `app/api/jobs/reconcile/route.ts` (`triage`), `lib/moderation.test.ts`, `lib/reconcile.test.ts` — three of the four promises have no surface yet |
| R17-8 | 17 | ops | The one operator action that sends mail to a real person writes no audit row, while both sibling admin routes do | `app/api/admin/outbox/retry/route.ts:1-6,13-30` (no `audit` call); `app/api/admin/startups/moderate/route.ts:48-54`; `17` §3.1 | fixed | `app/api/admin/outbox/retry/route.ts` (`OUTBOX_RETRY` row: action, the retried `key`, outcome, identity); `lib/outbox.test.ts` |
| R17-9 | 17 | ops | The report queue truncates at 50 newest rows with no cursor, no count and no ageing, so a wave hides the oldest complaints — the ones closest to breaching the promise | `app/api/admin/reports/route.ts:8,13-30`; `ops/takedown.md:11`; `17` §3.4 | fixed | already closed by `16` R16-12 and re-read here rather than re-fixed — `app/api/admin/reports/route.ts` (`?before=`, `X-Report-Queue-Age`/`-Overdue`/`-Promise-Hours`); `ops/takedown.md` |
| R17-10 | 17 | ops | `ops/takedown.md` cannot be run by anyone but its author — the outbox-retry call carries a literal <admin bearer> placeholder, `$APP_URL` is defined nowhere in `ops/`, and the job-health call names no credential while needing `$CRON_SECRET` — and it under-states what restoring does not fix | `ops/takedown.md:42` (`<admin bearer>`), `:51` ("(job auth)", no credential named), `:13,18,27,29,35,44` (`$APP_URL`, defined in no `ops/` file); `ops/rollback.md:41` (HEAD's only other mention of `CRON_SECRET`); `lib/jobs.ts` (the two guards); `app/api/admin/startups/moderate/route.ts:45`; `app/api/jobs/screenshot/route.ts:74-76`; `17` §5.7 | fixed in part | `ops/takedown.md` (`:42`'s header value, the `$CRON_SECRET` call, triage paging, the preview backfill at `:93-104`); `ops/README.md` §Auth (`APP_URL`/`ADMIN_TOKEN`/`CRON_SECRET`, 403 vs 401) — which token to mint and where to keep it is `D17-3` |
| R17-11 | 17 | data | `ops/rollback.md` offers a migration rollback that cannot work: 0000-0006 are forward-only, and `migrate resolve --rolled-back` reverts no schema | `prisma/migrations/0000_baseline` … `0006_stripe_provider` (no down files, no `DROP TABLE`/`DROP COLUMN`); `ops/rollback.md:45-46`; `17` §5.4 | fixed | `ops/rollback.md` (forward-only stated; `migrate resolve` no longer offered as a rollback); `prisma/migrations/0000`–`0012` (13 directories) |
| R17-12 | 17 | ops | There is no database-down runbook: not what the site serves, not the PITR window, not who authorises a restore | `ops/rollback.md:44-50`; `17` §3.7; `U17-3`, `U17-6` | fixed in part | `ops/database.md` (what the site serves, the restore path, who authorises); the PITR window is `U17-3` and the authorisation decision is `D17-2` |
| R17-13 | 17 | ops | There is no email-outage runbook and no way for an operator to see the queue they would be managing; with `RESEND_API_KEY` unset, a send is recorded as `"logged"` while nothing is delivered | `lib/email.ts:41`; `app/api/report/route.ts:77`; `app/api/waitlist/route.ts:66`; `ops/takedown.md:38-42`; `17` §3.7 | fixed in part | `ops/email.md`; `app/api/jobs/config/route.ts` (`mail.driver`, `mail.suppressed` beside `failedCount`/`oldestUnretriedKey`); `lib/ops.test.ts` — the status surface and the owner are `D17-7` |
| R17-14 | 17 | ops | There is no abuse-wave runbook, and the throttle that would blunt one is per-instance memory because Upstash is unconfigured — the production config report says so in its own words | `ops/rollback.md:52-53`; `lib/rateStore.ts:76,93`; `17` §5.2 (quoted `degraded` finding) | fixed | `ops/abuse-wave.md` (batch lever, edge lever, restore); `app/api/admin/startups/moderate-batch/route.ts` (`MAX_BATCH_DOMAINS = 50`); `lib/moderation.test.ts` — enabling Upstash stays `D17-6` |
| R17-15 | 17 | ops | There is no site-down comms: no status surface, no template, no owner, and no canonical operator name to sign the message with | `ops/` (two files, neither about comms); five page routes, none a status page (`17` §3.1); `doc/PROD-READINESS-CHECKLIST.md:198` | fixed in part | `ops/comms.md` (three templates, the assertion table, the identity rule, the channels) — the status surface is `D17-7` |
| R18-10 | 18 | ops | Client crashes are reported to the user's browser only: both error boundaries `console.error` and transmit nothing | `app/error.tsx:20`; `app/global-error.tsx:16`; `18` §5.1 | fixed | `lib/clientError.ts`; `components/ClientErrorReporter.tsx`; `app/error.tsx`; `app/global-error.tsx`; `lib/clientError.test.ts` |
| R18-11 | 18 | ops | No request correlation, deployment version or env on any log line: the 23 runtime sites carry a scope and domain fields only | `18` §3.1 (71 `console.*` in 24 files; three structured shapes) | fixed | `lib/log.ts` (`withRequestScope()`, `deploymentId()`, `appEnv()`, `describeError()`, the level rule); `lib/route.ts` (`X-Request-Id`); `lib/log.test.ts`; the runtime-site migration (`18` §5.1, `18` §5.9) |
| R18-12 | 18 | ops | The alert surface's status code is ambiguous: 401 (bad secret), 503 (config finding) and 200 (healthy) can all read to a monitor as "not 200" | `18` §5.5 probes; `lib/env.ts:128-144`; `lib/ops.test.ts:124-160` | fixed | `ops/alerts.md` (the three contracts verbatim, an expected status beside each of the five URLs); `app/api/health/route.ts` (the monitor-friendly liveness route) |
| R18-13 | 18 | privacy | Analytics have no consent gate, and the switch that turns them on is one env var; production today ships no script, no cookie and no consent string | `app/layout.tsx:29-31`; `lib/analytics.ts:17-28`; `18` §5.6 | fixed in part | `lib/analyticsConsent.ts` (fails closed); `components/AnalyticsConsent.tsx`; `lib/analyticsConsent.test.ts`; the legal-page update — whether analytics run at all is `D18-3` |
| R18-14 | 18 | ops | Nothing measures the customer-facing outcome: no successful-checkout rate, no settled-stake rate, no click-to-paid funnel | `18` §3.7, §3.9; `app/api/stats/route.ts`; `app/api/jobs/reconcile/route.ts` | fixed | `lib/opsMetrics.ts` (`funnel`, `money.window.settledRate`); `lib/opsMetrics.test.ts`; `ops/alerts.md` (the definitions) |
| R19-3 | 19 | ux | The board's Table Order panel has no zero-row branch: on day one it renders a header, a caption and blank space where ranks belong | `doc/review/19-launch-and-marketing-readiness.md` §5.5, §7 R19-3; `components/WorldOrder.tsx:74-79,80,125`; reproduction `curl -s "https://www.periodictable.lol/api/table-order"` → `200 []` (2026-09-15T08:03:29Z) | fixed | `components/WorldOrder.tsx` (zero-row branch) with `NO_STAKES_YET` exported from `lib/activityFace.ts`; asserted in `lib/launchReadiness.test.ts` |
| R19-4 | 19 | content | No FAQ exists and no URL is reserved for one — `/faq` and `/pricing` are 404s, and the only explanatory surface is the three-step help modal | §5.4, §5.9, §7 R19-4; `components/Modals.tsx:19-44`; the plan expects one (`00-REVIEW-PLAN.md:152`) | fixed | `app/faq/page.tsx`, `lib/faq.ts`, `lib/faqDocs.ts` (ten answers, numbers interpolated from `lib/pricing.ts` and two sentences quoted from `lib/legalDocs.ts`), appended last in `lib/sitemapData.ts`; asserted in `lib/launchReadiness.test.ts`, `lib/sitemapData.test.ts` and `lib/a11y.test.ts` |
| R19-5 | 19 | ux | The rules are reachable from the footer and the checkout modal only — the help modal prints "About & disclaimer · Rules & payments" as dead text and the element page links no legal surface | §5.4, §5.7, §7 R19-5; `components/FooterBar.tsx:17,19,21`; `components/Modals.tsx:41,490` | fixed | `components/Modals.tsx` (the help modal's link row; the checkout modal gained the FAQ link) and `app/elements/[sym]/page.tsx`'s `FootnoteLinks` in its claimed and unclaimed branches; asserted in `lib/launchReadiness.test.ts` |
| R19-6 | 19 | content | Nothing states the price ladder or what an outbid holder keeps — no surface names the $5 floor, the $1 beat-the-leader rule, or that rank is cumulative | §7 R19-6; `components/HeroCard.tsx:73`; `components/Modals.tsx:20-22`; `components/WorldOrder.tsx:125`; `doc/PROD-READINESS-CHECKLIST.md:296-302` | fixed | `MIN_STAKE`, `TAKEOVER_MARGIN` and `TIE_CLEARANCE` in `lib/pricing.ts`; six literal `$5`s templated (`components/HeroCard.tsx`, `components/SearchPill.tsx`, `components/TerritoryView.tsx`, `app/elements/[sym]/page.tsx`, `lib/ogCard.ts`, `lib/shareMeta.ts`), `lib/stakeQuote.ts`'s tie copy, and the ladder stated as FAQ item 3; swept by a comment-stripping assertion in `lib/launchReadiness.test.ts` |
| R19-7 | 19 | ops | Three published support addresses (`abuse@`, `payments@`, `hello@`) with SLAs and no evidence any is read; the sender address diverges across surfaces | §5.7, §7 R19-7; `app/legal/[slug]/page.tsx:110,116,122`; `HANDOFF.md:603`; `lib/email.ts:59`; `app/api/report/route.ts:65`; mailbox existence is U19-2 | fixed in part | the published sender and the report inbox read one constant (`SUPPORT.hi` as `lib/email.ts`'s default, `SUPPORT.abuse` in `app/api/report/route.ts`), the address is on the contact page with a revision-log entry and the digest, and the FAQ repeats it (`lib/intakeMail.test.ts`'s assertion re-pointed to `SUPPORT.hi`). Delivery is `U19-2`; the Vercel `EMAIL_FROM` is an env read, and `HANDOFF.md:613` and `doc/PROD-READINESS-CHECKLIST.md:141` still record `info@` |
| R19-8 | 19 | ops | The launch-gate runbook contradicts the runbook that supersedes it in four places (Whop env names, Upstash, a phantom `hidden=true` column, a comms file that does not exist) and its six gate boxes are unticked | §5.8-§5.9, §7 R19-8; `doc/phase-5-launch/02-launch-gate-runbook.md:6-12,14,17,21`; `ops/rollback.md:34,41`; `prisma/schema.prisma:75` | fixed | `doc/phase-5-launch/02-launch-gate-runbook.md` (Stripe replaces Whop, Upstash named once as not used, `hidden=true` → `moderationState = HIDDEN` through admin triage, the phantom comms file replaced by the written-before-posting rule, step 0 added, `ops/rollback.md` declared authoritative), with `README.md`, `01-seed-instrument.md`, `seed-list.csv` and `scripts/check-prod-env.mjs` (now covering all ten required names); asserted in `lib/launchReadiness.test.ts` |
| R19-9 | 19 | seo | No attribution channel and no link tagging: no product social account exists, the share card credits nobody, and `utm_` appears zero times repo-wide — the launch cannot be told apart from silence | §5.9, §7 R19-9; `lib/shareMeta.ts:39`; `lib/validate.ts:114` | fixed in part | `twitterSite(env)` in `lib/shareMeta.ts` — the single place a handle is configured, returning nothing when unset or blank — consumed by the home card, `lib/legalMeta.ts` and `app/elements/[sym]/page.tsx`; the `utm_source`/`utm_medium`/`utm_campaign=launch` convention is written into the runbook step that posts. The account and the post are `U19-1` |
| R20-1 | 20 | security | A `critical` Next 14 dependency acceptance with an alarm-free tripwire and a hard `expires 2026-12-31`: both criticals are unreachable only while `next.config.mjs` has no `images` key and hosting stays Linux | `doc/review/20-post-launch-and-debt.md` §5.2, §7 R20-1; `ops/accepted-advisories.json`; `doc/PROD-READINESS-CHECKLIST.md:314` | accepted-risk (checked mechanically, §5.13, `D20-1`) | `ops/accepted-advisories.json`'s `tripwires` array (`file`, `must_not_contain`, `because`, with `expires` per acceptance) read by `scripts/audit-prod.mjs`'s `checkTripwires()` — comments stripped, fails closed on a missing file, a missing field or a hit, and run before the audit; the migration is not attempted and the expiry is not extended (`D20-1`) |
| R20-4 | 20 | data | Twelve `Payment` rows in five shapes, six of them dead `PENDING` rows indistinguishable in a ledger view from a genuine unpaid $5 | §5.1, §7 R20-4; `doc/PROD-READINESS-CHECKLIST.md:296,301,308,316-320` | fixed in part (`Q1` open, CR-20.5) | `doc/PAYMENT-LEDGER-SHAPES.md` — the five shapes in the DB's lowercase vocabulary, the query that reproduces them, the enum error it trips, the two readings of one `PAID` status and the exclusion rule for stake-less rows — plus `lib/opsMetrics.ts` naming which of its totals may be shown; no row was deleted, so the clear-or-leave half is still `Q1`/CR-20.5 |
| R20-5 | 20 | money | Three stake-less `PAID` rows (`stakeId = NULL`) read as $17 of revenue — kept deliberately as settle proof, to be excluded from any revenue read | §5.1, §7 R20-5; `doc/PROD-READINESS-CHECKLIST.md:304,310,319`; `lib/webhook.test.ts` treats the same shape as a corrupted ledger when the app produces it | fixed | the same document's `stakeId IS NOT NULL` exclusion for settled revenue with the deliberate-not-corrupted note beside it; `lib/webhook.test.ts`'s corrupted-ledger contract now names the three production rows as the known instance |
| R20-6 | 20 | ops | No error tracking and no health endpoint: the entire capture surface is two `console.error` calls and only GitHub e-mail stands between an exception and a human | §5.4, §7 R20-6; `app/error.tsx:20`; `app/global-error.tsx:16`; `package.json` (no error-tracking dependency); doc 18 owns the design (`00-REVIEW-PLAN.md:147`); the same gap is `18` §7 R18-1 (P1, open) — this row is the deferral date and the health-endpoint half | superseded by `18` (`R18-1`) | nothing to change here: `18` §7 R18-1 shipped the `ErrorReport` sink, its reader and the thresholds in `ops/alerts.md`, and the health-endpoint half is now `/api/jobs/config` plus `/api/admin/ops`; the vendor question stays `D18-1` |
| R20-7 | 20 | ops | `reconcile` is scheduled — on a best-effort tick measured at ~7 %, with no independent reader and no daily floor for the diagnostics; the plan's "not scheduled" premise is half wrong | §5.3, §7 R20-7; `vercel.json` (two worker crons only); `.github/workflows/outbox-tick.yml:9,78,88`; `doc/PROD-READINESS-CHECKLIST.md:156`; `HANDOFF.md:807-817`; cf. R17-1, where "on no schedule" means no platform cron and the Action is its only cadence | fixed (`D20-7`) | `app/api/jobs/daily/route.ts` — one `30 4 * * *` `/api/jobs/daily` entry that drains previews on `DAILY_PREVIEW_BUDGET_MS` and then runs `config` and `reconcile` in-process through their own handlers, one `x-request-id` across all three, every leg stamping its own heartbeat row and the run its own — and the run's key is watched in `HEARTBEAT_ROUTES` beside the workers it drives — and a 500 naming the failing legs; the budgets are named in `lib/jobBudget.ts`, the second cron slot stays with the outbox, and the pinger question is now cadence-only (`Q4`) |
| R20-8 | 20 | security | Rate limits still fail open: Upstash is unconfigured, so `/go` and `/checkout` limits fall back to per-instance memory — reported as `degraded` and unchanged | §5.5, §7 R20-8; `lib/env.ts:121-124`; `doc/PROD-READINESS-CHECKLIST.md:198`; `doc/ROADMAP.md:426` | accepted-risk (reason in §9) | — |
| R20-9 | 20 | ops | Support and moderation have published SLAs and no cadence: nothing says how often the report queue or the three mailboxes are drained, and the queue notifies nobody | §5.8, §7 R20-9; `app/legal/[slug]/page.tsx:110,122`; `ops/takedown.md:11`; `app/api/admin/reports/route.ts` | fixed in part | `ops/takedown.md`'s daily sweep (the `X-Report-Queue-Open`/`-Overdue`/`-Oldest-Hours` headers off `app/api/admin/reports/route.ts`, the mailbox order, the Friday rule) and `ops/comms.md`'s shared channel list; the watching is still a person's habit on a day nobody may be at a desk |
| R20-10 | 20 | money | The refund and dispute cadence has never been exercised with real money: no runbook step, no dispute-evidence template, no test that a real refund leaves the ledger consistent | §5.11, §7 R20-10; `doc/PROD-READINESS-CHECKLIST.md:310` (J4 open); `lib/stripe.ts:312-327`; `lib/recompute.ts:27,132`; `05` §7 R05-6 (copy fixed, reversal unobserved); cf. R17-3 (the runbooks do not exist, literature side) | fixed in part | `ops/refunds-and-disputes.md` now names the test suite and line that pin the four events, says plainly that nothing there has been exercised with real money, and shares the takedown sweep's cadence; the `D14` citation the whole runbook set carried was corrected to `D17-5`; the real-money half waits on the first settled payment |
| R20-11 | 20 | data | PITR is promised and never drilled — no dated restore result exists — and the one retention decision made (hiding clears `previewImgUrl`) is still absent from the takedown runbook | §5.8, §7 R20-11; `ops/rollback.md:44-49`; `doc/PROD-READINESS-CHECKLIST.md:255`; doc 12 owns retention (`00-REVIEW-PLAN.md:127`) and the drill is its `12` §12 U12-1 | open (drill owed, due 2026-10-15) | `ops/database.md` writes the restore drill as **owed** with its date, its pass condition and the branch it runs against, and the takedown runbook names the retention owner; CR-20.2's 2026-10-15 re-read carries it (`U20-2`) |
| R20-14 | 20 | content | Does the game ever end? The design says stakes never expire (`doc/ROADMAP.md:26,98`) and no user-facing surface says it — the buyer's most valuable fact is unstated | §5.10, §7 R20-14; `doc/ROADMAP.md:26,98`; `doc/phase-6-v2/README.md:12`; `components/Modals.tsx:19-44` | fixed (`D20-2`) | `SERVICE_TERM_SENTENCE` in `lib/legal.ts`, rendered in the rules, in the FAQ and beside the price on both element-page branches, with the revision-log entry and the copy-lock digest moved in the same change; the row's own proposed wording (a board with no end date) was **not** used, because the published floor makes it false |

## P3

| ID | Phase | Category | Summary | Evidence | Status | Fixed in |
| --- | --- | --- | --- | --- | --- | --- |
| R01-4 | 01 | seo | No `rel="canonical"` on any route, and the only internal link into the board is `/?el=<sym>&stake=<n>` — a query-state document identical in size to `/` | §5.1, §5.2 (0 canonical tags); §5.9 (`/?el=H` → 200, 149431 bytes); `app/elements/[sym]/page.tsx:126-131` | fixed | `lib/shareMeta.ts`, `app/elements/[sym]/page.tsx`, `app/s/[domain]/page.tsx` |
| R01-5 | 01 | seo | Every sitemap URL claims to have changed at the moment the sitemap was rendered (all 123 `lastmod` identical) | `app/sitemap.ts:14-15`; §5.6 (123 identical `lastmod`, unchanged 41 minutes later) | fixed | `app/sitemap.ts`, `lib/sitemapData.ts` |
| R01-6 | 01 | seo | `robots.txt` declares no `Sitemap:` directive, so the sitemap is discoverable only by guessing `/sitemap.xml` or a manual submission | `app/robots.ts:8-14`; §5.6 (104-byte file, no `Sitemap:` line) | fixed | `app/robots.ts` |
| R01-7 | 01 | ops | The sitemap's host comes from `NEXT_PUBLIC_APP_URL`, whose absence nothing detects (`requireProdEnv` has no call site), while the code fallback is the apex | `app/sitemap.ts:12`; `lib/env.ts:11,164`; `app/api/jobs/config/route.ts:10-11`; §5.8 | fixed | `lib/siteUrl.ts`, `app/sitemap.ts` |
| R01-8 | 01 | security | The OG card interpolates stored strings into SVG markup unescaped, on the product's own origin; the only control is upstream WHATWG validation | `app/og/[sym]/route.tsx:18-19`; `lib/validate.ts:1-30,45-70,75`; §5.10 | fixed | `lib/ogCard.ts` (`escapeXml`) |
| R02-6 | 02 | ux | The activity card cannot report failure or staleness on its main path: handed-down rows suppress its own error, so a failed refresh leaves retained rows under a footer still claiming `live · updates every 30s` | §6 (row 3 and row 5); `components/ActivityCard.tsx:39,70,110,115`; §5.10 (`offline · not updating`, `Retry`, static dot) | fixed | `lib/activityFace.ts`, `components/ActivityCard.tsx`, `app/page.tsx` |
| R02-7 | 02 | content | The legal pages ship the home page's `<title>` and description verbatim | §5.4; `app/legal/[slug]/page.tsx:5,124-125`; `app/layout.tsx:17-22` | fixed | `lib/legalMeta.ts`, `app/legal/[slug]/page.tsx` |
| R03-1 | 03 | data | The dataset and the roadmap disagree about where the four exotics sit: columns 8/9/10/11 in `lib/elements.ts`, 7/8/9/10 in `lib/elements.json`, 7-10 in `doc/ROADMAP.md`; `03d8591` moved the pod and the tiles but not the JSON, and the API serves the `.ts` | `doc/review/03-the-board.md` §5.4-5.5; `lib/elements.ts` vs `lib/elements.json` `gridCol`; `doc/ROADMAP.md:142,148`; `prisma/seed.ts:36-60` | fixed | `lib/gridGeometry.ts`, `app/api/elements/route.ts`, `components/PeriodicGrid.tsx`, `doc/ROADMAP.md` |
| R03-2 | 03 | correctness | The API ships money aggregates that count stakes the tiles refuse to show — the face filter keeps only `amountUsd > 0` `DIRECT_STATES` rows while `pool`/`count` and `claimedElements` count every stake; P3 only because both sides are empty today, it becomes real the day a stake is hidden | §5.1, §5.8; `app/api/elements/route.ts:15-18,35-40`; `app/api/stats/route.ts:21,27`; `doc/PROD-READINESS-CHECKLIST.md` §J6 | fixed | `lib/moderation.ts`, `app/api/stats/route.ts`, `lib/claimFace.test.ts` |
| R03-3 | 03 | perf | The declared cache window never reaches the wire: `s-maxage=10, stale-while-revalidate=30` in the route, `public, max-age=0, must-revalidate` plus `Age: 6` and `X-Vercel-Cache: HIT` on the wire, as for all five read APIs | §5.7; `app/api/elements/route.ts:48` | fixed | `lib/route.ts`, `app/api/elements/route.ts`, `app/api/elements/[sym]/route.ts` |
| R03-4 | 03 | a11y | The search field announces combobox state it does not have: `aria-expanded="true"` and `aria-controls="search-results"` are hardcoded while the listbox is conditional, and the `role="option"` items carry no `aria-selected` | §5.2; `components/SearchPill.tsx:93,94,130,146,186` | fixed | `components/SearchPill.tsx`, `lib/searchCombobox.test.ts` |
| R04-2 | 04 | correctness | A hidden holder is priced as a newcomer: `?me=` and the modal answer from the hidden-filtered list while checkout looks the caller up unfiltered and reserves only TAKE, so the modal promises a 15-minute hold no `Reservation` row backs | `doc/review/04-element-detail-and-pricing.md` §5.8, §4.4, §6, R04-2; `app/api/elements/[sym]/route.ts:13,47-52`; `components/Modals.tsx:150-153`; `app/api/checkout/route.ts:215-218,296-311` | fixed | `app/api/elements/[sym]/route.ts` (`prices.boardComplete`), `lib/api.ts`, `lib/stakeQuote.ts`, `components/Modals.tsx`, `lib/routes.test.ts`, `lib/contracts.test.ts` |
| R04-3 | 04 | testing | `validateTake` (`lib/pricing.ts:53-58`) is dead code: its only references are its own test, while the take floor is enforced by `reservedTotal` and `lib/settle.ts:161` — so the documented $L+1 rule has no test on its live path | `doc/review/04-element-detail-and-pricing.md` §5.7, §5.8, R04-3; `lib/pricing.test.ts:11,93-95` | fixed | `app/api/checkout/route.ts:309-312`, `lib/detailFace.test.ts` (truth table) |
| R04-4 | 04 | seo | `/elements/<symbol>` is case-sensitive with no redirect, so `/elements/au` — the form a human types — is a 404 while `/elements/Au` is 200; `?el=` and the API agree with the strictness | `doc/review/04-element-detail-and-pricing.md` §5.5, §5.8, R04-4; `app/elements/[sym]/page.tsx:15-16,26-27`; `app/api/elements/[sym]/route.ts:25`; `app/page.tsx:108` | fixed | `lib/elements.ts` (`findElementBySymbol`), `lib/elements.test.ts`, `app/elements/[sym]/page.tsx`, `app/api/elements/[sym]/route.ts`, `app/og/[sym]/route.tsx`, `app/api/checkout/route.ts`, `app/page.tsx` |
| R05-3 | 05 | a11y | Home serves no `<main>` and no skip link, and the profile page has no landmark and no heading above `<h3>` | §5.2, §5.3, §5.4, R05-3; `app/layout.tsx`; `app/page.tsx`; `app/s/[domain]/page.tsx:164` | fixed | `app/layout.tsx` (skip link), `app/globals.css` (`--z-skip`), `app/page.tsx`, `app/s/[domain]/page.tsx` (+`<h1>`/`<h2>`), `app/elements/[sym]/page.tsx`, `app/legal/[slug]/page.tsx`, `lib/boundaryChrome.tsx`, `lib/a11y.test.ts` |
| R05-4 | 05 | a11y | Both branches of each checkout error render the same element id, so a client and a server failure produce duplicate ids and `aria-describedby` resolves to the first div | §5.3, §5.4, R05-4; `components/Modals.tsx:329-332,343-346,357-360` | fixed | `components/Modals.tsx` (one node per id, holding both messages), `lib/a11y.test.ts` |
| R05-5 | 05 | a11y | Four panels refetch every 30 s with no pause and none is `aria-live`, so a tab open an hour makes 480 requests and shifts numbers under a pointer | R05-5; §5.4; `app/page.tsx:67,73,76`; `components/ActivityCard.tsx:36`; `components/TerritoryView.tsx:31`; `components/WorldOrder.tsx:23`; SWR 2.5.1 gates `refreshInterval` on visibility (`node_modules/swr/dist/use-swr-guqrv3k1.js:624`) | accepted-risk | — (reason in `05` §9 Q2: a hidden tab already refetches zero times and no surface sets `refreshWhenHidden`, so there is nothing to pause; a manual toggle would remove the live board the product promises) |
| R05-6 | 05 | content | `/legal/rules` says "we do not offer refunds" twice while the shipped path reverses a settled payment (`kind: "refund"`) on `charge.refunded` | §5.2, §5.4 (probe 5.4.3: zero hits for the old copy), R05-6; `app/legal/[slug]/page.tsx:81-84,86-89`; `lib/recompute.ts:116,165`; `lib/stripe.ts:325-327,340` | fixed | `app/legal/[slug]/page.tsx` (copy only — the reversal itself is still unobserved) |
| R05-7 | 05 | content | Two promises nothing keeps: reports "actioned within 72 hours" and a waitlist "we'll be in touch", while the report surface is a pull queue and a waitlist join sends nothing | §5.4, R05-7; `app/legal/[slug]/page.tsx:104`; `app/api/admin/reports/route.ts`; `app/api/waitlist/route.ts:39,44`; `components/Modals.tsx:192` | fixed | `emails/report.tsx`, `emails/waitlist.tsx`, `emails/escape.ts`, `lib/email.ts`, `lib/outbox.ts`, `app/api/report/route.ts`, `app/api/waitlist/route.ts`, `lib/intakeMail.test.ts`, `lib/moderation.test.ts` (teardown), `README.md`, `.env.example` |
| R05-8 | 05 | content | Production serves the dev-simulator copy on `/pay/[paymentId]` ("DEV SIMULATOR", "No Stripe keys configured") while the API 403s outside mode `dev` | §5.2, §5.4 (probes 5.4.4-5.4.6), R05-8; `app/pay/[paymentId]/page.tsx:52,55,93`; `app/api/dev/pay/route.ts:15` | fixed | `app/pay/[paymentId]/page.tsx` (provider-mode gate), `app/pay/[paymentId]/PaySimulator.tsx` (new), `lib/a11y.test.ts` |
| R06-3 | 06 | ux | Two different 409s are both announced as "Price moved": the client branches on the presence of `takeLead` alone, so a reservation conflict and a floor violation read alike | `doc/review/06-checkout-before-payment.md` §5.3 (case 03), §7 R06-3; `components/Modals.tsx:267-269`; `app/api/checkout/route.ts` classify → `BELOW_FLOOR` + `takeLead` | fixed | `components/Modals.tsx`, `lib/checkoutFace.ts` (`checkoutRefusal`) |
| R06-4 | 06 | ux | The server's attestation rejection is stored where nothing renders it, so the buyer sees a form that simply will not submit | `doc/review/06-checkout-before-payment.md` §5.3, §7 R06-4; `app/api/checkout/route.ts:130-132`; `components/Modals.tsx:273-276` | fixed | `lib/checkoutFace.ts` (`checkoutRefusal`), `components/Modals.tsx` |
| R06-5 | 06 | ux | A social-handle rejection is printed under the Product URL label, because both branches return `field:"url"` | `doc/review/06-checkout-before-payment.md` §5.3, §7 R06-5; `lib/validate.ts:87-123`; `components/Modals.tsx:340-362` | fixed | `lib/checkoutFace.ts` (`CHECKOUT_MSG`), `components/Modals.tsx` |
| R06-6 | 06 | ux | A blocked challenge script or an autofilled honeypot gives one opaque sentence: the widget renders nothing when `api.js` fails to load | `doc/review/06-checkout-before-payment.md` §5.3 (cases 19-20), §7 R06-6; `components/TurnstileWidget.tsx:76-83` | fixed | `components/TurnstileWidget.tsx` (`onLoadFail`), `lib/checkoutFace.ts` |
| R06-9 | 06 | correctness | Concurrent submits answer one payment with two different bodies: the winner's carries `provider` and `reservation.guaranteedTake`, the loser's does not | `doc/review/06-checkout-before-payment.md` §5.4, §7 R06-9; `app/api/checkout/route.ts:88-104`; probe — both 200, same `paymentId` | fixed | `app/api/checkout/route.ts` (`idempotentReplay`) |
| R06-10 | 06 | data | Any string containing `@` is accepted as the receipt address; the client's regex is the only real check and direct callers bypass it | `doc/review/06-checkout-before-payment.md` §5.7, §7 R06-10; `app/api/checkout/route.ts:147`; `components/Modals.tsx:147,205` | fixed | `lib/validate.ts` (`isEmail`), `app/api/checkout/route.ts`, `components/Modals.tsx` |
| R07-5 | 07 | correctness | The partial-configuration warning is unreachable in production: it sits after the paused guard, which has already returned by then | `doc/review/07-payment-provider-integration.md` §5.5, §7 R07-5; `app/api/checkout/route.ts:109-114`; `lib/flags.ts:12-20` | fixed | `app/api/checkout/route.ts` (warning above the paused guard, latched once per process), `lib/phase7.test.ts` |
| R07-6 | 07 | data | Legacy `WHOP` rows are a dead end that still renders a payment page: `/pay/{id}` answers 200 while the simulator endpoint refuses the row | `doc/review/07-payment-provider-integration.md` §5.7, §7 R07-6; `prisma/migrations/0006_stripe_provider/migration.sql`; `prisma/schema.prisma:54-60`; probe — `/pay/{whop-row}` → 200, `POST /api/dev/pay` → 403 | fixed | `app/pay/[paymentId]/page.tsx` (row-provider gate), `lib/phase7.test.ts`; the `WHOP` enum value is deliberately retained |
| R07-7 | 07 | content | The simulator's copy asserts a cause it cannot know: "No Stripe keys configured" is false in a partial configuration and in a fully-configured preview | `doc/review/07-payment-provider-integration.md` §5.3, §5.5, §7 R07-7; `app/pay/[paymentId]/PaySimulator.tsx:56` | fixed | `app/pay/[paymentId]/PaySimulator.tsx` (the `reason` prop and the sentence deleted), `app/pay/[paymentId]/page.tsx`, `lib/phase7.test.ts` |
| R08-5 | 08 | data | `PaymentStatus.CANCELED` is unreachable, so an abandoned checkout has no shape and no cancel path exists to be trusted | `doc/review/08-settlement-and-ledger-integrity.md` §5.9, §7 R08-5; `prisma/schema.prisma:37-43`; no writer in `app/`, `lib/` or `prisma/` | fixed | `lib/abandonedCheckouts.ts`, `app/api/jobs/abandoned-checkouts/route.ts`, `lib/audit.ts` (`CHECKOUT_ABANDONED`), `lib/abandonedCheckouts.test.ts` (7 DB tests); `cancel_url` still has no server callback — the sweep is the writer |
| R08-6 | 08 | correctness | A failed card is not a failure state: `payment_intent.payment_failed` is recorded `IGNORED / unrelated-event` | `doc/review/08-settlement-and-ledger-integrity.md` §5.6, §7 R08-6; `app/api/webhooks/stripe/route.ts:107-115` | fixed | `lib/stripe.ts` (`DECLINED_EVENT_TYPES`, `stripePayloadIsDeclined`), `app/api/webhooks/stripe/route.ts` (`declined-attempt` detail), `lib/phase8.test.ts`; the payment deliberately stays PENDING and payable — see `08` §9 Q4 |
| R08-7 | 08 | data | Deleting a payment orphans its provider-event history and leaves the row without a payment | `doc/review/08-settlement-and-ledger-integrity.md` §5.9, §7 R08-7; `prisma/schema.prisma:323-330` (`onDelete: SetNull`) | fixed | `prisma/schema.prisma` (`ProviderEvent.elementId`/`startupId`, `@@index([outcome, createdAt])`), `prisma/migrations/0007_provider_event_attribution/migration.sql` (columns, backfill, index), `lib/settle.ts` (`recordProviderEvent`); attribution is copied at ingest, not a foreign key |
| R09-4 | 09 | data | A fully unwound stake is still listed as a bid and still counted, so a tile can show one stake, `count 1`, `pool 0` and no leader | `doc/review/09-ownership-and-competition.md` §5.5 (run ZA on `Po`), §7 R09-4; `lib/pricing.ts:149-157` | fixed | `app/api/elements/[sym]/route.ts` (`amountUsd > 0`, `count` from the listed rows), `app/elements/[sym]/page.tsx`; test `lib/routes.test.ts` (zero-amount row not listed, not counted) |
| R09-5 | 09 | content | The reclaim quote has no floor and no expiry, so a bidder unwound to $0 can be mailed a figure the intake then refuses | `doc/review/09-ownership-and-competition.md` §5.4, §7 R09-5; `lib/pricing.ts:14-17`; `lib/email.ts:85`; `lib/links.ts:17-23` | fixed | `lib/pricing.ts` (`smallestFreeAmount`), `lib/stakeQuote.ts` (`joinSpace`, the join and crown copy), `components/Modals.tsx`; test `lib/stakeQuote.test.ts`. The mailed sentence is doc `10`'s (copy-locked template) |
| R09-6 | 09 | ux | The crown copy promises that "any $5+ amount joins the ladder" on elements where $5 is the one amount that cannot | `doc/review/09-ownership-and-competition.md` §5.8, §7 R09-6; `components/Modals.tsx:414-426` | fixed | `components/Modals.tsx` (crown sentence from `crownCopy`, hold from `takeHold`), `lib/stakeQuote.ts` (`crownCopy`), `lib/api.ts`; tests `lib/detailFace.test.ts` (derived call shape; the three static sentences are forbidden), `lib/stakeQuote.test.ts` |
| R09-7 | 09 | ops | A dethroned holder with no email address is never told they lost #1 — nothing else records the event for the victim | `doc/review/09-ownership-and-competition.md` §5.9, §7 R09-7; `lib/settle.ts:222-233` | fixed | `lib/settle.ts` (the funding payment's address as the fallback, `OUTBID_UNNOTIFIED` when there is none), `lib/audit.ts`; test `lib/settle.test.ts` (funded-address case and the no-address audit case) |
| R10-8 | 10 | correctness | The email preview route 500s on its own default template: the `👑` subject cannot be encoded into a response header | `doc/review/10-email-and-notifications.md` §5.5, §7 R10-8; `app/api/emails/preview/route.ts:7-9,44`; `emails/outbid.tsx:14-15` | fixed | `app/api/emails/preview/route.ts` (the subject rides in an HTML banner instead of a header `:106`; the retired header has a `TypeError` regression test), `emails/outbid.tsx` (the subject is exported and shared with the sender rather than copied); test `lib/emailPreview.test.ts` (5) |
| R10-9 | 10 | security | Two templates interpolate unescaped, and the preview route reflects the caller's query string, on the product's own origin | `doc/review/10-email-and-notifications.md` §5.5, §7 R10-9; `emails/escape.ts:8` used by `emails/report.tsx:34,37` and `emails/waitlist.tsx:19`, not by `emails/receipt.tsx:25-33` / `emails/outbid.tsx:23-31`; live `?sym=<img src=x onerror=alert(1)>` → 200 with the tag reflected | fixed | `emails/escape.ts` is now used by all four templates — including the `providerRef` in `emails/refund.tsx:43`, which verification found, not the reading; `app/api/emails/preview/route.ts` resolves `?sym=` through the canonical `ELEMENTS` lookup (`:44`) instead of reflecting the caller's string, and the URL slots (`viewUrl`, `reclaimUrl`, `unsubUrl`) stay `%`-encoded rather than entity-escaped; tests `lib/listingMail.test.ts` (3, hostile render), `lib/emailPreview.test.ts` |
| R10-10 | 10 | ops | Anyone can drain the mail queue where the job secret is not enforced: the route's `GET` is the POST handler | `doc/review/10-email-and-notifications.md` §5.5, §7 R10-10; `app/api/jobs/outbox/route.ts`; `lib/jobs.ts`; live 2026-09-15 — unauthenticated `GET` → 200 `{"ok":true,"claimed":5,"completed":5,"failed":0}` | open | — |
| R10-11 | 10 | data | `EmailLog` cannot say which payment a message belonged to, and its schema comment lists two templates while four are written | `doc/review/10-email-and-notifications.md` §5.1, §7 R10-11; `prisma/schema.prisma:365-376` | fixed (the correlation half; the retention window for the address in `to` is still open under `16`) | `prisma/schema.prisma` (`EmailLog.dedupeKey`, `providerMessageId`, `providerStatus`, plus the two stale template comments corrected), `lib/email.ts` (`logEmail` writes the row's columns `:308-333`), `prisma/migrations/0008_email_notifications/` |
| R11-1 | 11 | correctness | The dev email preview 500s on every path but one: the response header is set to a subject containing a crown emoji, which is not a legal HTTP header value, so `undici` throws before the response exists | `doc/review/11-api-contracts.md` §5.8, §7 R11-1; `app/api/emails/preview/route.ts:36`; `emails/outbid.tsx:14-15`; `10` §7 R10-8 registers the same default-template 500 on this route. **Amended 2026-09-16:** the phase 10 fix pack removes the header that could not be encoded (`10` §5.7 — the subject renders in the body, and the route's contract for this doc is unchanged otherwise), so the 500 is fixed in the working tree; this row's own status is left to doc 11's pack.| fixed | Fixed by `10`'s pack, which removed the unencodable subject header (`10` §5.7); doc 11's pass rebuilt the route on `apiRoute`, so its refusals now carry a `code` and every response carries `x-request-id`; test `lib/emailPreview.test.ts` |
| R11-2 | 11 | correctness | `/api/activity`'s `limit` is clamped at the top only: `Math.min(parseInt(v) || 6, 20)` has no lower bound, so `?limit=-1` returns the whole feed (122 rows) while `0`, `abc` and `1e9` behave | `doc/review/11-api-contracts.md` §5.7, §7 R11-2; `app/api/activity/route.ts:16` | fixed | `app/api/activity/route.ts:21-22` (`Number.isNaN` → 6, `Math.min(Math.max(requested, 1), 20)`); test `lib/routes.test.ts:410` |
| R11-3 | 11 | correctness | Two error envelopes coexist and the failure envelope is empty: 11 routes emit `{ error, code }` plus `x-request-id`, the other 15 emit neither, and framework 405s and database-down 500s return a zero-length body with no request id | `doc/review/11-api-contracts.md` §5.3, §5.4, §5.11, §5.15, §7 R11-3; `lib/route.ts:35-46`; `lib/contracts.test.ts` pins the modern family only | fixed | `lib/route.ts` (`apiRoute` `:248`, `withContract` `:196`, `enforceContract` `:167`, `codeForStatus` `:87`, `isConnectivityFailure` `:129`, `inboundRequestId` `:137`, `jsonBody` `:150`, `notAllowed` `:217`, `allowedOptions` `:225`), all 28 `app/api/**/route.ts` converted with their handlers made private; tests `lib/contracts.test.ts` (9 boundary cases + the per-file wiring guard `:149-170`). The envelope is a **header** (`x-request-id` everywhere) plus a `code` on refusals: no 2xx body changes shape (§5.16) |
| R11-5 | 11 | correctness | `/api/report` answers a limiter rejection as success: the 11th report in an hour gets `200 {"ok":true,"note":"rate-limited"}` while every other limited route answers `429` | `doc/review/11-api-contracts.md` §5.6, §7 R11-5; `app/api/report/route.ts:13-15` | fixed | `app/api/report/route.ts:23` (429 `RATE_LIMITED` via `apiError`), `lib/api.ts:193` (`ReportResponse` narrowed to `{ok:true}`); test `lib/moderation.test.ts` (spends the hourly budget, asserts 429 with an `x-request-id` and no stored report) |
| R11-6 | 11 | correctness | `?status=` on the admin report list is an unvalidated cast — `status ? { status: status as never } : undefined` — so `?status=nope` is a 200 with an empty list while the triage route validates the same vocabulary and answers 400 | `doc/review/11-api-contracts.md` §7 R11-6; `app/api/admin/reports/route.ts:12`; `app/api/admin/reports/[id]/route.ts:20-22` | fixed | `lib/api.ts:200-203` (`REPORT_STATUSES`/`ReportStatus`/`isReportStatus`, shared by both admin report routes), `app/api/admin/reports/route.ts:20` (400 `BAD_STATUS`) and `:27-31` (`?before=` cursor with a secondary `id` sort; an unknown cursor id is `200 []`); test `lib/moderation.test.ts` |
| R12-2 | 12 | data | The denormalized `Element` trio has no independent detector: it is written and asserted inside one transaction by one function, and nothing recomputes pool/count/leader from `Stake` afterwards | `doc/review/12-data-layer.md` §5.4, §7 R12-2; `lib/recompute.ts:85-108,97-101`; `lib/reconcile.ts` is payment-shaped only | fixed | `aggregateDrift()` (`lib/recompute.ts:303`, type at `:272`) wired into `/api/jobs/reconcile` as a third always-run finding (`app/api/jobs/reconcile/route.ts:159,173`), so a drifted trio is a **503** like a divergent payment; it re-ranks with the writer's own `rankStakes` and asks the shared `ledgerInvariantFailures` (`lib/pricing.ts:188`) rather than a private copy of the rules; tests `lib/recompute.test.ts` (3 → 5), `lib/reconcile.test.ts` (9 → 10), pin repaired in `lib/phase8.test.ts` |
| R12-3 | 12 | data | `EmailLog.status` documents three values and the code writes a fourth: the schema comment says `sent / suppressed / error`, while a send with no provider key is recorded as `logged` (all 32 local rows) | `doc/review/12-data-layer.md` §5.9, §7 R12-3; `prisma/schema.prisma:371`; `lib/email.ts:36` | fixed | `prisma/schema.prisma:371` comment now `sent \| suppressed \| error \| logged`; `EmailLogStatus` (`lib/email.ts:98`) is the declared vocabulary, so a new value is a compile error where it is introduced; `lib/suppression.test.ts` repaired to write its literal *through* the type. The column is still a `String` on disk, so the database itself refuses nothing |
| R12-5 | 12 | data | Connection handling is unconfigured and unverifiable from the repository: `new PrismaClient({ log })` and nothing else, and a search for `connection_limit`, `pool_timeout`, `statement_timeout`, `pgbouncer` or `directUrl` finds documentation only | `doc/review/12-data-layer.md` §5.14, §7 R12-5; `lib/prisma.ts:5-9`; `U12-2` | fixed | `lib/prisma.ts` carries the decision and its reason: Neon's pooled endpoint is the ceiling, the client stays at Prisma's default, and a `connection_limit` on `DATABASE_URL` was rejected because it is a Vercel sensitive variable whose value cannot be read back. The measurement half is unchanged and still U12-2 (plus the `17` runbook step) |
| R12-6 | 12 | data | The strongest constraint in the schema exists only in a hand-written migration: "at most one active quote per element" is a partial unique index that `prisma/schema.prisma:205-206` can only document in a comment | `doc/review/12-data-layer.md` §5.8, §7 R12-6; `prisma/migrations/0001_phase1_ownership/migration.sql:280`; `npx prisma migrate diff` shows the drift | fixed | the written rule is in `doc/ARCHITECTURE.md` §6 ("a database is created by `prisma migrate deploy` over this directory and by nothing else", with `0009` added to its migration table) and the assertion is `lib/schema.test.ts`: the partial index is present after a fresh migrate *and still partial*, all six `0009` constraints exist with definitions that are not `NOT VALID`, and a violation is refused — so a database built by `db push` fails a test instead of silently losing the take-lead guard |
| R12-7 | 12 | data | Case is inconsistent across the read API and only one convention is pinned: `ChemicalFamily`/`PrestigeTier` are the two of nine enums with no `@map`, so `/api/elements` answers `EXOTIC_THEORETICAL` while `/api/activity` answers lowercase kinds | `doc/review/12-data-layer.md` §5.5, §7 R12-7; `prisma/schema.prisma:17-35` | fixed | the documentation arm, chosen over normalizing either side (both forms are already parsed by clients): `doc/ARCHITECTURE.md` §9 states both conventions and why each exists, `lib/contracts.test.ts` gained the three-case wire-convention describe (29 → 33), and `app/api/admin/reports/route.ts:14` notes that the operator spells the same value uppercase that `psql` shows lowercase. `11` R11-6 (the missing `?status=` validation) stays filed |
| R13-6 | 13 | correctness | A hard drain failure is reported as an empty queue: the claim is awaited outside the `try`, a throw is swallowed into `console.error`, and `drainDueWithin` then resolves `{completed: 0, failed: 0, timedOut: false}` | `doc/review/13-jobs-and-cron.md` §7 R13-6; `lib/outbox.ts:177-191,203-220` | fixed | `lib/outbox.ts:349-399` (the claim moved inside the `try`, per-iteration catch counts `errors`), `app/api/jobs/outbox/route.ts:60-72` and `app/api/jobs/screenshot/route.ts:99-115` (500 **with** the counts), `timedOut` kept separate from `errors`; §5.15, §7 R13-6 |
| R13-7 | 13 | correctness | The screenshot worker claims rows it cannot process and reports them as `checked`: no type filter on the claim, every non-`PREVIEW_GENERATE` row skipped, `checked` counted from the claim | `doc/review/13-jobs-and-cron.md` §5.8, §7 R13-7; `app/api/jobs/screenshot/route.ts:58,65,70` | fixed | `app/api/jobs/screenshot/route.ts:79-83` (`types: ["PREVIEW_GENERATE"]` on the claim), `:90` (`checked: out.claimed`); a `STAKE_ANALYTICS` row now stays due for the drain that can process it; §5.15, §7 R13-7 |
| R13-8 | 13 | correctness | The mail send carries no provider idempotency key, so the 10 s abort that converts an unanswered-but-delivered send into `"error"` also converts it into a duplicate on any retry | `doc/review/13-jobs-and-cron.md` §5.12, §7 R13-8; `lib/email.ts:46-64`; the only `Idempotency-Key` in the tree is `lib/stripe.ts:122` | fixed | `lib/email.ts:490-494` (`mailIdempotencyKey`, SHA-256 when the prefixed key would exceed 256 chars), sent at `:526-528`; three header-capturing assertions in `lib/jobsAndCron.test.ts`; bounded by the provider's 24 h window; §5.15, §7 R13-8 |
| R13-9 | 13 | ops | Money accepted without a provider cross-check is only visible in a CI log: the reconcile body counts and groups `unverified` rows and then answers 200 by design | `doc/review/13-jobs-and-cron.md` §7 R13-9; `app/api/jobs/reconcile/route.ts:64-80,90` | fixed | `.github/workflows/outbox-tick.yml:31-40` (`RECONCILE_UNVERIFIED_BASELINE: "0"`, with the run `35065018318` reading that justifies it) and `:145-151` (`::warning::` when `unverified.count` grows — the status code still does not move); §5.15, §7 R13-9 |
| R14-6 | 14 | security | A caller-supplied token is reflected into an HTML response on `/api/unsubscribe`, built by string concatenation — no exploit found, but the shape invites one | `doc/review/14-security.md` §7 R14-6; `app/api/unsubscribe/route.ts:22` | fixed | `app/api/unsubscribe/route.ts:74-83` (`UNSUB_CSP`: `default-src 'none'; base-uri 'none'; form-action 'none'` as a response header) plus `esc()` on the token; `lib/unsubscribe.test.ts` 8 → 9 (the CSP case rewritten). The site-wide `script-src` is deliberately untouched — `14` §8's unticked row says so (§5.16, §7 R14-6) |
| R14-9 | 14 | ops | One `/api/admin/*` route is authenticated but not audited: the outbox retry resets `attempts` and re-queues sends without an `AuditLog` row | `doc/review/14-security.md` §7 R14-9; `app/api/admin/outbox/retry/route.ts:14-30` | fixed | `lib/audit.ts:55` (`OUTBOX_RETRY`) and `app/api/admin/outbox/retry/route.ts:36,109` — the same transaction writes the row, with the dedupe key and the mail type in `detail` and the body's `operator` (sliced to 120) in `actorRef`; `lib/outbox.test.ts` 14 → 15; §5.16, §7 R14-9 |
| R14-10 | 14 | ops | Rejected operator credentials and exhausted limits are invisible: `lib/jobs.ts` contains no logging at all, so the highest-privilege surfaces leave no trace of a refusal | `doc/review/14-security.md` §7 R14-10; `lib/jobs.ts` (`Select-String "console"` → 0 matches) | fixed in part | `lib/jobs.ts:170-262` — `refuse()` logs route + `credentialShape()` (never a value) at `warn`, `AUTH_REJECTION_LIMIT` meters refusals per route at 60/h with a fail-closed `429`, and the twentieth raises a single escalated line; `lib/ops.test.ts` 21 → 26. The alert itself does not exist — nothing outside the repository reads those lines, which is `U14-6` (§5.16, §7 R14-10) |
| R15-4 | 15 | perf | Two of three board tabs scan `Element` for a leader flag with no index and no `take`: `Seq Scan on "Element" … Buffers: shared hit=2` at 122 rows, and neither tab bounds its read | `doc/review/15-performance-concurrency-cost.md` §5.6, §7 R15-4; `EXPLAIN (ANALYZE, BUFFERS)`; `12` §7 R12-6 (the schema's only partial index covers the other flag). **Amended 2026-09-16:** the quoted plan describes a query `/api/board` never issues — `isLeader` is a `Stake` boolean (`prisma/schema.prisma:159`), the route never reads `Element`, and `?tab=by-element` has carried `take: 20` (`app/api/board/route.ts:54`) since it was written; what survived was real (no index covered the predicate). See `15` §5.14 | fixed | `prisma/migrations/0011_leader_index/migration.sql` (hand-written partial index `Stake_isLeader_amountUsd_idx` on `("amountUsd" DESC) WHERE "isLeader" = true`, serving both leader tabs; `migrate diff` cannot express it), asserted from `pg_indexes` by `lib/schema.test.ts`, and `app/api/board/route.ts` says in place why the `take` half is **refused** (both tabs aggregate over all rows and rank after aggregating, so a `take` would silently undercount crowns and medals) |
| R16-13 | 16 | legal | The legal pages are absent from `sitemap.xml`, so the documents a buyer or a rights holder most needs have no discovery path beyond the footer | `lib/sitemapData.ts:17-26`; `app/sitemap.ts:12-15`; `16` §5.1 (sitemap probe: no `legal` URL) | fixed | `lib/sitemapData.ts`, `lib/sitemapData.test.ts` |
| R16-14 | 16 | privacy | Retention is undisclosed, indefinite where it exists and has no deletion path: mail-log and audit rows are deliberately kept and every relation is `Restrict` | `prisma/schema.prisma` (`onDelete: Restrict`); `doc/PROD-READINESS-CHECKLIST.md` §residue `:296-302`, §J6; `app/api/admin/*` (four routes, none destructive) | fixed | `lib/legalDocs.ts` (a retention sentence per category), `lib/erasure.ts`, `scripts/erase-subject.ts`, `lib/erasure.test.ts` — the standing window is D8 |
| R17-16 | 17 | testing | The rehearsal script silently passes in a degraded mode: it substitutes synthetic bodies when the secrets are unset, still prints its gate checklist, and persists no record of which blocks ran | `scripts/rehearse-release.sh:35,85-89,239,249,271`; `17` §5.6; no CI invocation | fixed | `scripts/rehearse-release.sh` (`RESULT`, `rec()`, `finish()`, degraded-coverage lines, `TOTAL n passed, m skipped`); `.gitignore` |
| R18-15 | 18 | ops | The platform's log retention (~1 h at the plan level) makes "check the logs" valid only within the same hour, and no instruction anywhere states this | `18` §5.8 documentation reading; `U18-1` settles the actual plan | fixed | `ops/alerts.md` (window + eight-row durable-substitute table); `ops/README.md`; `ops/takedown.md`; `ops/database.md`; `ops/email.md`; `ops/webhooks.md` — drains remain `D18-4` |
| R20-2 | 20 | security | The plan's "dead Turnstile CSP entries" premise is wrong: both entries are load-bearing — the widget loads from that origin and `TURNSTILE_SECRET` is a required production variable — so the entries stay | `doc/review/20-post-launch-and-debt.md` §5.6, §7 R20-2; `00-REVIEW-PLAN.md:155` (premise corrected here, not there); `components/TurnstileWidget.tsx:22`; `next.config.mjs` (`script-src` and `frame-src`); `lib/env.ts:44,76`; `doc/PROD-READINESS-CHECKLIST.md:309` | wontfix (premise corrected, §5.13) | premise corrected in place (`00-REVIEW-PLAN.md:155`, `20` §10): both CSP entries are load-bearing for the Turnstile widget, so removing them is a precondition of retiring Turnstile rather than hygiene, and the policy is unchanged |
| R20-3 | 20 | ops | Two `WHOP_*` variables still sit in Vercel Production though the code does not read them and the stated precondition for deleting them (Stripe proven) has been met | §5.7, §7 R20-3; `doc/PROD-READINESS-CHECKLIST.md:311,355,437`; `prisma/schema.prisma:54-63` (the enum values must outlive the vars); `lib/env.ts` (no `WHOP_*` in `REQUIRED_PROD_ENV:39-49`) | accepted-risk (reason in `20` §9) | — |
| R20-12 | 20 | ops | Growth experiments are unmeasurable while the analytics script does not serve: the north star and the funnel exist on paper with no experiment log, hypothesis list or channel attribution | §5.9, §7 R20-12; `doc/ROADMAP.md:433-434`; `doc/phase-5-launch/01-seed-instrument.md:12-13`; `19` §5.6 and `19` §7 R19-1 | fixed in part (blocked on `R19-1`) | `doc/phase-5-launch/03-launch-week-questions.md` — seven questions, each with the `psql` query that answers it, all seven run against Postgres before being written down, plus a README pointer; the readings themselves are blocked on R19-1 |
| R20-13 | 20 | content | The v2 backlog is properly gated and the gate depends on a metric nobody can read: the rule needs a stable funnel and a reclaim-email CTR, and neither can be observed today | §5.9, §7 R20-13; `doc/phase-6-v2/README.md:4,6,9-10,12`; `doc/ROADMAP.md:418-419`; `doc/phase-6-v2/01-badges-alerts-api.md:7`; blocked on `19` §7 R19-1 | open (by design) | `doc/phase-6-v2/README.md`'s two maintenance items now carry what settles them — the `2026-12-31` review date, the `$50k` trigger and the `/api/admin/ops` reader — with the gate itself unchanged |

## UNKNOWN — evidence not yet obtainable

| ID | Phase | Category | Unknown | What settles it |
| --- | --- | --- | --- | --- |
| U01-1 | 01 | seo | Whether each platform actually renders the element card (image, or text only) | Paste `https://www.periodictable.lol/elements/H` into a private X post draft, a Slack DM, a Discord DM, an iMessage to yourself and the LinkedIn Post Inspector, and read the rendered card; delete any public post afterwards |
| U01-2 | 01 | seo | Whether Google has the sitemap and whether the 122 element pages are indexed | Search Console → verify the `www` URL-prefix property → Sitemaps → submit `https://www.periodictable.lol/sitemap.xml` → read Pages/Coverage for the element URLs |
| U01-3 | 01 | seo | Whether a leader domain of ~30+ characters overflows the OG card's 640 px text column (no clamp in the route) — **settled 2026-09-14:** the card clamps the domain to 24 characters with an ellipsis (`clampDomain`, `lib/ogCard.ts`) and sizes the headline against a fixed 760 px column, so no domain can reach the edge; `lib/ogCard.test.ts` asserts the cap. The original probe is kept for the record | `curl -s https://www.periodictable.lol/og/<sym> -o card.svg` for a symbol whose leader has a long domain (needs the first real stake), or substitute the text in a local copy of the SVG and measure the text element's bounding box in a browser |
| U02-1 | 02 | ux | What the shell looks like at 360/390/768/1024/1440 px — rail vs sheet, and whether the footer and the stale pill clear each other | Capture those five widths of `/`, `/legal/about`, `/legal/nope` and `/zzz-not-a-page` on a host whose browser writes PNGs (this one's Chrome 152 does not, §5.8) |
| U02-2 | 02 | a11y | Keyboard-only traversal of the live shell: tab order, focus visibility, and Escape ownership with a sheet and a modal open | Tab through `/` with no mouse — the contracts are asserted in `lib/a11y.test.ts` but never traversed on the running site |
| U02-3 | 02 | ux | What a client render throw actually shows, given no error boundary exists (R02-3) | Force a render error on a preview deploy with a temporary `throw` — it edits app code, so it needs a go-ahead |
| U02-4 | 02 | ux | How long the pre-hydration `$5 · unclaimed` face stays on screen (R02-4) | DevTools → Slow 4G plus 6× CPU throttle on `/`, capturing at first paint and after hydration |
| U03-1 | 03 | ux | All visual rendering — tile faces, the pod, the sheen, gestures — and the screen-reader half of R03-4 | `chrome --headless=new --screenshot=out.png --window-size=1440,2400 <url>` per §4 state |
| U03-2 | 03 | perf | Cold mobile load and LCP against the budget; desktop is measured (§5.6) | DevTools phone viewport with 4× CPU throttling, or `npx lighthouse <url> --output=json` |
| U03-3 | 03 | correctness | What a claimed tile, a missing logo or a hidden listing renders — the one production write this doc defers until a go-ahead | Two stakes on a throwaway DB, then read `/api/elements` and `/api/stats`; residue 2 `Stake` rows |
| U03-4 | 03 | ux | Whether `?el=Hbar` deep-links and scrolls without JS | `curl.exe -sS <url>/?el=Hbar` for the face, a browser for the scroll |
| U04-1 | 04 | data | Whether the `ActivityLog` rows carry `deltaUsd`/`resultTotalUsd` or the pre-Phase-3 NULLs, and what the feed prints for each | `psql "$DATABASE_URL"`, SQL in a temp `.sql` piped in (`-c` breaks on PascalCase) — read only |
| U04-2 | 04 | correctness | Whether any production `Stake` row belongs to a `HIDDEN` startup, which decides R04-2's reach from static-only to live | Same connection: `SELECT count(*) FROM "Stake" s JOIN "Startup" u ON u.id = s."startupId" WHERE u."moderationState" = 'HIDDEN';` |
| U04-3 | 04 | money | The claimed-state render, the receipt shape and the real $5 charge on the element path | J4 — one real card payment, then `/elements/<sym>` and the Stripe dashboard; residue 1 `Payment`, 1 `Stake`, 1 `ActivityLog` |
| U05-1 | 05 | a11y | Whether the 22 `text-money` sites render ≥18.66 px bold, which 3.00–3.25:1 passes, or are failing small text | `getComputedStyle` over `.text-money` at 1280/360 px |
| U05-2 | 05 | a11y | Whether the white `.exotic-tile:focus-visible` outline clears 3:1 on every exotic theme, not only the worst — **settled 2026-09-14:** the outline is drawn 2 px outside the tile border box, so it is read against the board's dark stage and not against the tile fill (white 20.17:1 at the top gradient stop, 11.38:1 at the lightest), and the only light surface it can touch is a 1 px overlap onto a neighbouring tile in the 3 px gutter, where the worst family pastel `#9F7BEF` gives 3.20:1 — above 1.4.11's 3:1 with no margin, so no theme puts the ring on a failing backdrop (`05` §5.4, `05` §7 R05-2) | Focus each theme's tile and sample the outline against its background |
| U05-3 | 05 | a11y | Tab order, focus visibility and 1.4.10 reflow at 400 % zoom on the live shell | A browser run — blocked on this host by U02-1 (`02` §5.8) |
| U06-1 | 06 | ux | What the modal looks like and does on a phone — whether the keyboard covers `Continue to checkout`, and whether the four client-gate silent bails are noticeable in practice | A browser run at 360×640 and 390×844 with the keyboard raised, opening the stake modal, typing only a handle and pressing Continue; the unit half needs `@testing-library/react` + jsdom, neither of which is in `package.json` |
| U06-2 | 06 | ops | Whether a real visitor's Turnstile challenge ever fails in normal use on the production host (the rate of `Bot check failed.`) | The production 400 rate for `/api/checkout` — Vercel logs filtered on that response; client-side, a `turnstile` error-callback counter |
| U06-3 | 06 | ops | Whether `?canceled=Li` is reached by real buyers often enough to matter (its finding is a code fact either way) | Vercel request logs for `GET /?canceled=*` over a week of live payments, or a Stripe session list filtered on `cancel_url` |
| U06-4 | 06 | ops | Whether any password manager in the wild fills the `name="website"` honeypot for this form | A production counter on the honeypot branch (none exists) — the settling change is the counter, which is code, so this stays an operator call |
| U07-1 | 07 | ops | Whether the production Stripe key currently configured in Vercel is valid, and whether the endpoint `we_1UFVsvQ8DCQQIpW5GtOvFfws` is currently delivering | A dashboard reading of the endpoint's delivery rate plus one `GET /v1/balance` with the production key |
| U07-2 | 07 | money | Whether a real card completes on this integration (session creation is verified only against Stripe's rejection path) | One test-mode session paid with a test card, then the webhook walk in `08` §5 against that row |
| U07-3 | 07 | security | Whether `nextUrl.origin` can be forced to an attacker host by a proxy in front of the app — the success and cancel URLs are built from it | A request through the production proxy with `Host`/`X-Forwarded-Host` variants and a read of the resulting session's `success_url`; in-process probes here resolved to the local origin in every variant |
| U07-4 | 07 | ops | The dashboard-side state of the two credentials and the endpoint (key labels, roll history, `WHOP_*` variable deletion) | Operator screenshot/export of the Vercel and Stripe configuration pages |
| U08-1 | 08 | ops | Whether a real Stripe endpoint retries the 500s this route returns, for how long, and whether the dashboard's endpoint view shows the failures an operator needs to see | A Stripe dashboard reading of the endpoint's delivery attempts after one forced `settle-retryable` 500, or `stripe events resend` against a registered endpoint |
| U08-2 | 08 | correctness | Whether real Stripe payloads for `charge.refunded` / `charge.dispute.created` carry a *partial* amount, and whether this route would treat a partial refund as a full unwind | A live refund for less than the captured amount, or Stripe CLI `stripe trigger charge.refunded` with an edited amount, then a read of the resulting stake totals |
| U08-3 | 08 | correctness | Whether `MONEY_TX` behaves as designed when the database connection drops mid-transaction on the deployed provider (Neon) — whether the delivery is retried or the reservation is left decayed | A managed-Postgres failover during a settle probe, or a connection-kill test against the production-shaped database |
| U08-4 | 08 | data | How many `ProviderEvent` rows in production are `ERROR` today, and which reasons dominate | A production DB read of `ProviderEvent` grouped by outcome and detail — needs credentials no review session holds |
| U09-1 | 09 | ops | How often R09-1's floor-plus-hold lockout is hit by real buyers | A production read of `ClaimReservation` joined to the leader's amount at the same instant, or a week of `ActivityLog` `CHECKOUT_*` rows carrying `code: RESERVATION_CONFLICT` |
| U09-2 | 09 | data | Whether a real (test-mode Stripe) buyer has ever settled a take whose reservation had expired | `select count(*) from "Payment" p join "ClaimReservation" r on r."paymentId" = p.id where p.path = 'TAKE' and p.status = 'PAID' and r.status = 'EXPIRED' and r."expiresAt" < p."paidAt"` against a database with paid rows |
| U09-3 | 09 | data | What the applied rank was at the moment a late take settled — the rank is not recorded anywhere | Adding the settled rank to the settlement detail, or inspecting `select type, detail from "AuditLog" where type like '%PAYMENT%' order by "createdAt" desc limit 20` on a production-like database to see whether the rank can be reconstructed from the timestamps |
| U09-4 | 09 | correctness | Whether the tie guard can be bypassed a second time on an element that already carries a tied pair | Re-run the two-simultaneous-claims probe of `09` §5.7 against a database where `Ho` already carries two equal rows: two `POST /api/checkout` calls at the same amount from different IPs, both paid through `POST /api/dev/pay`, then read `GET /api/elements/Ho` for a third equal row; no production data is touched by the local run |
| U10-1 | 10 | ops | Whether mail from `periodictable.lol` authenticates: SPF, DKIM (provider key), DMARC policy, and whether the provider's sending domain is verified — nothing in this checkout can read the zone | `dig +short TXT periodictable.lol`, `dig +short TXT <selector>._domainkey.periodictable.lol`, `dig +short TXT _dmarc.periodictable.lol`, plus the provider console's domain-verification status; any missing record is a deliverability defect regardless of the provider's own reporting |
| U10-2 | 10 | ops | Whether a receipt, an outbid notice and a waitlist confirmation actually arrive, in which folder, and how the from-name and footer render — needs a real inbox and a send that is not the `"logged"` branch | Set `RESEND_API_KEY`/`EMAIL_FROM` on a non-production deployment, trigger one settle and one waitlist join against a real mailbox, then read the delivered message source for `Authentication-Results` (SPF/DKIM/DMARC verdicts) and check the spam folder; the `EmailLog` rows give the provider-side status |
| U10-3 | 10 | ops | Whether the production `EMAIL_FROM` is the default (`lib/email.ts:60`) or an explicit value, and whether `RESEND_API_KEY` is set at all — the difference between mail leaving the building and every send taking the `"logged"` branch (`:41`) | Read the production deployment's environment (`RESEND_API_KEY`, `EMAIL_FROM`) and the required findings of its `/api/jobs/config`; then send one message and confirm a provider message id exists |
| U10-4 | 10 | ops | Whether any address hard-bounces or marks a message as spam today, and whether any production `EmailLog` row is already `error`; without a bounce webhook (R10-7) the only source is the provider console or the production log table | Provider console → the sending domain's bounce/complaint counts; and a production read of `SELECT status, count(*) FROM "EmailLog" GROUP BY 1` plus the rows where `status = 'error'` |
| U10-5 | 10 | ops | Whether the manage-link disclosure (R10-2) has ever been used against a listing: any production `ManageToken`/`ManageSession` row, any `PROFILE_UPDATED` audit row whose actor was a token holder, and any unexplained customer-facing listing change | Production reads: `SELECT count(*) FROM "ManageToken"`, `SELECT count(*) FROM "ManageSession"`, `SELECT * FROM "AuditLog" WHERE action IN ('MANAGE_LINK_REQUESTED','MANAGE_LINK_CONSUMED','PROFILE_UPDATED') ORDER BY "createdAt"`, cross-checked against owner reports of edited copy |
| U10-6 | 10 | content | Whether the receipt's "watch 🟢 clicks delivered climb on your profile" (`emails/receipt.tsx:29`) is true as rendered: `/s/ladder-b.dev` serves 200 and exposes `clicks`, but the label, the green dot and whether the number moves after a `/go` click were not verified this pass | Buy a stake, click its tile once, and read `/s/<domain>` before and after: the counter must read one higher and carry the label the mail describes |
| U11-1 | 11 | correctness | What `/api/admin/*` returns on success (report list shape, triage echo, moderation echo) | `curl -H "Authorization: Bearer $ADMIN_TOKEN" https://www.periodictable.lol/api/admin/reports` from a shell that holds the operator secret |
| U11-2 | 11 | data | Whether a real Resend send returns the same status the skipped path records | One send with `RESEND_API_KEY` set, then `SELECT status FROM "EmailLog" ORDER BY "createdAt" DESC LIMIT 1` |
| U11-3 | 11 | ops | Whether Vercel's edge rewrites any of these bodies or headers (405 bodies, empty 500s, `Vercel-CDN-Cache-Control`) | `curl -i https://www.periodictable.lol/api/report` (405) and any 5xx observed live; compare with `11` §5.3 |
| U11-4 | 11 | perf | Route latency against Neon rather than a local socket | The same route matrix against the deployed host, or a Neon branch with this checkout pointed at it |
| U11-5 | 11 | ops | ~~Whether anything actually calls `/api/jobs/config`~~ — **settled, `13` §5.13**: `.github/workflows/outbox-tick.yml:78-98` calls `config` and `reconcile` on a `*/10` tick and is their only caller; the residual unknown is that tick's *real* cadence and whether an external pinger exists (`U13-1`, `U13-4`) | `gh run list --workflow=outbox-tick.yml`; the pinger's own console |
| U12-1 | 12 | data | Whether a Neon PITR restore actually works, how long it takes, and what a restored database looks like — the plan asked for a dated drill *result* and this is not one; destructive and credentials-bearing, so it is operator-run by policy as well as by access | The commands in `12` §5.14, run by the operator against a Neon branch |
| U12-2 | 12 | data | Live-Neon schema state, connection ceiling, and per-table production row counts today | `npx prisma migrate status`, plus `SHOW max_connections` and `SELECT count(*) FROM pg_stat_activity` against production — read-only, but needs `DATABASE_URL`, a Vercel sensitive variable |
| U12-3 | 12 | perf | Whether any index is genuinely unused rather than merely unused at 34 rows — scan counters cannot distinguish "no query needs it" from "no query is big enough yet" | `pg_stat_user_indexes` after production has a few thousand stakes and a week of real traffic |
| U12-4 | 12 | data | Whether `migrate deploy` applies cleanly over the *production* rows rather than over an empty database — an empty-database replay cannot catch a backfill that collides with existing data | A production dump restored into a throwaway database, then `npx prisma migrate deploy` against it |
| U12-5 | 12 | data | How a migration behaves while the app serves traffic — lock duration, whether a deploy can hang on `ALTER TABLE`, and what a second concurrent deployment sees | Apply a pending migration against a database under write load and time it; the `0003`-style whole-table `UPDATE` is the class to test |
| U12-6 | 12 | privacy | What PII actually accumulates in production over time — which addresses, how many, and whether anything has already been requested for deletion | The PII inventory query from `12` §5.11 run against production (read-only), plus the operator's own record of data-subject requests, which is not in the repository |
| U13-1 | 13 | ops | Whether the GitHub tick ever reaches its nominal `*/10` cadence on a busier day — 5.1 % is measured over 110 h with a median gap of 3 h 12 m, and the mechanism (shared CI scheduler, best-effort) cannot be fixed from the repository | `gh run list --workflow=outbox-tick.yml --limit 200` over a longer window, and the run list's own start times |
| U13-2 | 13 | ops | Whether the two `vercel.json` crons actually fire on the current deployment, and what Vercel does with a failing invocation (retry, or nothing) | Vercel dashboard `Project → Settings → Cron Jobs` history for `periodictable*`; read-only, needs production access |
| U13-3 | 13 | ops | Whether GitHub disables the scheduled workflow after 60 days of repository inactivity, and whether anyone is notified when it does | The repository's Actions tab after a 60-day quiet period; the repository was pushed to on 2026-09-15, so the clock is far from expiry |
| U13-4 | 13 | ops | Whether an external pinger (cron-job.org or similar) exists and is pointed at the four job URLs — the decision record makes it optional and nothing in the checkout can see an external service | The pinger's own console / URL list (operator reading) |
| U13-5 | 13 | ops | The exact number of sends a platform kill at 30 s costs: the row keeps its lease and self-heals, but whether the in-flight send had already left Resend is not observable from here | One real 30 s kill in production (`vercel logs` for the invocation) plus the `EmailLog` rows around it |
| U13-6 | 13 | data | The production queue's depth, attempt distribution and `EmailLog.status` mix — `13` §5.7 reads a scratch database replayed from migrations, so its 115/0/0/32 figures describe the probe, not the product | `GET /api/jobs/outbox` with credentials on production (its body reports claimed/completed/failed/checked/updated) |
| U14-1 | 14 | money | Whether any `Payment` row is in flight (`provider = STRIPE`, unsettled) and the `PaymentProvider` distribution — the fact that decides whether R14-1's failure mode strands an existing buyer or only creates free ones | Operator, Neon SQL: `SELECT provider, status, count(*) FROM "Payment" GROUP BY 1,2` |
| U14-2 | 14 | security | Whether `ADMIN_TOKEN` and `PAYMENTS_LIVE` are set in production, and consistent with each other | `vercel env ls production` (names only, never values) plus the payment link rendered on `/`; the fail-closed `403` proves nothing about presence |
| U14-3 | 14 | security | Whether `RESEND_API_KEY`, `UPSTASH_REDIS_REST_URL`/`_TOKEN` and `TURNSTILE_SECRET` are set in production — decides whether R14-3 delivers and whether the limits are shared across instances | Vercel → Logs at process start: the single `rate-limit: no shared store configured … using instance-local` line appears only when Upstash is absent |
| U14-4 | 14 | security | Whether any alias of a preview deployment is reachable without Vercel Authentication | Enumerate `gh api repos/danilgorbunofff/PeriodicTable/deployments` (and `…/deployments/<id>/statuses`) and `curl -sS -o NUL -D -` each host, expecting `302 → vercel.com/sso-api`; one branch-alias host answers without SSO if R14-7 is reachable |
| U14-5 | 14 | security | Which request header this deployment sets as the client identity, i.e. whether `x-forwarded-for`/`x-real-ip` can be trusted as the limiter key | One request per header value against a harmless production route and comparison of limiter behaviour, or the Vercel project's edge configuration; PROD answers `Server: Vercel` with no `cf-ray`, so `cf-connecting-ip` is client input (`14` §7 R14-2) |
| U14-6 | 14 | security | Whether Vercel adds `Access-Control-Allow-Origin: *` to any `/api/*` response besides `/` | Header sweep: `curl -sS -D - -o NUL` over all 26 production routes (`14` §5.1 captured `/` and `/api/stats`; `/` has it, `/api/stats` does not). Matters only if a cookie-authenticated route is added later |
| U14-7 | 14 | security | The last-rotation dates of `STRIPE_WEBHOOK_SECRET`, `CRON_SECRET`, `ADMIN_TOKEN` and `CLICK_SALT` (and whether `CLICK_SALT` is set at all — unset means the literal `"ptl-dev-salt"` is hashed into every `Report.ipHash`) | Provider consoles plus a dated line per secret in the runbook; **values are never read or recorded** |
| U14-8 | 14 | security | Whether a real browser blocks a foreign-host `logoUrl` render, and whether a future build keeps `script-src` free of `'unsafe-inline'` | Browser matrix against a listing whose `logoUrl` points at a foreign host (needs a manage session, i.e. after the v2 manage pages ship); the CSP half is a build-time check of the emitted headers |
| U15-1 | 15 | perf | Field LCP / INP / CLS for `/` and one element page on a cold 4G handset — the plan's own first line for this doc | CrUX dashboard or the PageSpeed Insights API for `https://www.periodictable.lol/`; `npx lighthouse https://www.periodictable.lol/ --output=json` from a quiet connection |
| U15-2 | 15 | perf | The real concurrency ceiling, i.e. how many simultaneous settlements one element tolerates on production infrastructure | A staging deployment with production-shaped data plus `k6`/`artillery` at 25, 50 and 100 virtual users on one symbol, recording p95 and the 5xx rate — **not** runnable against production or from this host |
| U15-3 | 15 | perf | Neon's contribution to cold start, its pooled-connection ceiling and its region latency — `12` §5.14 owns the pooling facts and the restore drill; `15` §5.13's ceiling depends on them | The production `DATABASE_URL` (`doc/PROD-READINESS-CHECKLIST.md:196,436` shows the read pulls back empty) plus a Neon branch latency probe |
| U15-4 | 15 | money | What a *slow* or briefly-5xx Stripe does to a checkout and to the webhook — the branch `15` §5.10 could not produce without a real account | A Stripe test-mode key plus a fault-injecting proxy in front of `api.stripe.com`, or Stripe's own dashboard "failed payments" view after any real incident |
| U15-5 | 15 | ops | What a slow Resend does to a 30 s job invocation and to the 15 s inline drain | A Resend test key plus artificial delay (proxy or Resend sandbox) on one settle; observe `OutboxEvent` depth afterwards, which is the number `13`'s cron depends on |
| U15-6 | 15 | ops | Real spend: the Vercel, Neon, Resend and Upstash lines for a launch month, Stripe's own fee schedule, and which of them the accounts actually bill at the tiers assumed in `15` §5.12 | Provider consoles and the first invoice, read next to §5.12's model inputs (5.6 kB and 11 rows per settlement) |
| U15-7 | 15 | perf | The animation cost of `components/TableCamera.tsx` on the 122-tile grid, on a mid-range phone | A device profiler (Chrome DevTools performance trace, 4× CPU throttle) on `/`, recording long tasks during a camera move; `components/TableCamera.tsx` is read, never profiled, in this review |
| U15-8 | 15 | perf | How many ISR writes the element routes actually incur, i.e. whether §5.12's largest cost line is real | Vercel's "ISR writes" usage metric over a week, compared with `symbols_reached_each_minute × 1440 × 30`; if it is anywhere near 5 M/month, that is a product decision about the 60 s window |
| U15-9 | 15 | ops | Whether any real request has ever hit the body-less 500s of R15-3, and what the production 5xx rate is | Vercel → Logs filtered on status ≥ 500 over a week, plus an alert on 5xx rate; the log lines will name the Prisma model (as in §5.10) but carry no request id |
| U16-1 | 16 | legal | Whether the Stripe account has a tax registration and how Managed Payments files; whether a statement descriptor is configured; whether Stripe acts as merchant of record for this account as `lib/stripe.ts:40,47,55` assumes | Stripe dashboard → **Settings → Tax**: registration list and "Stripe manages tax filing" state; **Settings → Payments → Statement descriptor**; **Settings → Emails → Receipts** — record the three readings with a date |
| U16-2 | 16 | privacy | Whether `abuse@`, `payments@` and `hello@` actually receive mail (inbound routing, not just a verified sending domain) | Resend dashboard → Domains (verified records, reply-to) **and** the Cloudflare Email Routing / mailbox rules for `periodictable.lol` — paste the routing table into the finding |
| U16-3 | 16 | privacy | Which region and role the Neon database and the Vercel deployment use, and whether a DPA exists for each processor | Neon console → Project settings → Region/roles; Vercel → Project settings → Functions region; the processors' DPA acceptance pages in the operator's accounts |
| U16-4 | 16 | privacy | The production retention baseline: how many `Payment.email`, `ClickEvent`, `EmailLog.to` and `AuditLog` rows exist today, and their age spread | With `DATABASE_URL` set, one read: `psql "$DATABASE_URL" -c "select (select count(*) from \"Payment\"), (select count(*) from \"ClickEvent\"), (select count(*) from \"EmailLog\"), (select count(*) from \"AuditLog\"), (select min(created_at) from \"ClickEvent\");"` — no `.env` exists in this worktree, so it was not run |
| U16-5 | 16 | legal | Whether the "bid"/competition vocabulary, and any lottery- or prize-competition-shaped reading of it, has been seen by counsel (there is no memo in `doc/`) | A dated counsel note in `doc/`, or the operator's recorded decision (`16` §9 D9) |
| U17-1 | 17 | ops | The Stripe account's dispute state and its delivery-retry policy; whether the webhook endpoint's signing secret has ever been rotated; whether managed payments forms the same account | Stripe dashboard → **Developers → Webhooks → endpoint → signing secret** (age/rotation history), **→ recent deliveries** (status codes and retry schedule), **Payments → Disputes** (open disputes, deadlines), **Settings → Team** — record each with a date |
| U17-2 | 17 | ops | Which humans hold Vercel, Neon, Stripe, Resend, Cloudflare, GitHub and the `ADMIN_TOKEN`, and who is the second holder for each | Each console's **Settings → Team/Members** page, plus a written handover note committed to `doc/`; the token value is never read out — only its holder list |
| U17-3 | 17 | ops | The Neon project's PITR/history window on the current plan, whether branch restore is enabled, and which role may authorise it | Neon console → **Project → Settings → History/restore window** (and **Backup & restore**); then a rehearsal restore into a preview branch, timed and recorded |
| U17-4 | 17 | ops | The production environment-variable list: which of the 9 required and 2 advisory names are set, in which environment, and when each last changed | Vercel dashboard → **Project → Settings → Environment Variables** (names, environments, updated-at) — the config endpoint proves *satisfaction* only (`17` §5.2) |
| U17-5 | 17 | ops | Whether a failed run of `outbox-tick.yml` reaches a human at all (watchers, notification settings, mobile) | GitHub → the workflow's **watchers/notification recipients** plus the account holder's **Settings → Notifications**; then a deliberate failure drill and a stopwatch |
| U17-6 | 17 | ops | What the site actually serves while the database is unreachable, and whether the edge caches it | A drill (or a preview pointed at a stopped branch): load `/`, `/api/stats`, `/api/activity` and a checkout, and record status + body for each — this pass could not run it (no console access) |
| U18-1 | 18 | ops | The plan level of the Vercel project and therefore the runtime-log retention window available in an incident | Vercel dashboard → project → Settings → Plan; then Logs for a past window to see the oldest retrievable entry |
| U18-2 | 18 | ops | Whether any log drain, integration or external log sink is configured | Vercel dashboard → project → Settings → Integrations (and → Log Drains if present) |
| U18-3 | 18 | ops | Whether an uptime monitor exists at all, what it polls (the site root? `/api/jobs/config`? `reconcile`?), at what interval, and to whom it alerts | the monitor's own console (cron-job.org job list or equivalent) → each job's URL(s), schedule and notification settings |
| U18-4 | 18 | ops | Who receives the GitHub Actions failure emails for `outbox-tick.yml` | GitHub → the repository → Settings → Notifications / the account's notification settings; plus the workflow's own watchers |
| U18-5 | 18 | money | Whether the live Stripe key is in live or test mode, and whether webhook delivery has failed recently (Stripe disables endpoints after sustained failure) | Stripe dashboard → Developers → Logs (key mode) and → Developers → Webhooks → the endpoint's delivery attempts and status |
| U18-6 | 18 | money | Whether Stripe's own notifications for failing endpoints and chargebacks are enabled, and to which address (the product's `REPORT_NOTIFY_EMAIL`/`EMAIL_FROM` are separate) | Stripe dashboard → Settings → Notifications (and Business settings → email recipients) |
| U18-7 | 18 | ops | Whether any `ProviderEvent` `ERROR`/`DUPLICATE` rows, failed `EmailLog` rows or `AuditLog` rows exist in production right now, and their counts | a `psql` session against production with the `18` §3.8 SQL (no production reader exists) |
| U18-8 | 18 | ops | What a production 500 actually looks like in the platform log (fields, verbosity, presence of the route and status) | Vercel dashboard → the deployment → Logs, filtered to a known 500 from any route |
| U19-1 | 19 | ops | What the announcement actually is: channel, account, copy and tagged link — nothing in the repository records it, and the file the runbook names for it does not exist (`19` §5.9-§5.10, R19-9) | Write it, post it, then append the channel, date and URL to `19` §11 |
| U19-2 | 19 | ops | Whether `abuse@`, `payments@` and `hello@` deliver and are answered, which decides whether the 72-hour and 2–3-day promises are true (`app/legal/[slug]/page.tsx:110,122`) | Send one message from an outside address to each, then read the replies — and the Resend dashboard for delivery events |
| U19-3 | 19 | ops | Whether `NEXT_PUBLIC_PLAUSIBLE_DOMAIN` is set in Vercel Production (for a Sensitive value `vercel env ls` proves presence only) and which property the seven events land in | Vercel → Environment Variables (read, do not display); set if absent → redeploy → `curl -s https://www.periodictable.lol/ \| Select-String plausible` ≥ 1 → Plausible → Realtime |
| U19-4 | 19 | seo | How the `www` property is verified, given no token is served: whether a DNS TXT record exists at all (`19` §5.9) | Search Console → Settings → Ownership verification, plus the registrar's DNS page or `dig TXT www.periodictable.lol` |
| U19-5 | 19 | ux | Whether a zero-stake board reads as "new and open" or "broken" to a stranger — the empirical half of `19` §9 Q1, and the input to R19-3's priority | The T+24h funnel once R19-1 is fixed; failing that, a five-person five-second test on the empty and seeded versions, recording which word they use first |
| U20-1 | 20 | ops | Whether the two `WHOP_*` variables are still present in Vercel Production, and what else the Production env list holds | Vercel → Project → Settings → Environment Variables → Production (read-only), compared against `lib/env.ts:39-49` and the ledger's inventory (`checklist:437`) |
| U20-2 | 20 | data | The Neon plan's actual PITR window and branch retention, and whether any restore has ever been rehearsed | Neon console → Project → Settings (history/restore window) + a search of `doc/` for a dated drill record — `ops/rollback.md:44-49` currently states the promise, not a result |
| U20-3 | 20 | ops | Whether an external pinger (cron-job.org or similar) exists at all, and which URLs it calls | The pinger's dashboard → job list, checked against the four paths `HANDOFF.md:815-817` names; the code comments assert the design (`app/api/jobs/reconcile/route.ts:33-44`) but cannot prove a job exists |
| U20-4 | 20 | ops | Whether a red `outbox tick` run reaches a human outside GitHub's own e-mail | GitHub → account notification settings → Actions, plus the repository's webhook/integration list; the workflow defines `permissions: {}` and no notification step (`.github/workflows/outbox-tick.yml:17`) |
| U20-5 | 20 | security | Whether the Whop app and API key still exist and whether the key can still be used by anyone | Whop dashboard → Apps/API keys; if the app exists, revoke the key and record the date in `20` §11 |
| U20-6 | 20 | ops | Whether the advisory assessment was actually re-run after the file's own commit — the entry says "Assessed 2026-09-16" while the file was committed 2026-09-14T15:14:17Z (`c75ca9b`) and today is 2026-09-15 | Re-run `npm run audit:prod` and the two `/_next/image` probes from `checklist:314` against production, then correct the entry's date in the same commit as the result |
| U20-7 | 20 | money | The Stripe dashboard's current dispute, refund and payout state, and whether the webhook endpoint still carries all seven registered event types | Stripe → Developers → Webhooks → the `we_1UFVsv…` endpoint → event list, and Stripe → Disputes / Refunds / Payouts, compared against `checklist:355` |
| U20-8 | 20 | ops | The real cadence of the job tick since 2026-09-11 (the ledger's reading is ~7 %, 6 runs in 14 h 30 m, and predates the diagnostics wiring) | GitHub → Actions → `outbox tick` run history, or `gh run list --workflow=outbox-tick.yml` with an authenticated `gh`; R20-7's `20` §9 Q4 decision should be made from this number, not from the nominal `*/10` |

## Operator decisions

Thirty-six questions that only the operator can answer — nine from `16` §9, seven from `17` §9, eight
from `18` §9, five from `19` §9 and seven from `20` §9 (of which Q1, Q5 and Q7 stay open, with the reasons in that doc's §9) — recorded **2026-09-16** during those phases' fix passes.
software's half is already done — what is recorded here is what was decided, what was left to the operator, and
when the software stopped being able to guess. `D20-4`…`D20-6` are decisions this pass took rather than questions put to anyone; they carry ids because a decision without one is the kind that gets re-litigated.

| ID | Phase | Decision | Resolution |
| --- | --- | --- | --- |
| D16-1 | 16 | The legal entity, the jurisdiction, the address that can be served, and whether an imprint is required | **Recorded as an operator decision, software half done (2026-09-16).** The About page renders `OPERATOR_NAME`/`OPERATOR_ADDRESS`/`OPERATOR_COUNTRY`/`OPERATOR_LAW` as a labelled block behind the labels "Operator", "Postal address", "Country of establishment" and "Governing law and venue", prints `OPERATOR_UNPUBLISHED` for each unset value, and lists the unset names as `operator`-severity findings in `/api/jobs/config`. Only the operator can supply the values (R16-11, `16` §9 D1, §5.10) |
| D16-2 | 16 | The tax registration and display: registered or not, whether Managed Payments' merchant-of-record filing is the whole answer, and whether prices are tax-inclusive | **Recorded as an operator decision, software half done (2026-09-16).** The position that holds without the reading is written once and rendered on the Rules page, at checkout and on the receipt, with `automatic_tax`/`invoice_creation`/`tax_id` deliberately unset so nothing is asserted about the operator's own filing. The registration facts are U16-1 (R16-4, `16` §9 D2, §5.10) |
| D16-3 | 16 | The refund window and the withdrawal-right waiver wording | **Recorded as an operator decision, no change made (2026-09-16).** The `05` R05-6 wording stands as corrected and **no** waiver sentence was added, because writing waiver language before the jurisdiction and the policy are confirmed is the one kind of copy that can make things worse. The evidence layer an argument would need (attestation, text hash, timestamp) is stored with every payment (R16-6, `16` §9 D3) |
| D16-4 | 16 | Whether 18+ stays attestation-only, and what the operator does when a charge is disputed by a parent | **Recorded as an operator decision, software half done (2026-09-16).** No age gate was added — an attestation everyone ticks costs a conversion and deters nobody — and the attestation now persists with its version, text hash and time so a dispute has a record of what was asserted. The refund-or-contest answer is `17`'s dispute runbook (R16-6, `16` §9 D4) |
| D16-5 | 16 | Whether to launch with seeded demo listings and, if so, what the About page may claim | **Recorded as an operator decision, software half done (2026-09-16).** Seeded rows are marked in the API and labelled in the UI on every surface, retiring the moment a real payment exists, and the About clause admits they were never paid for. The policy itself is shared with `19`, whose §9 Q1 has since answered it the same way (R16-9, `16` §9 D5, §5.10; `D19-1`) |
| D16-6 | 16 | Analytics: whether to enable Plausible, and if so whether a notice or a gate is owed — and what happens to the Google favicon and the Microlink hover preview | **Decided 2026-09-16: disclosed, off, no consent gate.** Plausible stays unset, so no script loads and no gate is owed; the seven events are named on the privacy page with the lawful basis a self-hosted, cookie-less, IP-truncating reading would rest on, so enabling it later is a disclosed act. The other two thirds are closed outright: the icon fetch moved server-side and the hover preview is gone, so a page view causes no third-party request (R16-3, `16` §9 D6, §5.10) |
| D16-7 | 16 | The statement descriptor, the mailbox that is legally the operator's, and the address or agent that can be served | **Recorded as an operator decision, software half done (2026-09-16).** The descriptor has a validated 5–22-character grammar, is printed in the refunds paragraph and on the receipt when set, and its absence is an `operator` finding; the contact page states the abuse process without invoking the DMCA. The dashboard value is U16-1, the mailboxes U16-2, the payer lookup `17`'s (R16-5, R16-8, `16` §9 D7) |
| D16-8 | 16 | Retention periods per category and what happens on a deletion request | **Recorded as an operator decision, software half done (2026-09-16).** The privacy page states a retention sentence per category and the deletion path; `lib/erasure.ts` + `scripts/erase-subject.ts` (R12-4) keep the promise, with the window a required `days` argument so the number is stated at the moment of the run rather than guessed (R16-14, `16` §9 D8) |
| D16-9 | 16 | Whether to keep the `bid`/`stake`/`own`/`crown` vocabulary, and whether the lottery-shaped reading has been seen by counsel | **Decided 2026-09-16: keep the words, correct the definition, send the memo.** `OWNERSHIP_SENTENCE` and `STAKE_SENTENCE` now define the vocabulary as a ranking on an advertising board with no sellable, redeemable or inheritable interest, and render where the words are used. Whether the characterisation survives a lawyer's reading is U16-5, whose input is `16` §5.9 (R16-10, `16` §9 D9, §5.10) |
| D17-1 | 17 | Who is on call, what response time is promised, and what pages them | **Recorded as an operator decision, software half done (2026-09-16).** No rota was invented and no alarm belongs here — the alerting surface is `18`'s. What the runbooks now do is print the number the promise rests on beside the promise: the tick's measured cadence (46 runs in a day, a median gap of 141 min, a worst gap of 6 h 40 m) sits next to the `<24 h` triage and 72 h settlement-promise language, so a reader can see the clock they are being held to. Who is named, in which order, and for which hours is the operator's. |
| D17-2 | 17 | Who may authorise a destructive run — a seed, a wipe, a restore — and how the authorisation is recorded | **Recorded as an operator decision, software half done (2026-09-16).** The software refuses first: `lib/seedGuard.ts` blocks `--fresh` against anything that looks like production without `--allow-remote`, and the demo clear path needs `--apply --allow-remote --confirm=<host>` plus a typed confirmation. What is *not* in the code is whether one person may do both halves, or where the host name is quoted from; `ops/database.md` writes the step as "quote the host from the dashboard", which is a procedure, not a permission. |
| D17-3 | 17 | Who holds the operator token, where it is kept, and how a leak is revoked | **Recorded as an operator decision, software half done (2026-09-16).** `ADMIN_TOKENS` accepts a comma-separated list of named tokens and `operatorIdentity()` records the name in the audit row, so a call is attributable to a person rather than to "the operator"; a single token still works, unnamed. Custody — who has which string, in which password manager, and what happens on the day someone leaves — is the operator's, and `U17-2` is the console read that would show how it is going. |
| D17-4 | 17 | Whether operators get a console, or keep running `curl` from a runbook | **Recorded as an operator decision, no change made (2026-09-16).** No admin page was built. The middle position holds: every operator action stays a copy-pasteable call in `ops/takedown.md` with the token scheme stated at the top of the file and the response you should expect beside it. That is a worse interface and a much smaller attack surface than a console that renders private report text and can send mail to a stranger; if one is wanted, it is a phase of its own and not a paragraph of this one. |
| D17-5 | 17 | Who may approve a refund, a credit or a disputed charge, and to what limit | **Recorded as an operator decision, software half done (2026-09-16).** `ops/refunds-and-disputes.md` states both clocks (the Stripe dispute deadline and the card network's, and how far they differ), the evidence pack to assemble, and the ledger reads that settle a claim — so the decision can be made from the file. No approval threshold was invented: the customer-facing window and the waiver wording are `D16-3`'s, and a limit this pass made up would have been the one number nobody agreed to. |
| D17-6 | 17 | Whether to enable Upstash, i.e. whether the rate limit is meant to hold across instances | **Recorded as an operator decision, software half done (2026-09-16).** The blast lever is real and bounded — `POST /api/admin/startups/moderate-batch` takes up to 50 domains per call and writes one audit row for each — so a wave can be cleared in a handful of calls whether or not the limiter holds. The edge lever that would blunt it earlier is written into `ops/abuse-wave.md` §2 and needs Upstash, which stays unset; the config report's own word for that state is `degraded`. |
| D17-7 | 17 | Whether the site has a status page, who speaks for it, and in what words | **Recorded as an operator decision, software half done (2026-09-16).** Three templates — acknowledged, investigating, resolved — are written into `ops/comms.md` with the channels they are sent on, an assertion table for what may be claimed, and a rule for how the message is signed. Nothing is published: a status surface nobody keeps current is worse than no status surface, and an incident is announced by the same call whether or not one exists. |
| D18-1 | 18 | The error-tracking vendor and plan, or whether to accept self-hosted rows plus ~1 h platform logs | **Recorded as an operator decision, software half done (2026-09-16).** The sink is self-hosted — `ErrorReport` via `/api/internal/error`, fed by both process handlers, every unhandled API-route error and the browser boundaries — so "platform logs only" is no longer the default by inaction, and rows outlive the hour. Forwarding them to a vendor is spend and data-processing terms (`16` §7), not code. |
| D18-2 | 18 | The alert channel, its recipient list, and the thresholds that mean "wake me up" | **Recorded as an operator decision, software half done (2026-09-16).** `ops/alerts.md` names the six signal classes with a numeric threshold each, the alarm codes with severities, the expected status for each of the five URLs and the derived stale-tick bounds; the channel column is deliberately blank because no address, alias or phone number may be invented. |
| D18-3 | 18 | Whether analytics run at all, and with which vendor, consent posture and notice | **Recorded as an operator decision, software half done (2026-09-16).** Analytics are off and now gated: `NEXT_PUBLIC_PLAUSIBLE_DOMAIN` no longer enables the script, `lib/analyticsConsent.ts` fails closed, the vendor loads only after an explicit Allow, and no funnel number comes from a browser (`R18-14` computes it server-side). The legal pages were updated in the same change. |
| D18-4 | 18 | Whether to buy log drains or a longer-retention tier | **Recorded as an operator decision, software half done (2026-09-16).** The durable substitute exists and is documented — `ops/alerts.md` maps each log question to a table that outlives the hour, `ErrorReport` included — so drains are an improvement rather than the only way to reconstruct an incident. Cost against incident value is the operator's arithmetic; `U18-1` supplies one of its terms. |
| D18-5 | 18 | Whether to run an uptime monitor at all, and if so its interval, URL list and escalation path | **Recorded as an operator decision, software half done (2026-09-16).** What a monitor needs exists: `/api/health` with a distinguishable `db` field, unauthenticated by design, the documented expected status for each of the five URLs, and `?ok=1` on the ops route for a monitor that wants 503. The monitor itself is outside the repository (`U18-3`). |
| D18-6 | 18 | Who is on call, and what "on call" means for a solo launch | **Recorded as an operator decision, software half done (2026-09-16).** The pass fixed how much time the human has rather than who the human is: the stale-tick bounds (26 h for the daily workers, 13 h 20 m for the rest) are derived, printed and alarmed. For a solo launch `ops/alerts.md` states the honest default — the monitor emails and nothing pages. |
| D18-7 | 18 | Whether the launch-day dashboard is a manual SQL checklist or an operator-only page, and who may run it | **Recorded as an operator decision, software half done (2026-09-16).** `/api/admin/ops` serves every §3.8 row — money, outbox, mail, provider events, errors, funnel, cost — and `/api/admin/audit` the activity row, each behind the existing bearer. Whether that becomes the launch-day procedure or stays a checklist is the operator's call; `ops/alerts.md` carries the checklist form either way. |
| D18-8 | 18 | Whether a customer-facing status or communication surface exists for site-down events | **Recorded as an operator decision, software half done (2026-09-16).** The templates and the facts each must contain are in `ops/comms.md`; nothing customer-facing was published, deliberately, because a status page nobody owns is worse than none. Whether one exists, where it lives and who writes the post decides where those templates point. |
| D19-1 | 19 | Whether the launch is announced onto a seeded board or an empty one, and what the announcement may therefore claim | **Decided 2026-09-16: seed before announcing.** `19` §9 Q1 asked it and `19` §11 recorded it; `D16-5`'s labelling rule is what keeps the claim honest, because the seeded rows stay provenance-marked in the API and labelled `demo` in the UI until a real payment exists. Both halves of the contradiction the pass found are corrected in the tree — `doc/phase-5-launch/README.md:4`'s goal line and `01-seed-instrument.md`, which now says which half the repository implements and which half waits on the operator — and the review plan's stale pointer (`00-REVIEW-PLAN.md:152`) now points at the review that asks the question instead of calling seeding the ledger's open decision. Running `npx tsx prisma/launch-seed.ts` at T-30 m, with `npm run db:clear-demo-data --apply --allow-remote --confirm=<host>` as the abort path, is the operator's step (R19-2, U19-5) |
| D19-2 | 19 | Where the launch is announced, and in whose name — no product account exists and nothing in the repository records a channel | **Recorded as an operator decision, software half done (2026-09-16).** No worktree can invent the channel, so the pass made the untagged-link mistake structurally impossible instead: `twitterSite(env)` in `lib/shareMeta.ts` is the single place a handle is configured and returns nothing when unset, the card and the element page consume it, and the runbook carries the `utm_source`/`utm_medium`/`utm_campaign=launch` convention so the announcement's link is measurable the moment it exists. One primary channel owned by the operator's own account, plus the one community where the audience already is, with `Show HN` held until the board is non-empty; the account, the copy and the post are U19-1 (R19-9, `19` §9 Q2) |
| D19-3 | 19 | Who watches the launch for how long, and what they are pre-authorised to do | **Recorded as an operator decision, software half done (2026-09-16).** One named human for four hours from T-0, pre-authorised to flip the kill switch without asking, then the same readings once at T+24 h. The readings are three URLs and an inbox now — `/api/admin/ops` carries money, funnel, outbox, mail, provider events and errors in one response and `/api/admin/audit` the activity row — so the watcher needs a bearer rather than a SQL session. The name is deliberately left as a NO-GO box in `19` §8, because no reading substitutes for the decision (R19-1, R20-6, R20-7) |
| D19-4 | 19 | Where the FAQ lives, and whether it is a fifth legal document | **Decided 2026-09-16: its own `/faq` route, and not a legal document.** `app/faq/page.tsx` renders ten answers whose numbers are interpolated from `lib/pricing.ts` and whose payment sentences are quoted from `lib/legalDocs.ts`; `LEGAL_SLUGS` and `LEGAL_LINKS` still hold four, so the terms-version surface and the footer's four one-word labels are unchanged, and the route is appended last in the sitemap so the legal block's indices stay stable. It is reached from the help modal, the checkout modal and both branches of the element page (R19-4, R19-5, `19` §9 Q4) |
| D19-5 | 19 | Whether `02-launch-gate-runbook.md` is corrected or declared superseded | **Decided 2026-09-16: correct it, because the operator runs it tonight.** Stripe replaces Whop, Upstash is named once as not used, the phantom `hidden=true` column becomes `moderationState = HIDDEN` through admin triage, the comms file that does not exist is replaced by the announcement convention, step 0 (`check-prod-env.mjs` + `audit:prod`) is in the order, and `ops/rollback.md` is declared authoritative for the switch. The six gate boxes stay unticked on purpose: they are launch-morning probes, and a box ticked from a worktree would be the same kind of lie the file had been telling (R19-8, `19` §9 Q5) |
| D20-1 | 20 | Whether to keep accepting the Next 14 criticals until 2026-12-31, and whether the tripwire may stay a re-read | **Answered 2026-09-16: yes, while the tripwire holds.** The acceptance's own weakness was that it lived in prose plus a memory; `ops/accepted-advisories.json` now writes it as data (`file`, `must_not_contain`, `because`, one entry per acceptance with its own `expires`) and `scripts/audit-prod.mjs` runs `checkTripwires()` before it reports the audit, so `npm run audit:prod` fails closed on a moved or voided premise. The migration is not attempted and `2026-12-31` is not extended (`20` §9 Q6, §5.13; R20-1) |
| D20-2 | 20 | Does the game ever end — and what may the product say about it | **Answered 2026-09-16: no end date, with the board's floor published.** `SERVICE_TERM_SENTENCE` states both halves — stakes never expire, and the board runs at least until the published date — and renders beside the price, in the rules and in the FAQ. R20-14's own proposed wording ("a board with no end date") was deliberately not used: it was written when no floor existed, and a permanent promise with no date is the one a buyer cannot check (`20` §9 Q3, §5.13; R20-14) |
| D20-3 | 20 | What happens to holders' money if the site is shut down | **Answered 2026-09-16: a published term rather than a paragraph in a plan.** `SERVICE_TERM.until` is `2027-09-09` and extends by publishing the new date, so the floor is a commitment the product can be held to; a halt is announced 30 days ahead with checkout off from the notice day; stakes are not refunded on wind-down and never expire regardless. The rules and the FAQ carry it and `ops/comms.md` carries the notice as a template, which is what let `19` §8's CR-20.4 box be ticked rather than annotated (`20` §9 Q2, §5.13; R20-15) |
| D20-4 | 20 | The six dead `PENDING` rows and the three stake-less `PAID` rows: clear, or disclose | **Disclosure taken 2026-09-16; the clearing decision is left to the operator.** `doc/PAYMENT-LEDGER-SHAPES.md` records the five shapes, the reproduction query and the enum trap where a ledger reader meets them, and `lib/opsMetrics.ts` says which of its totals may be shown to a user; nothing was deleted or altered, because the rows are the only record that those checkouts were attempted. Q1's clear-or-leave half therefore stays open, and CR-20.5 needs a date in §11 before anyone who is not the operator reads the table (`20` §9 Q1, §5.13; R20-4, R20-5) |
| D20-5 | 20 | Whether this register's own mechanisms stay prose, or become tests | **Decided 2026-09-16: tests.** `lib/postLaunch.test.ts` (27 cases) pins what the pass claims — the tripwire's fields, order and expiry-per-acceptance, the budget split, the two cron entries and their schedules, the term's four published homes with the printed and ISO dates agreeing, the runbook citations that must name tests which exist, `ops/alerts.md`'s bound table against `HEARTBEAT_ROUTES` and `HEARTBEAT_STALE_MS` row by row, and the absence of a stray `D14` — and `lib/jobsAndCron.test.ts` gained four database cases that call the composite the way the platform does, including every leg's heartbeat row. A register whose claims are not testable is the debt CR-20.1 exists to name (`20` §5.13) |
| D20-6 | 20 | Which decision the ops runbooks were citing as `D14` | **Defect corrected 2026-09-16: the id is `D17-5`.** No `D14` exists in this file, and four ops files cited it as the authority for the refund-approval limit — a citation that would have sent a reader looking for a decision that was never made. `ops/refunds-and-disputes.md`, `ops/database.md`, `ops/takedown.md` and `ops/comms.md` were repointed, with one deliberate `D14` mention kept as the explanation of the mistake (`20` §5.13; R20-10) |
| D20-7 | 20 | Vercel Hobby's two cron slots: the 10-minute pinger, or a daily entry for the diagnostics | **Answered 2026-09-16: neither as written — the second and last slot runs one composite.** Vercel Hobby allows two cron jobs and both were already spent (`doc/PROD-READINESS-CHECKLIST.md:28`), so `30 4 * * *` `/api/jobs/daily` drains previews on `DAILY_PREVIEW_BUDGET_MS` and then invokes `/api/jobs/config` and `/api/jobs/reconcile` in-process through their own handlers — one `x-request-id` across all three legs, every leg stamping its own heartbeat and the run stamping one for itself, and a 500 that names the failing legs. The pinger stays available and uncreated: its question is cadence (~7 % → ten minutes), not the only coverage the diagnostics have, and Q4 remains the standing invitation to create it (`20` §9 Q4, §5.13; R20-7) |

## Summary

| Doc | Phase | Status | P0 | P1 | P2 | P3 | Last reviewed |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 00 | Review plan | draft | — | — | — | — | 2026-09-14 |
| 01 | Discovery and unfurl | draft — fixes applied | 0 | 0 | 3 | 5 | 2026-09-14 |
| 02 | Shell and static surfaces | draft | 0 | 0 | 5 | 2 | 2026-09-14 |
| 03 | The board | draft | 0 | 0 | 0 | 4 | 2026-09-14 |
| 04 | Element detail and pricing | draft — fixes applied | 0 | 0 | 1 | 3 | 2026-09-14 |
| 05 | Accessibility and content | draft — fixes applied | 0 | 0 | 2 | 6 | 2026-09-14 |
| 06 | Checkout before payment | draft — fixes applied | 0 | 1 | 3 | 6 | 2026-09-15 |
| 07 | Payment provider integration | draft — fixes applied | 2 | 0 | 2 | 3 | 2026-09-15 |
| 08 | Settlement and ledger integrity | draft — fixes applied | 0 | 1 | 3 | 3 | 2026-09-15 |
| 09 | Ownership and competition | draft — fixes applied | 0 | 0 | 3 | 4 | 2026-09-15 |
| 10 | Email and notifications | draft — fixes applied | 1 | 1 | 5 | 4 | 2026-09-16 |
| 11 | API contracts | draft — fixes applied | 0 | 0 | 1 | 5 | 2026-09-16 |
| 12 | Data layer | draft — fixes applied | 0 | 0 | 2 | 5 | 2026-09-16 |
| 13 | Jobs and cron | draft — fixes applied | 0 | 1 | 4 | 4 | 2026-09-16 |
| 14 | Security | draft — fixes applied | 1 | 2 | 5 | 3 | 2026-09-16 |
| 15 | Performance, concurrency, cost, resilience | draft — fixes applied | 0 | 6 | 6 | 1 | 2026-09-16 |
| 16 | Legal, privacy, tax | draft — fixes applied | 1 | 8 | 3 | 2 | 2026-09-16 |
| 17 | Operator tooling and runbooks | draft — fixes applied | 0 | 7 | 8 | 1 | 2026-09-16 |
| 18 | Observability, analytics, alerts | draft — fixes applied | 0 | 9 | 5 | 1 | 2026-09-16 |
| 19 | Launch and marketing readiness | draft — fixes applied | 0 | 2 | 7 | 0 | 2026-09-16 |
| 20 | Post-launch and debt | draft — fixes applied | 0 | 1 | 10 | 4 | 2026-09-16 |
